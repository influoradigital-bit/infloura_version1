# Schema Changes Log (Vikram — Backend)

Every Prisma/Flyway migration is logged here per team rule (no migration without a log entry).

| Date | Migration | Table | Change | Reversible / Data impact | Task |
|------|-----------|-------|--------|--------------------------|------|
| 2026-09-18 | `V20260918180000__brand_ai_credit_escrow_funded_month.sql` | `brand_ai_credits` | `ALTER TABLE brand_ai_credits ADD COLUMN escrow_funded_month DATE NULL;` | Additive, nullable, no backfill. Every existing row starts `NULL` = "no funded launch has been calendar-month-gated yet," so a workspace's next funded launch (in whatever UTC month is current then) is correctly treated as the first one this guard has seen, not a retroactive claim about launches funded before this column existed. | T-GOLIVE-0918-R2 lane CREDITS-2 (Vikram, Swapnil-approved go-live round 2) — kabir round-1 MEDIUM finding on `27ca63b`: a CALENDAR_MONTH workspace (Free/comp/ex-Pro, no billing period to gate on) still refilled unconditionally on EVERY funded launch, so a brand OWNER/ADMIN could repeat a full refill indefinitely via a fund-then-refund loop (`EscrowService#refund`). `AICreditService#applyEscrowFundedReset`'s calendar branch now calls the new `BrandAiCreditRepository#applyEscrowFundedResetOncePerCalendarMonth`, guarded forward-only (`IS NULL OR < :firstOfMonth`) on this SEPARATE marker — deliberately not `last_reset`/`topUpOnJoinCalendarClock`'s guard, since a funded launch is a funding event, not the ordinary monthly reset, and must not be coupled to (or suppress) that guard's own semantics. Same round also split the unlimited-window write out of `applyEscrowFundedResetOncePerPeriod`'s guarded UPDATE into a new, unconditional, monotonic `extendUnlimitedWindow` — a second funded launch in the SAME billing period is still a credit no-op, but it must still extend the campaign's live window (the ruling caps the refill, not the window) — and fixed `SubscriptionService#reconcileAiCreditAllotment`'s plan-allotment sync to call `getPlanForCreditSync` instead of `getActivePlanForWorkspace`, closing a hole where a status-only PAST_DUE webhook synced a grace-period Pro brand's `plan_allotment` down to Free (a later 1-credit refund would then clamp the balance DOWN to Free's allotment instead of adding 1). Regression coverage: `AICreditClockRepairRoundTest` (`probeF`/`probeG`/`probeH`, H2 real-service), `AICreditServiceTest`, `BrandAiCreditRepositoryQueryTest`; `AICreditClockIntegrationTest` (new, Testcontainers MySQL, skips without Docker) added for scenario 11's TIMESTAMP-rounding case. |
| 2026-09-18 | `V20260918170000__brand_ai_credit_escrow_funded_period_end.sql` | `brand_ai_credits` | `ALTER TABLE brand_ai_credits ADD COLUMN escrow_funded_period_end TIMESTAMP NULL;` | Additive, nullable, no backfill. Every existing row starts `NULL` = "no funded launch has been billing-period-gated yet," so the very next funded launch for any workspace is correctly treated as the first one this guard has seen, not a retroactive claim about launches funded before this column existed. | T-CREDITCLOCK-0918 repair round (Vikram, Swapnil-approved go-live lane) — same-day follow-up ruling from Swapnil, relayed after the clock decision doc (`wiki/decisions/2026-09-18-ai-credit-clock.md`) shipped: a funded launch (`AICreditService#applyEscrowFundedReset`) now refills AT MOST ONCE PER BILLING PERIOD for a `BILLING_PERIOD`-clock workspace, "like upgrades" — reversing that decision doc's earlier "not decided here, refills on every funded launch" note for that clock only. A CALENDAR_MONTH workspace (Free/comp/ex-Pro) is unaffected and keeps refilling on every funded launch. Deliberately a SEPARATE marker from `credit_grant_period_end` (`BrandAiCreditRepository#applyEscrowFundedResetOncePerPeriod`, guarded `IS NULL OR < :periodEnd`) so the decision doc's scenario 9 ("a funded launch on Pro -> 450 without touching `credit_grant_period_end`") still holds and this guard cannot suppress, or be suppressed by, the billing-refill primitive's own once-per-period grant. Repair round also fixed, in the same lane: `refillForBillingPeriod`'s WHERE guard (`<>` → `<`, an older out-of-order period could otherwise re-grant); `applyEscrowFundedReset`'s plan-allotment sync now calls `SubscriptionService#getPlanForCreditSync` instead of `getActivePlanForWorkspace`, which was mapping a PAST_DUE grace-period Pro brand to Free mid-grace; `reconcileAiCreditAllotment` gained a `confirmedPeriodEnd` overload so `applySubscriptionWebhookUpdate` only triggers a billing-period refill when THIS delivery actually carried a period, instead of trusting whatever stale `currentPeriodEnd` the row already held (e.g. an upgrading Free row's calendar anchor); and `resetForNewCycleIfDue` moved its same-month guard from a Java read-then-write into one SQL-guarded atomic UPDATE (`BrandAiCreditRepository#calendarResetIfDue`), closing a TOCTOU window ShedLock only narrows. **Not decided/not fixed in this round** (flagged, not fixed — a product call, not this lane's to make): whether a repeated `grantAdminPlan` comp grant of the SAME plan within a month should also be capped; it still calls the unconditional `resetForNewCycle` on every grant. |
| 2026-09-18 | `V20260918160000__backfill_credit_grant_period_end.sql` | `brand_ai_credits` | Data-only backfill (no DDL): `UPDATE brand_ai_credits bac INNER JOIN subscriptions s ON s.workspace_id = bac.workspace_id INNER JOIN plans p ON p.id = s.plan_id SET bac.credit_grant_period_end = s.current_period_end WHERE bac.credit_grant_period_end IS NULL AND s.is_comp = FALSE AND p.code = 'PRO' AND s.status IN ('ACTIVE', 'PAST_DUE')`. | Idempotent, non-destructive — scoped to `credit_grant_period_end IS NULL` only (a second run, or a workspace this build's own refill path already touched, matches zero rows). Only sets the marker column; no `credits_remaining`/`monthly_allotment` value is touched. | T-CREDITCLOCK-0918 (Vikram, Swapnil-approved go-live lane) — `wiki/decisions/2026-09-18-ai-credit-clock.md` §4 "Backfill". Every workspace already on the BILLING_PERIOD clock (`SubscriptionService#creditClockFor`) whose `credit_grant_period_end` marker was never set would otherwise fail `BrandAiCreditRepository#refillForBillingPeriod`'s `IS NULL` guard branch and receive one extra refill on its next billing-period event — this backfill pre-seeds the marker to the row's own `current_period_end` so that guard is accurate from the first real event onward. |
| 2026-07-18 | `V20260718160000__creator_tier_override.sql` | `creator_profiles` | Add nullable `tier_override VARCHAR(20)` (values NANO/MICRO/MID/MACRO enforced app-side via `CreatorTier.valueOf`, not a DB CHECK — matches `@Column(length=20)` under `ddl-auto=validate`), `tier_adjusted_by VARCHAR(26)` (FK → `admin_users.id`), `tier_adjusted_at TIMESTAMP`; index on `tier_override`. | Additive + nullable only. No existing rows rewritten; NULL override = keep the follower-derived tier (current behavior preserved). No destructive/backfill step. | Admin `creatorApi.adjustTier` (PUT /admin/creators/{id}/tier) |
| 2026-07-18 | `V20260718180000__workspace_phone.sql` | `workspaces` | `ALTER TABLE workspaces ADD COLUMN phone VARCHAR(30) NULL AFTER billing_email;` | Additive + nullable only, no default, no backfill. Existing rows get `phone = NULL`. | Brand Settings > General phone field (`WorkspaceService#updateMyWorkspace`/`getMyWorkspace`, `PATCH`/`GET /workspaces/me`) — approved by Swapnil, follow-up to the L-9 workspace read/update endpoints which had flagged phone as unpersistable (no column). |
| 2026-07-18 | `V20260718190000__campaign_hype_config.sql` | `campaigns` | `ALTER TABLE campaigns ADD COLUMN hype_config JSON NULL AFTER campaign_type;` | Additive + nullable only, no default, no backfill. Every existing campaign is non-HYPE today; `hype_config` stays NULL for all of them. | Priya task: persist the Hype campaign's defining config block (`brand-new-hype-campaign.tsx` `hype` block) instead of silently dropping it on save. `campaign_type` itself already existed (V30) — only the config block needed storage. |
| 2026-07-21 | `V20260721160000__meera_interaction_log.sql` | `meera_interaction_log` (new table) | New append-only flywheel-logging table: `id`, `workspace_id`, `session_id` (nullable), `event_type` VARCHAR(32) (`OPTIONS_PRESENTED\|OPTION_TAPPED\|DRAFT_CREATED\|DRAFT_FUNDED\|REVISION_REQUESTED` — `DRAFT_ABANDONED` deliberately excluded, no live trigger today), `tool_name` (nullable), `recommended_flag` BOOLEAN (nullable), `campaign_id` (nullable, no FK), `revision_reason` TEXT (nullable, redacted via `SensitiveTextRedactor` before insert), `prompt_version` VARCHAR(32) (nullable — see migration comment), `created_at`. Indexes on `(workspace_id, created_at)` and `campaign_id`. | Pure new table, no existing data touched. No FK on `campaign_id`/`workspace_id` (event-log convention, matches `portfolio_events`/`creator_nudge_log`). Retention: 180 days policy (Priya B7 ruling), no purge job yet — v1 write-only, no read query. | Phase 2 item 2.3 (`wiki/ai-review/meera-label-to-moat-build-plan.md` §2.3) — flywheel logging for the "options presented → tapped → draft → funded" funnel + revision-reason capture. Design: `wiki/build/phase2-backend-design.md` §3; reviewed `wiki/build/phase2-priya-review.md` / `phase2-ash-review.md`. |
| 2026-07-28 | `V20260728120000__backfill_escrow_hold_collaboration_id.sql` | `escrow_holds` | Data-only backfill (no DDL): `UPDATE escrow_holds eh INNER JOIN payment_milestones pm ON pm.id = eh.milestone_id SET eh.collaboration_id = pm.collaboration_id WHERE eh.collaboration_id IS NULL AND eh.milestone_id IS NOT NULL`. | Idempotent, non-destructive — only touches rows where `collaboration_id IS NULL`; a hold with no `milestone_id` (campaign-level pool funding) has nothing to join against and is correctly left untouched/NULL. Safe to re-run (second run matches zero rows). | CRITICAL money-path fix: `wiki/tech/escrow-frozen-hold-fix-spec.md` (Fix 4 of 4). See `wiki/errors/CR-22a-withdrawal-money-path-audit.md` finding #2 — `EscrowService.initiateFund` never set `collaboration_id` for any hold created through the ordinary brand escrow flow, which made `requireFrozenHoldsForCollaboration`'s (pre-fix, non-fallback) lookup silently iterate an empty list during dispute settlement. Fix 2 (code, same task) now binds `collaboration_id` at creation for every new hold; this migration backfills the pre-existing rows so the column itself tells the truth going forward, on top of Fix 1's now-fallback-aware lookup. |
| 2026-08-09 | `V20260809120000__brand_kyc_prompt_dismissed.sql` | `users` | `ALTER TABLE users ADD COLUMN kyc_prompt_dismissed BOOLEAN NOT NULL DEFAULT FALSE;` | Additive, NOT NULL with a DEFAULT — matches `email_verified`/`phone_verified`/`onboarding_completed`'s existing boolean convention on `users` (no nullable tri-state). Every existing row backfills to `FALSE` = "not dismissed yet," i.e. current behavior (prompt always shows until skipped) is preserved for everyone. No backfill step, no data rewritten beyond the DEFAULT fill. | OB-1 (`BrandF.md` §105/§91) — KYC prompt re-prompted across devices because dismissal lived only in `localStorage`. Backs `POST /onboarding/brand/kyc-prompt-dismissed` + the `kycPromptDismissed` field on `GET /onboarding/brand/status`'s `OnboardingStatusResponse`. |
| 2026-08-17 | `V20260817140000__contract_terms_text.sql` | `contracts` | `ALTER TABLE contracts ADD COLUMN terms_text TEXT NULL COMMENT '...';` — new column, distinct from the pre-existing `terms` column (mapped to `Contract.termsJson`, a SHA-256 tamper hash of the generate request, not terms). | Additive + nullable only, no default, no backfill (no source of truth exists to backfill real terms for already-generated contracts — same reasoning as `V20260817130000` for signer names). Every existing contract row reads `terms_text = NULL` going forward, honestly "none on file." | F-0283 (`contract-terms-never-persisted`, T-BRANDOPEN-0817) — `ContractGenerateRequest` gained an optional `terms` field (free-text, `@Size(max=20000)`), threaded through `ContractService#generate` into `Contract.termsText` and read back out on `ContractResponse#terms()`. Immutable by construction: `Contract` exposes no post-construction setter for `termsText` (only `Contract.Builder#termsText` at construction time) and `ContractController` has no terms-editing HTTP surface — gated statically by `.proof-os/gates/F-0283-contract-terms-persisted.sh`. No default clause set was authored (product/legal decision, out of scope). |
| 2026-09-03 | `V72__meera_creator_deal_terms.sql` | `collaborations`, `campaigns` | `collaborations`: 7 new columns — `usage_months INT NULL`, `usage_perpetual BOOLEAN NOT NULL DEFAULT false`, `usage_channels VARCHAR(255) NULL` (comma-separated `UsageChannel` names), `exclusivity_days INT NULL`, `exclusivity_scope VARCHAR(32) NOT NULL DEFAULT 'NONE'`, `exclusivity_brands TEXT NULL` (JSON array), `max_revisions INT NOT NULL DEFAULT 2`. `campaigns`: `end_brand_name VARCHAR(200) NULL`, `end_brand_category VARCHAR(100) NULL`. | Additive only — every new column nullable or DEFAULTed, no backfill. `end_brand_name`/`end_brand_category` are enforced NOT-blank at the DTO/service layer (`CampaignService.create`) for NEW campaigns only, deliberately NOT a DB NOT NULL (no legal value exists for pre-existing rows). | T-MEERA-CREATOR-PHASE-A, A2 — structured deal terms on `Collaboration` (`Collaboration.applyDealTerms`, cleared on `revive()`) + end-brand identity on `Campaign`. Wired into `POST /deals`, `POST /deals/{id}/counter` (`DealDtos.DealTermsDto`, nested `dealTerms` field — verified against `src/lib/types.ts`'s `DealTerms`/`UsageChannel`/`ExclusivityScope`, exact match) and campaign create/update (`CampaignDtos.CampaignWriteRequest`/`CampaignPatchRequest`). |
| 2026-09-03 | `V73__creator_agent_preferences.sql` | `creator_agent_preferences` (new table) | New table, one row per creator: `id`, `creator_id` (FK → `creator_profiles.id`, UNIQUE), rate floors (`reel_floor`/`story_set_floor`/`post_floor` DECIMAL(12,2) NULL — null until first GET computes defaults), `excluded_categories`/`blocked_brands` (JSON TEXT), `approval_level INT NOT NULL DEFAULT 0`, `creator_language VARCHAR(10) NOT NULL DEFAULT 'hi-IN'`, `brand_tone VARCHAR(16) NOT NULL DEFAULT 'FRIENDLY'`, `working_hours_start`/`working_hours_end` (nullable hour ints), `working_days` (JSON TEXT), `weekly_sponsored_limit` (nullable int), `represented BOOLEAN NOT NULL DEFAULT false`, `agency_name` (nullable), `consent_accepted_at TIMESTAMP NULL` (A6 DPDP gate — null = not consented), `created_at`/`updated_at`. Index on `creator_id`. | Pure new table — no existing data touched. Rows are created lazily (`CreatorAgentPreferencesService.getOrCreatePreferences`), not backfilled here; a creator who has never opened Meera simply has no row. `ON DELETE CASCADE` from `creator_profiles`. | T-MEERA-CREATOR-PHASE-A, A3/A6 — `CreatorAgentPreferences` entity, `CreatorAgentPreferencesRepository`, `GET`/`PUT /creator/agent-preferences`, `POST /creator/agent-preferences/consent`. **Info barrier (A7)**: this is the repository `InfoBarrierTest` asserts no Brand-named class under `service/meera`/`web` may import — it holds the exact floor numbers SPEC.md bans from ever reaching a brand. |
| 2026-09-03 | `V74__meera_creator_conversations.sql` | `meera_creator_conversations` (new table) | New table: `id`, `creator_id` (FK → `creator_profiles.id`), `conversation_id` (FK-by-convention → `ai_conversations.id`, UNIQUE — a 1:1 index, not a fan-out), `started_at`, `last_message_at`, `message_count INT NOT NULL DEFAULT 0`. Indexes on `creator_id` and `(creator_id, last_message_at DESC)`. | Pure new table. `ON DELETE CASCADE` from `creator_profiles`. **Known gap**: nothing in this migration's owning slice writes to this table on a real Meera turn yet — see TASKS.md's "Known gaps" section (the write hook belongs on the CREATOR-audience chat-turn persistence path, `MeeraSessionService`/influora-ai `chat.py`, both outside this task's file ownership). | T-MEERA-CREATOR-PHASE-A, A6 — DPDP list/export/delete over a creator's Meera conversations (`CreatorAgentConversationService`, `GET`/`DELETE /creator/agent-preferences/conversations/**`). Ownership enforced via this table's `creator_id`, not `AiConversation.workspaceId` (which carries the creator's USER id for a CREATOR turn, a different id space than `creator_profiles.id` — see the service's class javadoc). |
| 2026-09-03 | `V20260903130000__admin_email_send_lock.sql` | `admin_email_send_lock` (new table) | New singleton-row table: `id VARCHAR(26) PRIMARY KEY` (one fixed row, `id='SINGLETON'`, seeded by the migration's own `INSERT`), `updated_at TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)`. Entity `AdminEmailSendLock`, repository `AdminEmailSendLockRepository#lockForUpdate` (`SELECT ... FOR UPDATE`, `jakarta.persistence.lock.timeout=10000`). | Pure new table, one seeded row, no existing data touched. Nothing else in the codebase ever inserts a second row here. | T-ADMINMAIL-0903 round 3, B1 (`REVIEW-R2.md` ship-blocker) — `AdminCustomEmailService.enforceRateLimit()` was a lock-free read-then-decide (`findTopByOrderByCreatedAtDesc()`), so two concurrent `send()` calls could both read the same stale "last campaign" and both pass the persisted rate limit. `send()` now takes this row's lock FOR UPDATE before checking the rate limit, serializing the whole rate-limit-check-through-commit critical section across every admin and every app instance. Proven by a real Hibernate+H2 concurrency test (`AdminEmailSendLockRepositoryConcurrencyTest`), not a mock — see that test's class javadoc for why. |
| 2026-09-03 | `V20260903140000__admin_email_campaigns_queued_count.sql` | `admin_email_campaigns` | `ALTER TABLE admin_email_campaigns ADD COLUMN queued_count INT NULL AFTER skipped_unsubscribed;` then backfilled to `GREATEST(recipient_count - skipped_unsubscribed, 0)` for every existing row, then `MODIFY COLUMN queued_count INT NOT NULL`. | Additive, backfilled in the same migration so the column can be `NOT NULL` immediately (matches the existing reconstruction formula exactly, so no existing campaign's replay response changes). No destructive step. | T-ADMINMAIL-0903 round 3, B5 (`REVIEW-R2.md`) — `recipient_count` is the pre-unsubscribe audience size, not what was actually queued; the audit log's own `queued` detail already recorded the true figure, so the two records could silently disagree. `AdminCustomEmailService.send()` now writes `queuedCount = toEnqueue.size()` directly, and `replaySendResponse` reads the stored column instead of reconstructing `recipientCount - skippedUnsubscribed`. |

| 2026-09-03 | `V20260903150000__creator_agent_preferences_ai_monthly_cap.sql` | `creator_agent_preferences` | `ALTER TABLE creator_agent_preferences ADD COLUMN ai_monthly_cap_usd DECIMAL(6,2) NULL;` | Additive, nullable, no backfill. `NULL` = "use the process-wide default" (`AI_CREATOR_MONTHLY_CAP_USD`, currently USD 0.75) — every existing row keeps today's behavior exactly. | Gate fix round 1 (Priya Q7, T-MEERA-CREATOR-PHASE-A) — influora-ai's `spend_tracker.creator_cap_override_from_context` already read an `ai_monthly_cap_usd` key off the CREATOR context payload ("set by support through the admin override") but nothing in influora-api ever wrote it: a creator who hit the monthly cap had no lever for the rest of the month. `CreatorAgentPreferences.aiMonthlyCapUsd` + admin-only `PUT /admin/creator-agent/creators/{creatorId}/monthly-cap` (`CreatorAgentPreferencesService#adminSetMonthlyCapOverride`) close the write side; `MeeraContextService#assembleCreatorContext` surfaces it as `ai_monthly_cap_usd` (2-decimal string, omitted entirely when unset) on the CREATOR context payload. Not reachable from the creator's own `PUT /creator/agent-preferences` — `UpdatePreferencesRequest` has no field for it, by design. |
| 2026-09-03 | `V20260903160000__creator_agent_preferences_consent_version.sql` | `creator_agent_preferences` | `ALTER TABLE creator_agent_preferences ADD COLUMN consent_version VARCHAR(16) NOT NULL DEFAULT 'v1';` | Additive with a DEFAULT — every existing row (all consented, if at all, under the only notice ever shown) is grandfathered in as `'v1'`, not silently un-consented. No backfill logic beyond the column DEFAULT. | Gate fix round 2, item 1 (Priya Q3) — consent had a timestamp but no version, so a DPDP notice text change could never force re-consent. `CreatorAgentPreferences.CURRENT_CONSENT_VERSION` is the single server-side constant; `isConsentAccepted()` now requires the stored version to equal it, not merely a non-null `consent_accepted_at`; `recordConsent()` re-stamps both fields when the stored version is stale. Surfaced as `consent_version` on `PreferencesResponse`, `ConsentResponse`, and `MeeraContextDtos.CreatorContextResponse`. |
| 2026-09-03 | `V20260903170000__creator_agent_preferences_timezone_currency.sql` | `creator_agent_preferences` | `ALTER TABLE creator_agent_preferences ADD COLUMN working_hours_timezone VARCHAR(64) NOT NULL DEFAULT 'Asia/Kolkata', ADD COLUMN floor_currency VARCHAR(3) NOT NULL DEFAULT 'INR';` | Additive with DEFAULTs matching the platform's existing single-market assumption (every current row is effectively IST/INR already) — no behavior change for any existing creator, both fields just become explicit instead of implicit. | Gate fix round 2, item 3 (Priya Q8) — `working_hours_start`/`working_hours_end` carried no timezone (decorative — nothing could tell whether "now" was inside a bare 9-18) and the three rate floors carried no currency (INR assumed only in UI labels / a hardcoded assembler prefix). Validated server-side in `CreatorAgentPreferencesService.validate` (`java.time.ZoneId.of` / `java.util.Currency.getInstance`, 400 `INVALID_TIMEZONE`/`INVALID_CURRENCY` on a bad value) and threaded through `UpdatePreferencesRequest`/`PreferencesResponse`/`CreatorContextResponse`. |
| 2026-09-05 | `V20260905120000__refresh_tokens_remembered.sql` | `refresh_tokens` | `ALTER TABLE refresh_tokens ADD COLUMN remembered BOOLEAN NOT NULL DEFAULT TRUE AFTER expires_at;` | Additive with a DEFAULT — every existing row predates this field and was issued under the single fixed 30-day lifetime that was the only behavior in effect before this change, so `remembered=TRUE` backfills the historically accurate value (not an invented default); their real `expires_at` is untouched. No backfill logic beyond the column DEFAULT. | F-0551 backend half (CEO ruling: `rememberMe` controls refresh-token lifetime server-side, not access-token storage — access token is memory-only for every session regardless). `LoginRequest.rememberMe` (nullable Boolean, `isRemembered()` defaults absent/null to `true` — back-compat with clients that predate the field) threads into `AuthService#issueTokens`/`#refresh` → `RefreshToken.remembered` and `JwtService#getRefreshExpirySeconds(boolean)` (two configured lifetimes: `refreshExpirySeconds`=2,592,000s/30d "remembered" unchanged default, new `refreshExpiryNotRememberedSeconds`=86,400s/24h "not remembered" — see `JwtProperties` javadoc for the 24h justification). `AuthCookieService#writeRefreshCookie` now takes the same boolean to pick the cookie's Max-Age. `brandRegister`/`creatorRegister` have no `rememberMe` input and always pass `remembered=true` (unchanged prior behavior — no remember-me concept at signup). `AuthService#refresh` reads `stored.isRemembered()` off the PRESENTED token (never re-derived from the refresh call itself) so a rotation carries the original choice forward — proven by `AuthServiceTest#testRefreshPreservesNotRememberedAcrossRotation`/`...RememberedAcrossRotation`, falsified against a reverted `remembered=true`-always regression (Mockito strict-stubbing caught the wrong `getRefreshExpirySeconds` overload being called). Admin auth (`AdminAuthService`/`AdminAuthCookieService`/`AdminRefreshToken`) is untouched — separate entity, separate cookie service, always the single fixed lifetime. |

| 2026-09-12 | `V20260912120000__backfill_free_subscriptions.sql` | `subscriptions` | Data-only backfill (no DDL): `INSERT INTO subscriptions (id, workspace_id, plan_id, status, current_period_start, current_period_end) SELECT <generated id>, w.id, (SELECT id FROM plans WHERE code='FREE'), 'ACTIVE', <first-of-current-UTC-month>, <first-of-next-UTC-month> FROM workspaces w WHERE w.type='BRAND' AND NOT EXISTS (subscriptions row for w.id)`. | Idempotent, non-destructive — `NOT EXISTS` guard means a workspace with any existing row (Free, Pro, comp, or a prior run of this same migration) is never touched; `subscriptions.workspace_id`'s UNIQUE constraint (V54) is a second DB-enforced backstop. Scoped to `type='BRAND'` only, so no AGENCY workspace is touched. Period anchor is byte-identical to `UsageCounterService#resolvePeriodStart`'s existing fallback for the current UTC month, so no in-flight usage counter is orphaned (re-verified per F-4's own instruction, not just asserted — see `SubscriptionService#currentBillingCycleStart` / `resolvePeriodStart`, both `LocalDate.now(UTC).withDayOfMonth(1)`). `id` is a 26-char hex string (not a real ULID) — safe because nothing in the codebase ever parses a stored id back into a ULID for ordering/timestamp extraction. | F-4 (`wiki/tech/SUBSCRIPTION-MODEL-REDESIGN-0912.md` §6/§8 Phase 3) — one-time catch-up for every BRAND workspace created before `AuthService#brandRegister`/`FestivalSponsorProvisioningService#provision` started eagerly provisioning the Free row at workspace-creation time (same task, app-code half, no migration of its own). |
| 2026-09-18 | `V20260918150000__brand_ai_credit_grant_period.sql` | `brand_ai_credits` | `ALTER TABLE brand_ai_credits ADD COLUMN credit_grant_period_end TIMESTAMP NULL, ADD COLUMN last_reset_period_end TIMESTAMP NULL;` | Additive, nullable, no backfill — every existing row's guard starts disabled (`NULL`, treated as "not yet granted/reset for this period" by both write paths), which is a safe/conservative starting state, not a behavior change for anyone mid-cycle. | REPAIR ROUND on T-S3-F0879-0917 (F-0881 HIGH, F-0883/F-0885 MEDIUM), per Swapnil's ruling `.proof-os/tasks/T-S3-F0879-0917/RULING-upgrade-grant.md`. `credit_grant_period_end`: `BrandAiCreditRepository#grantAllotmentIncrease`'s atomic UPDATE guard so `AICreditService#applyPlanAllotment` grants an allotment INCREASE (now SET to the full new allotment, not topped up by the delta — the ruling replaces the S3 top-up rule) at most once per the subscription's OWN billing period (`Subscription.currentPeriodStart`/`currentPeriodEnd`), closing F-0883's repeat-grant on every PAST_DUE→ACTIVE flap. `last_reset_period_end`: closes F-0884 (decided, not just flagged) — `resetForNewCycle` now also guards on this column so `SubscriptionService#applyRenewalSafetyNet`'s mid-period safety-net reset and `AICreditResetJob`'s next 1st-of-month run cannot both refill the same still-current billing period, without any change to `SubscriptionService.java`. Both writes moved off the old blind full-row `save()` for the grant path specifically (`BrandAiCreditRepository#syncPlanAllotment`/`#grantAllotmentIncrease`, same atomic-UPDATE shape as `bumpDailyActions`) — F-0885 lost-update fix, closed for `applyPlanAllotment` only; `resetForNewCycle` and `applyEscrowFundedReset` still full-row `save()` and remain exposed to a concurrent `tryDecrement` race (NOT closed, flagged for a follow-up). Also fixes F-0882: `BrandAiCredit.Builder` no longer silently rewrites an explicit `.creditsRemaining(0)` up to the allotment (see `BrandAiCreditTest`). |
| 2026-09-17 | `V20260917120000__brand_ai_credit_plan_loyalty_split.sql` | `brand_ai_credits` | `ALTER TABLE brand_ai_credits ADD COLUMN plan_allotment INT NOT NULL DEFAULT 100 AFTER monthly_allotment, ADD COLUMN loyalty_bonus INT NOT NULL DEFAULT 0 AFTER plan_allotment;` then three backfill UPDATEs: `loyalty_bonus = CASE WHEN first_campaign_at IS NOT NULL AND monthly_allotment >= 50 THEN 50 ELSE 0 END`, `plan_allotment = monthly_allotment - loyalty_bonus`, `monthly_allotment = plan_allotment + loyalty_bonus`. | Additive columns + a same-value backfill by construction: `plan_allotment` is algebraically back-derived as the exact (unfloored) remainder `monthly_allotment - loyalty_bonus`, so the final `monthly_allotment` rewrite (`plan_allotment + loyalty_bonus`) reproduces the pre-migration value **exactly, on every row**, never just "usually" — no row's current allowance can go up. **REPAIR ROUND [vikram, 2026-09-17]:** the original version of this row credited `loyalty_bonus = 50` whenever `first_campaign_at IS NOT NULL`, unconditionally, then floored `plan_allotment` at 0 via `GREATEST(monthly_allotment - loyalty_bonus, 0)` — kabir proved on a real MySQL 8.0.40 instance this RAISES `monthly_allotment` on any funded row stored below 50 (30→50, 0→50, 49→50) or negative (-5→0), because the floor forces `plan_allotment` up to 0 while `loyalty_bonus` stays 50. Not reachable by current app code (seeded plans are 100/400, builder default 100, no admin path writes `plans.ai_monthly_allotment` below 50) but the migration's own never-raise invariant has to hold unconditionally, not just on reachable states. Fixed by gating the loyalty credit on `monthly_allotment >= 50` and dropping the floor on `plan_allotment` (an out-of-range row now reconstructs its original value with `loyalty_bonus=0` instead of being clamped up). **Verified against a real, throwaway local MySQL 8.0.40 instance** (full 130-migration Flyway chain via `flyway-core:10.10.0`, the exact version this project's `pom.xml` resolves) — DDL and all three UPDATEs execute cleanly, and re-probing kabir's exact rows (`WS_G` 30, `WS_H` 0, `WS_J` 49, `WS_K` -5, plus the reachable `WS_B`/`WS_D`/`WS_F` 150/400/450 rows) confirms every row's `monthly_allotment` is unchanged pre- vs post-migration. Still does NOT retroactively repair a historical case where an old Pro brand's `monthly_allotment` was already clobbered down to 150 by the pre-fix `applyEscrowFundedReset` (that brand's `plan_allotment` backfills to 100, not 400); that remains a separate reconciliation concern. | F-3 / F-0836 (`wiki/tech/SUBSCRIPTION-MODEL-REDESIGN-0912.md` §6 F-3, §7 SM-0.2) — `monthly_allotment` was written directly by two owners (`AICreditService#applyPlanAllotment` for the plan sync, `#applyEscrowFundedReset` for the loyalty bonus), so whichever wrote last clobbered the other; per Swapnil's SM-0.2 ruling the loyalty bonus now STACKS on the plan allotment (Free+funded=150, Pro+funded=450) instead of overwriting it. `monthly_allotment` stays a real mapped column (kept in sync by `BrandAiCredit#recomputeMonthlyAllotment`) rather than becoming `@Transient`, because `BrandAiCreditRepository#refundCredits` pins a JPQL clamp against `c.monthlyAllotment` (`BrandAiCreditRepositoryQueryTest`). Same task also fixes F-0836 in `AICreditResetJob` (renamed `applyProAllotmentIfActive` → `syncPlanAllotment`, now unconditional): a workspace that was ever on Pro and then cancelled previously kept the stale Pro allotment forever, because the old Pro-only guard never wrote Free's allotment back down. Repair round also adds a WARN log in `AICreditResetJob#syncPlanAllotment` when the resolved plan is null (kabir), instead of silently skipping the sync. |
| 2026-09-21 | `V20260921110000__creator_credit_accounts.sql` .. `V20260921110600__creator_voice_speaks.sql` (7 files) | `creator_credit_accounts`, `creator_credit_grants`, `creator_credit_ledger`, `creator_credit_welcome_claims`, `creator_credit_packs`, `creator_credit_orders`, `creator_voice_speaks` (all new tables); one `hsn_sac_codes` seed row (`applies_to='CREATOR_CREDITS'`) | 7 pure-new tables, no existing data touched, plus one additive seed row into the pre-existing `hsn_sac_codes` table (its own `UNIQUE(applies_to)` index has no conflict — `CREATOR_CREDITS` is a new value). `creator_credit_packs` seeds exactly one active row, `PACK_60` (60 credits / 24900 paise). No JPA-level FK is declared on any of the seven new entities (this codebase's plain-string-column convention, matches every other domain table) — the raw SQL FKs in the migrations themselves are the only enforcement, same as elsewhere in this file. Two Java enums gained a new constant in the SAME commit (`InvoiceNumberSeriesType.CREATOR_CREDITS`, `HsnSacAppliesTo.CREATOR_CREDITS`) — both additive; `InvoiceNumberService#platformPrefix`'s exhaustive switch was updated to `"INF/CRC"` for the new series so the switch stays exhaustive. | T-CREATOR-CREDITS-V2 (SPEC.md, Priya, 2026-09-21) — creator AI credits (owner rulings R1-R8: 1/2/3-credit turns, one Rs 249/60-credit pack, 40 signup + 15/month free, 30/day cap, `CREATOR_CREDITS_ENABLED` default false). Backend: `CreatorCreditService` (the sole writer — `CreatorCreditAccountInitializer`/`CreatorCreditWelcomeClaimWriter` are its two `REQUIRES_NEW` helper beans, `CreatorCreditWriterBoundaryTest` enforces this by source scan), `CreatorCreditOrderService` (Razorpay order + `confirmPaid`, never flag-gated), `CreatorCreditOrderReconciliationJob`, `CreatorCreditController` (`GET/POST /creator/credits/**`), `CheckoutSignatureVerifier`. Hooked into `MeeraSessionService#doSendTurn` (creator turn charge + compensating release), `#releaseTurnCredit` (now routes by `ConversationTenantType`), `#doPersistAssistantWriteback` (409 `TURN_RELEASED` guard), `CreatorMeeraController#speak` (paid-voice-turn gating), `CreatorBriefService#paste` (3-credit charge), and `RazorpayWebhookController#dispatchFundingEvent` (a `ccr:` receipt branch before the escrow fallthrough — K-07 CRITICAL). Migrations verified only via a scoped `@DataJpaTest`/H2 harness (`CreatorCreditServiceTest`, `create-drop` from entity annotations, Flyway disabled) — **the real Flyway migration files themselves have NOT been executed against a live MySQL boot in this task** (no Docker/Testcontainers available in this environment); a real-MySQL `ddl-auto=validate` boot (SPEC.md A39) is flagged as not done. |

## Notes
- `V20260718160000` backs the admin tier-adjust endpoint. Before it, `creator_profiles` had **no** persisted tier — tier was always derived from `total_followers` at read time (`AdminCreatorService.deriveTier`). The override wins over the derived value when set; `AdminCreatorService.resolveTier` centralizes that precedence.
- FK from a pre-admin-panel table (V6) into `admin_users` (V34) — safe under Flyway's strictly-increasing order, same pattern as V36/V38.
- `V20260718180000` closes the phone gap flagged in the L-9 pass (`Workspace#updateContactEmail`/`WorkspaceMemberDtos.WorkspaceReadResponse` javadocs previously called this out explicitly; those comments are now removed since the gap is closed). `phone` follows the same full-replace, blank-clears-it semantics as `billing_email` (`Workspace#updatePhone`), with a loose format check (allowed chars: `+ ( ) - space`, 7-15 digits) enforced in `WorkspaceService#updateMyWorkspace` — not a DB CHECK, same precedent as `tier_override` above.
- `V20260718190000` (`campaigns.hype_config`): single JSON column, same precedent as `target_audience`/`BrandProfile.niche_tags` — the hype block mixes scalars with a `formatLanes` list and only ever applies to `campaign_type = 'HYPE'` rows, so flat columns would be dead weight on every other row. Persisted/read via `CampaignDtos.HypeConfigDto` + `JsonLists.toJsonObject`/`objectFromJson` — note `liveUntil` is typed `String` (not `Instant`) on that record specifically because `JsonLists`'s `ObjectMapper` has no jackson-datatype-jsr310 module registered (a plain `new ObjectMapper()`, unlike Spring's auto-configured request/response mapper), so a temporal type would fail on the storage round-trip. `CampaignService.create`/`update` only ever write `hype_config` when the campaign's `campaignType == HYPE`; a hype block sent on a non-HYPE request is validated as absent-is-fine and silently ignored (`CampaignValidator.validateHypeConfig`). Also folded in: `campaignType` now defaults to `STANDARD` when unspecified on create (previously stayed `NULL` — see V30's note that this was deferred until "the brand-facing form" sends a type; the Hype form now does, so the human REST path picks up the same STANDARD-default convention `CreateCampaignExecutor#parseCampaignType` already uses on the AI-drafted path). `campaignType` remains immutable after creation (not part of `CampaignPatchRequest`) — `update()` can only revise the `hype` block, and only for a campaign that was already created as HYPE.
- **[G3 fix, no new migration]** `CampaignService.update`'s original `hype`-on-PATCH handling did a full-replace of `hype_config` whenever the PATCH included a `hype` block: `JsonLists.toJsonObject(normalizeHype(req.hype()))` overwrote the stored JSON wholesale, so a caller sub-mitting only the fields it meant to change (e.g. `slotsFilled` after a deliverable approval) silently reset every other sub-field it omitted — `audioTrack`/`currency`/`slotsFilled`/`liveUntil` back to their `normalizeHype` defaults (blank/`"INR"`/`0`/blank), and would have failed `validateHypeConfig`'s 5-required-field check outright for anything less than a full resend of `sourceReelUrl`/`hashtag`/`formatLanes`/`perReelRate`/`slotCap`. Fixed by merging the incoming `hype` DTO field-by-field onto the existing stored config (`CampaignService#mergeHype`) before validating and persisting — chosen over "require a complete object on every hype PATCH" because it mirrors `Campaign.applyPatch`'s own existing convention (null sub-field = leave alone, non-null = overwrite) applied one level down into the JSON blob, rather than the full-replace convention used for the *list* fields (platforms/hashtags/targetAudience), which don't have a coherent "patch one element" semantic the way a named-field record does. Validation (`validator.validateHypeConfig`) now runs against the **merged** result, not the raw patch, so partial patches can never leave a required field missing and an explicit invalid value (on a required *or* optional sub-field) is still rejected 400. Test coverage: `CampaignServiceTest` (full hype PATCH, partial-merge anti-data-loss, malformed-merged-value 400, non-HYPE PATCH silently ignored).
- **[Priya gate review defect 1, no new migration]** `V73__creator_agent_preferences.sql` and `V74__meera_creator_conversations.sql` (both logged above, 2026-09-03) originally shipped without the trailing `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci` clause every other `CREATE TABLE` migration in this repo carries — on a stock MySQL 8 server (default collation `utf8mb4_0900_ai_ci`, not `utf8mb4_unicode_ci`), V73's FK from `creator_agent_preferences.creator_id` to `creator_profiles.id` fails with a collation mismatch and Flyway aborts the whole Spring context boot. Both files fixed in place (2026-09-04) to match the rest of the repo exactly; V72 and the three `V20260903*` ALTER TABLE migrations were checked and have no `CREATE TABLE` statement to be missing the clause on. New regression coverage: `MeeraCreatorPhaseABootValidationTest` (Testcontainers, real MySQL 8 with its own default collation, skipped not failed when Docker is unavailable) — would have caught this before it shipped. Any environment that already attempted the failed V73 (a `flyway_schema_history` row with `version='73' AND success=0`) needs `DELETE FROM flyway_schema_history WHERE version='73' AND success=0` or `flyway repair` before it will boot again — see the OPS NOTE in `.proof-os/tasks/T-MEERA-CREATOR-PHASE-A/TASKS.md`.
- **[F-0836 sibling, no new migration — arjun 2026-09-17]** `SubscriptionService#applyRenewalSafetyNet` synced `brand_ai_credits.plan_allotment` only when the resolved plan was `PRO`, then called `resetForNewCycle`. If `plan_allotment` was ever left stale at a Pro value (e.g. `reconcileAiCreditAllotment` failed and logged-and-swallowed), a renewal resolving to Free re-applied the stale 400/450 to `credits_remaining` — the same bug `V20260917120000`'s companion fix (`77fbb6b`) closed in `AICreditResetJob#syncPlanAllotment`. It now calls `AICreditService#applyPlanAllotment` unconditionally before the reset, and logs a WARN (no write) if the plan resolves to null. Data impact: writes only `plan_allotment` (the plan-sync-owned column per §6 F-3); `loyalty_bonus` is untouched, so SM-0.2 stacking (Pro 450 with a funded campaign) is preserved. No schema or API change. Test coverage: `SubscriptionServiceTest` — `testApplyRenewalSafetyNetFreeTierSyncsAllotmentBeforeReset` (rewritten: it previously asserted the Free renewal did NOT sync, locking in the bug; red with the Pro-only guard restored), ordered sync-then-reset on the Pro test, and a null-plan test.
- **[T-CREDITCLOCK-0918, no new migration — vikram 2026-09-18]** Every `@Modifying` query in `BrandAiCreditRepository` (`tryDecrement`, `refundCredits`, `refundDailyActions`, `bumpDailyActions`, `syncPlanAllotment`, and the four new queries this task adds — `refillForBillingPeriod`, `topUpOnJoinCalendarClock`, `calendarReset`, `applyEscrowFundedReset`) now binds `updatedAt` to a passed-in `Instant now` parameter instead of the JPQL `CURRENT_TIMESTAMP` literal every one of them used before. Discovered building `AICreditClockScenarioTest` (a real H2 `@DataJpaTest` harness, F-0895): H2Dialect's `current_timestamp` function contributor types that literal as `java.sql.Timestamp`, which Hibernate 6's semantic validator refuses to assign to this entity's `Instant`-typed `updatedAt` column — and Spring Data JPA validates every `@Query` method in an interface eagerly at repository-bean creation, so even a method the test never calls fails the whole bean. Production (`MySQLDialect`) is unaffected either way; this is a pure H2-testability accommodation, not a behavior change — callers now pass `Instant.now()` where the literal used to resolve server-side. Also added `clearAutomatically = true, flushAutomatically = true` to every `@Modifying` query in this file (per the credit-clock decision doc's §5 instruction) so a read via `findByWorkspaceId`/`ensureInitialized` later in the SAME transaction sees the bulk update's result instead of a stale first-level-cache entity — this is a genuine correctness fix, not just a testability one: `reconcileAiCreditAllotment` and other T-CREDITCLOCK-0918 service methods chain a sync write and a refill/top-up write inside one `@Transactional` boundary, and any later read in that same transaction (e.g. `getStatus`) would otherwise have returned the pre-bulk-update values. No schema or column change; `AICreditClockScenarioTest`'s 11 scenarios and the whole `AICreditServiceTest`/`AICreditResetJobTest`/`SubscriptionServiceTest`/`BrandAiCreditRepositoryQueryTest` suites are the regression coverage.
- **[F-0965 — `V20260919100000__platform_stats_source_and_follower_provenance.sql`, tester 2026-09-19]** Ruling 2026-09-19 (option a, separate imported total): `creator_profiles.total_followers` counted every `platform_stats` row, so a creator-declared platform (`PortfolioService.declarePlatform`, unverified) inflated the total brands filter and rank on. Adds `platform_stats.source VARCHAR(20) NOT NULL DEFAULT 'CREATOR_REPORTED'` (`META_API` | `IMPORTED` | `CREATOR_REPORTED`) and `creator_profiles.followers_source VARCHAR(20) NOT NULL DEFAULT 'NONE'` (`VERIFIED` | `IMPORTED` | `NONE`). Backfill: `is_verified=TRUE` rows become `META_API`; an unverified INSTAGRAM row for a profile linked from `external_creators.linked_creator_profile_id` becomes `IMPORTED`; totals/engagement recomputed by the same rule as `FollowerTotals.java` (sum of META_API platforms, else the IMPORTED one, else 0 for declared-only creators). Profiles with no `platform_stats` rows (e.g. V7 seed data) are left untouched. **Data impact:** creators whose total came only from declared platforms drop to 0 followers in discovery (by the ruling). Every writer now goes through `FollowerTotals`; `ScoreCalculationJob` scores only META_API rows. Coverage: `FollowerProvenanceMigrationBackfillTest` (runs this exact file on H2 MySQL-mode), `FollowerTotalsTest`, writer tests in `PlatformStatsAggregationJobTest`, `ExternalCreatorLinkServiceAdoptPlatformStatTest`, `PortfolioServiceTest`, `ScoreCalculationJobTest`.
- **[T-CREATOR-CREDITS-V2 round 2, `V20260921110700__creator_voice_speaks_delivery_state.sql`, vikram 2026-09-22]** Review finding #3 (K-15): the "was this paid voice turn's audio already delivered" decision used to live in a process-local `ConcurrentHashMap` on `CreatorMeeraController` (`deliveredVoiceTurns`) — lost on restart, not shared across instances, and read/decided OUTSIDE the account row lock, so two parallel `speak` calls for the same paid turn (one slow-and-succeeding, one fast-and-failing) could both reach the refund decision before either had written anything visible to the other, letting the fast failure refund a surcharge the slow call still went on to spend on a real, delivered reply. Adds `creator_voice_speaks.delivered BOOLEAN NOT NULL DEFAULT FALSE` and `creator_voice_speaks.refunded BOOLEAN NOT NULL DEFAULT FALSE` (both additive, no backfill needed — every existing row correctly starts "never delivered, never refunded"). Decided under the account lock in two new `CreatorCreditService` methods: `markVoiceDelivered` (success path) and `releaseVoiceIfUndelivered` (failure path — refunds only when a claim row exists, is not already delivered/refunded, and `speakCount == 1`, i.e. no other claim for this turn could still be in flight). `CreatorMeeraController` no longer holds any in-memory delivery state. No data impact (purely additive columns, default `FALSE`). Coverage: `CreatorVoiceRefundRaceTest` (real H2, the interleaved-claims race — proven RED by removing the `speakCount == 1` guard), `CreatorVoiceSpeakChargeTest` (controller delegates to the new methods, never calls `release` directly).
- **[T-CREATOR-CREDITS-V2 round 2, no new migration — vikram 2026-09-22]** Review finding #1 (K-15): `CreatorCreditService#release` used to consult `IdempotencyService#isCompleted(..., MeeraSessionService.PERSIST_WRITEBACK_SCOPE)` to decide whether a TURN-scope release should refuse (write-back already persisted). That check races: the write-back's ASSISTANT row commits (and releases the account lock) inside `MeeraSessionService`'s own `TransactionTemplate`, and only AFTER that does `IdempotencyService` mark the reservation COMPLETED, in a separate `REQUIRES_NEW` transaction — a `release()` landing in that window sees `isCompleted()==false` and wrongly refunds a turn whose reply is already durably persisted (refund-and-keep-reply). Fixed with a new `CreditLedgerReason.WRITEBACK_MARKER` (zero-delta ledger row, no schema change — `reason` is a plain `VARCHAR(20)` string column, and `WRITEBACK_MARKER` fits) written by a new `CreatorCreditService#markWritebackPersisted`, called from `MeeraSessionService#doPersistAssistantWriteback` in the SAME physical transaction as the ASSISTANT insert, under the SAME account lock. `release()` now checks for this marker in the ledger rows it already reads under its own account lock, instead of asking `IdempotencyService` — closing the race entirely, since the write-back's transaction holds this exact account row lock for its whole duration, so `release()` cannot even reach the check until the write-back has either fully committed (marker + reply both visible) or never touched the lock at all. Coverage: `CreatorTurnReleaseRoutingTest#releaseAfterWritebackCommitButBeforeIdempotencyMarkedCompletedStillNoOps` (reproduces the exact race window — proven RED by reverting the check), `CreatorAssertTurnNotReleasedTest#releaseNoOpsWhenWritebackMarkerPresent`.
- **[T-CREATOR-CREDITS-V2 round 2, no new migration — vikram 2026-09-22]** Review finding #2 (A18/K-15): the only test exercising `CreatorCreditService#assertTurnNotReleased` mocked `CreatorCreditService` itself, so neutering the method's real body left every test green. Added `CreatorAssertTurnNotReleasedTest` (real `@DataJpaTest`, same harness as `CreatorTurnReleaseRoutingTest`): charges a turn, releases it for real, then asserts the real `assertTurnNotReleased` throws 409 `TURN_RELEASED` — proven RED by neutering the method to a no-op — plus the reverse (write-back marker present, release is a no-op, no REFUND row).
- **[T-CREATOR-CREDITS-V2 round 2, no new migration — vikram 2026-09-22]** Review finding #4 (K-24/A26): no local test ran the real `CreatorCreditService#creditPurchase` — every unit test mocked either it or `CreatorCreditOrderService`. Added `CreatorCreditPurchaseRealPathTest` (real `@DataJpaTest`, `influora.creator-credits.enabled=false`): calls the real `confirmPaid`→`creditPurchase` on a PENDING order and asserts one PAID grant (60 remaining, `expires_at == paidAt + 90d`), one `GRANT_PURCHASE` ledger row referencing `order:<id>`, the order `CREDITED`, and a second `confirmPaid` call is a true no-op — proven RED by adding a flag gate to `creditPurchase` (the exact K-24 regression this test exists to catch).
- **[T-CREATOR-CREDITS-V2 round 2, no new migration — vikram 2026-09-22]** Review finding #5 (A32/K-21): the IDOR test mocked `orderService.findOwnedOrder` to return empty directly, so the real repository-level ownership filter (`CreatorCreditOrderRepository#findByIdAndCreatorUserId`) was never exercised. Added `CreatorCreditOrderOwnershipRealTest` (real `@DataJpaTest`): persists creator B's order, asserts `findOwnedOrder(A, bOrderId)` is empty and `findOwnedOrder(B, bOrderId)` is present against real H2 rows, and drives `CreatorCreditController#verify` with a REAL `CreatorCreditOrderService` to prove creator A gets 404 and B's order row is byte-for-byte unchanged — proven RED by changing `findOwnedOrder` to `orderRepository.findById(orderId)` (dropping the ownership filter).
