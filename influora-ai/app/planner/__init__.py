"""Week-planning data and arithmetic for creator Meera.

`events.py` loads the festival and special-days calendar
(`app/planner/events.jsonl`) and answers "what falls in this creator's next
seven days". The posting pattern and today's topics come from Spring, through
the `plan_my_week` tool — this package never reads the database.
"""
