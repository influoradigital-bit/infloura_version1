package com.influora.domain.enums;

/** Mirrors {@code TicketStatus} in {@code src/admin/types/admin.types.ts}. */
public enum TicketStatus {
    OPEN,
    IN_PROGRESS,
    WAITING_USER,
    RESOLVED,
    CLOSED
}
