{"verdict":"proved","oracle":"mvn","isolation":"oracle",
 "done_when":"influora-api compiles (mvn exit 0) and AdminFinanceServiceTest executes green (Tests run: 7, Failures: 0) — GET /admin/escrow/flagged returns FROZEN holds with campaign name + dispute reason joined live, honest fallbacks, and the SUPER_ADMIN+ADMIN/MFA gate blocks an unauthorized caller before any read",
 "evidence":{"oracle":"mvn","command":"mvn -o test-compile surefire:test -Dtest=AdminFinanceServiceTest","result":"Tests run: 7, Failures: 0, Errors: 0, Skipped: 0","where":"influora-api/src/test/java/com/influora/service/admin/AdminFinanceServiceTest.java:141"},
 "not_checked":["the derived/native repo SQL against real MySQL — repos are mocked; needs a Testcontainers/@DataJpaTest harness this module lacks","HTTP filter-chain authz (that Spring Security routes an unauth request into the gate) — only the service-layer gate call is proven","the most-recent-dispute tiebreak when one collaboration has multiple disputes — tested only with one dispute per collaboration; the merge fn keeping the newest is unverified","FE↔BE parity is by inspection (DTO fields match escrowApi.getFlagged's inline type), not a running client"],
 "skipped":[{"step":"proof-os ledger/journal","reason":"fcntl-unavailable on Windows; recorded in verdict + BACKLOG.md + commit 4717eb4"}]}
