# Phase D fact sheet (condensed) — captions, suggestions, scores, series, shortlist, tax, digest precedents, frontend @ HEAD 143ca1e + working tree

Verified by direct file reads on 2026-09-04. Phase B, C and E fact sheets are not repeated.

## 1. Captions

- `domain/entity/CreatorCaptionCache.java` → table **`creator_captions`** (L30-31; class name differs on purpose). Columns: `id` (26), `creator_profile_id` NN (26), `ig_media_id` NN (64), `caption_text` TEXT (plaintext, public post text, risk-accepted L22-28), `tagged_themes` json (`taggedThemesJson`), `tag_status` NN (16, `CaptionTagStatus {PENDING, TAGGED, FAILED}`), `posted_at`, `tagged_at`, `created_at`. Mutators `applyTagResult(String taggedThemesJson)` L104, `markFailed()` L113. Builder fields: id, creatorProfileId, igMediaId, captionText, postedAt. Migration `V20260721130000__creator_captions.sql` (unique `(creator_profile_id, ig_media_id)`, index on `tag_status`, FK to creator_profiles, collation clause present). Migration note: any consumer of caption_text MUST wrap_untrusted before any model call.
- `repository/CreatorCaptionCacheRepository.java`: ONLY `findByCreatorProfileIdAndIgMediaId(String, String)` L12-13 and `findByTagStatusOrderByCreatedAtAsc(CaptionTagStatus, Pageable)` L16-17. **No per-creator or posted_at-window finder.**
- `job/CreatorCaptionSyncJob.java`: `@Scheduled(cron = "${influora.creator-copilot.caption-sync-cron:0 0 2 * * *}", zone = "UTC")` + `@SchedulerLock(name = "CreatorCaptionSyncJob", lockAtMostFor = "PT30M", lockAtLeastFor = "PT1M")` L85-86; gated by `CreatorCopilotProperties.isEnabled()` (**default false**); iterates `tokenRepository.findByWorkspaceIdIsNullAndRevokedFalseAndExpiresAtAfter(now)`; calls `instagramClient.getMedia(igBusinessAccountId, token, props.getCaptionSyncMediaLimit(), token.getAuthPath())` (limit default 25); `persistIfNew` skips blank captions and duplicates; timestamp `yyyy-MM-dd'T'HH:mm:ssZ`.
- `media_metrics.caption` also exists (`MediaMetric.java` L43-48) but is ADR-restricted (V26): "internal BrandSafety pipeline ONLY, never surface via any brand-facing DTO, keep out of logs". Two independent caption copies, never joined.
- `job/CreatorThemeTaggingJob.java`: `PAGE_SIZE = 200`; cron `${influora.creator-copilot.theme-tag-batch-cron:0 0 3 * * *}` UTC, lock `CreatorThemeTaggingJob`; reads PENDING captions; `themeMatchService.themesForText(caption.getCaptionText())` L87; unions into `CreatorProfile.themeTagsJson` (L71-72, setter L282). Zero-model keyword match (Kabir threat-2 PASS).
- `service/trendspark/ThemeMatchService.java`: `TAXONOMY_PATH = "trendspark/theme-taxonomy.json"` L30 loaded `@PostConstruct`, fail-closed; `public Set<String> themesForText(String freeText)` L98 (lowercase substring containment against `keyword_to_theme_mappings`, filtered by `knownThemes`); also `score(Trend, String)` L58, `knownThemes()` L79, `parseThemeJson(String)` L86. Taxonomy record `TaxonomyFile(String version, List<String> themes, Map<String, List<String>> keywordToThemeMappings)` L143-147, `@JsonIgnoreProperties(ignoreUnknown = true)`. Resource `influora-api/src/main/resources/trendspark/theme-taxonomy.json` version 1.0, 40 themes.
- **No disclosure/label detection code anywhere** (only seed data `V20260714150000__campaign_templates.sql:72` `JSON_ARRAY('#ad', '#shopnow')`, a test fixture, a FE mock, and copy). **No Devanagari-aware code** anywhere; language is a BCP-47 tag only.

## 2. Tier-1 suggestion

- `CreatorNudgeLog` (`creator_nudge_log`): `id, creator_profile_id, trend_id NN (FK trends), match_score NN, theme(32) NN, headline(255) NN, content_idea TEXT NN, message_source(16) enum, prompt_version(32) NN, shown_at NN, dismissed_at, acted_at, created_at`; generated `shown_day DATE` + unique `(creator_profile_id, shown_day)` (V20260721140000). `markDismissed()` L125, `markActed()` L132.
- `CreatorNudgeLogRepository`: `findByCreatorProfileIdAndShownAtAfter(String, Instant)` L13-14 (the per-day cap), `findByIdAndCreatorProfileId` L18. No aggregates.
- `service/creatorcopilot/CreatorNudgeService.java`: `record SuggestionResult(String status, SuggestionDto suggestion)` with `pendingTagging()`, `noSuggestionToday()`, `ready(dto)` L67-79; `@Transactional public SuggestionResult getSuggestion(String creatorProfileId)` L81-82 (same-day short-circuit, pending tagging when themeTagsJson blank, best active trend via `themeMatchService.score`, threshold `props.getScoreThreshold()` default 2, AI phrasing with template fallback, writes the log row, handles the unique-key race); `markDismissed(String creatorProfileId, String suggestionId)` L175, `markActed(...)` L182; `expiresAt` = start of next UTC day.
- `web/CreatorCopilotController.java` `@RequestMapping("/creator/copilot")`: `GET /suggestion/today` L42-49 → `SuggestionTodayResponse(SuggestionDto suggestion, String status)`; `POST /suggestion/{id}/dismiss` L54; `POST /suggestion/{id}/acted` L63. `SuggestionDto(String id, String theme, String headline, String contentIdea, String expiresAt)` camelCase (frozen v1).
- `config/CreatorCopilotProperties.java` prefix `influora.creator-copilot`: `enabled` (false), `scoreThreshold` (2), `maxSuggestionsPerCreatorPerDay` (1), `promptVersion` ("creator-copilot-v1"), `captionSyncMediaLimit` (25), `captionSyncMaxCreatorsPerRun` (0 = unlimited); crons `theme-tag-batch-cron`, `caption-sync-cron` bound as placeholders.
- `CreatorSuggestionServiceTokenService.mint(String creatorProfileId)` L59: claims `creator_profile_id`, `scope = "creator"`, NO `workspace_id`, ES256 via `SpringJwksKeyService`. `CreatorSuggestionAiClient.requestSuggestion(String creatorProfileId, String theme, String trendText) -> SuggestionCopy(headline, contentIdea)` L102, never throws, null on failure; `PATH = "/internal/creator-suggestion"`. Wire `SuggestionRequest(creator_profile_id, theme_matched, trend_text)` — exactly 3 fields, no caption text (Priya R1 ruling quoted at `CreatorSuggestionAiDtos.java` L10-13).

## 3. Scores

- `CreatorScore` (`creator_scores`, immutable, builder): `time DATETIME(6) NN, creator_profile_id, fake_follower_score(5,2), fake_follower_reasons json, quality_score, engagement_consistency, posting_frequency, audience_match_score, brand_safety_score, garm_flags json, content_sentiment, estimated_rate_min(12,2), estimated_rate_max(12,2), rate_currency(3) NN, rate_confidence(5,2), algorithm_version(20) NN, computed_at NN, created_at`. Null = not scored. `CreatorScoreRepository`: `findFirstByCreatorProfileIdOrderByTimeDesc` L31, `findLatestByCreatorProfileIdIn(Collection<String>)` L45-55 (native). **No time-range finder.**
- `job/ScoreCalculationJob.java`: `ALGORITHM_VERSION = "v1.0.0"`, `RECENT_MEDIA_LIMIT = 30`; `@Scheduled(cron = "0 0 4 * * *", zone = "UTC")` + lock `ScoreCalculationJob` PT30M/PT1M L135-137; enumerates `creatorProfileRepository.findAll(CreatorProfileSpecifications.discoverable())` (unpaginated); `scoreOne` L266-324 uses `creatorMetricsRepository.findFirstByCreatorProfileIdAndPlatformOrderByTimeDesc(id, "INSTAGRAM")`, `mediaMetricsRepository.findByCreatorProfileIdOrderByTimeDesc(id, PageRequest.of(0, 30))`, and **`fakeFollowerDetectionService.analyze(latestMetric, recentMedia, List.of())`** L286-289 — historical series passed empty, so the spike signal never fires.
- `service/scoring/FakeFollowerDetectionService.java`: `record FakeFollowerResult(BigDecimal score, List<String> reasons, Map<String,Object> debug)` L38-42; `public FakeFollowerResult analyze(Optional<CreatorMetric> latestMetric, List<MediaMetric> recentMedia, List<CreatorMetric> historicalMetrics)` L45-48; signal 2 requires `historicalMetrics.size() >= 7`; `maxDailyGrowth > 20` → +30 "Sudden follower spike detected (%.1f%% in one day)", `> 10` → +15; `calculateMaxDailyGrowthRate` L133-148 adjacent-pair growth; no reach signal.
- `QualityScoreService.calculate(Optional<CreatorMetric>, List<MediaMetric>) -> QualityScoreResult(overall, engagementScore, consistency, frequency, audienceMatch)`, `absent()` when either input empty.
- `BrandSafetyScoreService.scoreCreator(String creatorProfileId, List<MediaMetric> recentMedia) -> Optional<BrandSafetyResult(brandSafetyScore, garmFlagsJson, contentSentiment)>` L119; sends `media_metrics.caption` in chunks of 25 to `POST /internal/brand-safety` (scope `service`, workspace-bound token from `metaOAuthTokenRepository.findFirstByCreatorProfileIdAndRevokedFalseOrderByCreatedAtAsc(...).getWorkspaceId()`); fail-safe to empty.
- `job/PlatformStatsAggregationJob.java`: `0 45 3 * * *` (no zone), lock `PlatformStatsAggregationJob`, `PAGE_SIZE = 500`.

## 4. Series finders

- `CreatorMetricsRepository`: `findFirstByCreatorProfileIdAndPlatformOrderByTimeDesc(String, String)` L36-37; `findByCreatorProfileIdOrderByTimeDesc(String, Pageable)` L40; **`findByCreatorProfileIdAndPlatformAndTimeBetweenOrderByTimeAsc(String, String, Instant, Instant)`** L43-44; `findByCreatorProfileIdAndTimeBetweenOrderByTimeAsc(String, Instant, Instant)` L47-48; `findLatestPerCreatorAndPlatform(List<String>)` L55-60. Precondition note L22-31: caller-supplied ids must go through `MetricsAuthorizationService#resolveAuthorizedCreatorProfileId(workspaceId, creatorProfileId)`; scheduled jobs exempt.
- `MediaMetricsRepository`: `findFirstByMediaIdOrderByTimeDesc(String)` L28; `findByCreatorProfileIdOrderByTimeDesc(String, Pageable)` L31; `findByCreatorProfileIdAndPlatformOrderByTimeDesc(String, String, Pageable)` L34-35; **`findByCreatorProfileIdAndTimeBetweenOrderByTimeAsc(String, Instant, Instant)`** L38-39; `findByMediaIdOrderByTimeAsc(String)` L42. **No latest-row-per-media query** (N polls = N rows).
- `AudienceDemographicsRepository`: only `findFirstByCreatorProfileIdOrderByTimeDesc` L27.

## 5. Shortlist signals

- `SavedCreator` (`saved_creators`): `id, workspace_id NN, creator_profile_id NN, saved boolean NN, created_at, updated_at`; `of(id, workspaceId, creatorProfileId, saved)`, `setSaved(boolean)` (bumps updatedAt). Repository: ONLY `findByWorkspaceIdAndCreatorProfileId` and `findByWorkspaceIdAndCreatorProfileIdInAndSavedTrue(String, Collection<String>)`. **No count by creator.**
- `FeaturedCreator` (`featured_creators`) with `findActiveFeatured(category, now, Pageable)`.
- `CollaborationStatus.SHORTLISTED` is **never set** anywhere in main source (only read/filtered).
- **No brand-viewed-profile tracking** (CreatorMetric.profile_views is an Instagram metric, deprecated by Meta).
- `CollaborationRepository`: `countByCreatorIdAndStatus(String creatorId, CollaborationStatus)` L151, `countByCreatorId` L153, `findByCreatorIdAndStatusNotIn` L113-114, `findByCreatorIdAndSource` L124, `findByCreatorIdAndCampaignIdIn` L127, `findByCreatorIdAndIdIn` L138, `countByCampaignIdInGroupByStatus` L175-179. **creator_id is a users.id** (L143-148 warns PortfolioService passes a profile id and gets 0).

## 6. Tax

- `CreatorProfile`: `gstin`(15) L155-156, `pan`(10) L158-159, `tax_registration_status` (`CreatorTaxRegistrationStatus {UNREGISTERED, GST_REGISTERED, EXEMPT}`, default UNREGISTERED L216) L161-163, `creator_invoice_code`(12) L171-172; `applyTaxIdentity(String gstin, String pan, CreatorTaxRegistrationStatus status, String creatorInvoiceCode)` L420-423.
- `CreatorTaxIdentityService`: only `submit(AuthPrincipal, TaxIdentitySubmitRequest)` L55-56 (`POST /me/tax-identity`). No threshold logic.
- `PayoutRepository`: only SUM is `sumAmountByCreatorUserIdAndConfirmedAtIsNull(String)` L115-119; `findByCreatedAtBetween(Instant, Instant)` L82 (not creator-scoped); `findByMilestoneId` L45; `findByStatusIn` L64. **No creator-scoped FY sum.**
- `InvoiceNumberService.currentFiscalYear()` L86-92 (Apr-1 cutoff, IST, "2026-27"). **No GST threshold constant** anywhere.

## 7. Digest precedents

- `MeeraContextService.buildOutcomeDigest(List<Campaign>, List<Collaboration>, BrandProfile)` **private** L457-458; `BrandContextAssembler.assembleOutcomeDigest(...)` public L280-287 → `OutcomeDigest(campaign_outcomes, niche_rate_band)`; brand-audience prompt block only; `RATE_BAND_K_ANON_FLOOR = 5`.
- `MonthlyStatementEvent(String userId, String workspaceId, String entityId, String statementPeriod, String statementUrl)` → `"cron.monthly_statement"`; listener `NotificationListener` L605-619 sends template `user.monthly_statement` with `statement_period`, `statement_url`; in `EMAIL_ONLY_EVENTS`; **no publisher**. `NotificationEventContractTest` requires every event to have a real publisher AND a listener (per `CreatorConnectNudgeJob` L118-121, which publishes via `events.publishEvent(...)`).
- Only weekly cron: `AudienceDemographicsJob` `0 30 3 * * SUN`. `CreatorConnectNudgeJob` runs `0 30 9 * * *` Asia/Kolkata with `@Value` keys `influora.creator-connect-nudge.{enabled:false, registered-within-days:30, batch-limit:200}` and `influora.web-base-url`.
- `TaskSchedulerConfig.POOL_SIZE = 10` L36 against 22 `@Scheduled` methods.
- `MeeraVoiceAiClient.speak(String workspaceId, String text, String lang) -> SpeakResult(boolean ok, byte[] audioBytes, String contentType)` L192 (creator user id in the workspaceId slot); `CreatorMeeraController.speak` L245-267 streams bytes and discards; `R2StorageService.putBytes(objectKey, bytes, contentType)` and `presignGet` exist but are unused on the voice path. `TTS_MAX_CHARS = 500` (`voice.py` L65-73).
- `CreatorAgentBaselineService` L42-43: `sample_label_compliance` is hardcoded `new SampleLabelCompliance(100, 73, 0.73)` with a doc naming Phase D as the replacement owner; `medianCreatorReplyHours()` is a platform median over `collaborationRepository.findAll()` and `DealMessageRepository.findFirstMessageTimestampsBySender()` (global, unwindowed).

## 8. AI service

- Tier-1 ruling (binding, quoted at `routes/creator_suggestion.py` L23-28, `prompt/creator_suggestion.py` L17-22, `CreatorSuggestionAiDtos.java` L10-13): NO creator caption text reaches any model on the creator scope. Only caption-accepting route: `POST /internal/brand-safety` (scope `service`, `workspace_id`, items `{content_id, caption, media_type, posted_at}`, `BRAND_SAFETY_MAX_ITEMS_PER_CALL` 25, `BRAND_SAFETY_MAX_CAPTION_CHARS` 2000, model `BRAND_SAFETY_MODEL` default Sonnet, forced tool `get_analyze_creator_content_schema()`, raises 503 on spend gate, 502 on failure).
- `routes/creator_suggestion.py`: `POST /internal/creator-suggestion`, body `creator_profile_id, theme_matched, trend_text`; `verify_creator_token(..., endpoint="creator_suggestion", body_creator_profile_id=...)`; model `CREATOR_COPILOT_MODEL` (default `TRENDSPARK_MODEL` = `claude-haiku-4-5-20251001`), `creator_copilot_max_tokens` 300, headline 120 chars, content idea 300 chars, trend text 200 chars; validators `_statement_count > 2`, `_has_forbidden_petname`, `has_invented_price`, `_MARKETPLACE_RE`; response `{"success": true, "data": {"headline", "content_idea", "message_source": "AI"|"FALLBACK"}}`; spend gate keyed by `creator_profile_id`, falls back (still 200).
- `prompt/trend_tag.py`: `THEMES` (40, same as Java JSON) L50-59, `THEME_SET`, `validate_themes(raw, *, max_themes)` L151-170.
- `costs/pricing.py` `PRICING_TABLE`: `claude-sonnet-4-5-20250929`, `claude-haiku-4-5-20251001`, `claude-opus-4-1-20250805`, `claude-3-5-haiku-20241022`, `gemini-2.5-flash`; import-time assertion over `(CLAUDE_MODEL, GEMINI_MODEL, TRENDSPARK_MODEL)`; `estimate_sarvam_tts_cost_usd(char_count)` L166.
- No Devanagari-aware validator; `voice_default_stt_language` hi-IN, `voice_default_tts_language` en-IN; `creator_language` default hi-IN in assembler L568.

## 9. Frontend

- `src/pages/creator-copilot.tsx`: render order header → "Talk to Meera" Card (or featureDisabled card) → `<ConsentScreen>` → `{showPreview && <CopilotPreviewCard className="mb-3" />}` → `<DailySuggestionSection />`; container `max-w-3xl`; `useDailySuggestion()` second instance deduped by react-query.
- `src/components/creator/copilot/DailySuggestionSection.tsx` (`{ className?: string }`, switch on status idle/loading/error/dismissed/ready, toast on error), `DailySuggestionCard.tsx` (props `suggestion, status, onDismiss, onMarkActed, onRetry?, className?`; framer-motion), `BusinessAccountRequired`, `CopilotPreviewCard`, `IGConnectPrompt`, `SuggestionEmptyState` (reasons `verifying_connection | pending_tagging | no_suggestion_today`).
- `src/hooks/useDailySuggestion.ts`: `SuggestionStatus = 'idle' | 'loading' | 'ready' | 'dismissed' | 'error'`; query keys `['creator','copilot','suggestion', day]`, `['creator','meta','connection-status']`; `staleTime: Infinity` on the suggestion; sessionStorage interaction marker; derivation table L181-190.
- `src/pages/creator-analytics.tsx`: `useCreatorMetrics`, `useCreatorScores`, `useCreatorDemographics`, `useCreatorOwnMedia`; four `CreatorMetricsCard` tiles; `MetricsTrendChart` on **recharts** via shadcn chart primitives; series keys `followers | impressions | reach | engagementRate`; `FakeFollowerIndicator`, `QualityScoreDisplay`, `BrandSafetyBadge`, `AudienceDemographicsPanel`, `ContentPerformancePanel`.
- `src/pages/creator-dashboard.tsx`: three-tile `FadeUp` grid (Available balance, Active deals, Pending actions) L475-553, then contracts card L563; a full-width card fits between L553 and L563.
- `src/lib/api.ts`: `creatorCopilot` L6219-6241 (`getTodaySuggestion`, `dismissSuggestion`, `markSuggestionActed`), types `DailySuggestion` L6190, `CreatorSuggestionTodayResponse` L6202; `creatorAnalytics` L4710-4740 (`getMyMetrics(startDate?, endDate?)`, `getMyScores`, `getMyDemographics`, `getMyMedia`); `contentPerformance` L4777 (`ContentPerformanceItem` has no caption); `NotificationItem.type` includes `'meera_nudge'` but nothing renders it specially and `surfaceInChat` is never set from the wire (L3934-3945).
- Brand weekly digest switch: `src/pages/brand-settings.tsx` L900-907 permanently disabled with `title="Weekly digest isn't available yet"`, locked by `src/pages/__tests__/brand-settings.disclaimer.test.tsx` L190-201. No creator digest UI.

## 10. Gaps Phase D builds (28, condensed)

No label detection or multilingual vocabulary; no per-creator caption finder; no label verdict storage; hardcoded sample_label_compliance; no latest-per-media query; follower-spike signal dead and no reach signal; no shortlist counter and SHORTLISTED never set; no profile-view tracking; no windowed creator counts; no FY payout sum and no GST threshold; no per-creator reply-speed query; no DATA_GAP concept; no health entity or rule log; no rate-change measurement; no weekly job; MonthlyStatementEvent unpublished; outcome digest is brand-only prompt context; no creator report route; digest asset storage unwired; TTS 500-char cap; captions forbidden on creator scope; no Devanagari validator; no digest prompt or pricing; no creator digest UI; `meera_nudge` unrendered; no digest api namespace; no progressive-disclosure card; scheduler pool comment stale.
