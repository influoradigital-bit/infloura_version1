# Adversarial verification of the `unenforced-limit` gate

Produced by an independent fresh-context verifier that received only the gate path
and the done_when. It did NOT see the gate author's reasoning.

- refuted: True
- exit observed on the real repo: 1
- a real instance of the defect slipped past (green on bad input): True
- vacuous pass (exit 0 having scanned nothing): False
- false positives: 0   verified true positives: 5

## What was observed

RUN: `python .proof-os/gates/unenforced-limit.py` exits 1 on the real repo — 718 java files, 79 entities, 10 candidate fields, 5 findings (Campaign.budgetMax PER_ITEM_ONLY, Campaign.maxCollaborators ECHO_ONLY, Deliverable.deadline / Deliverable.revisionCount / Plan.trackedCreatorLimit UNENFORCED). I grep-verified all 5 against main/: maxCollaborators is read only by CampaignMapper:47 and CreatorCampaignMapper:29/51; trackedCreatorLimit only by BillingController:137/220 (DTO fill); revisionCount only by `> 0` ternaries; deadline only by reporting filters and PortfolioService on-time math; budgetMax only by DealService:1507's single-offer compareTo. 5/5 true positives, 0 false positives — the 5 OKs are real too (WorkspaceMemberService.enforceSeatLimit counts members+pending invites then throws; RedemptionWriter.validateCode compares usageCount >= usageLimit then throws). NOT VACUOUS: empty --root exits 2, `--only` matching nothing exits 2, an unparseable/crashing tree exits 2 (I hit this incidentally via a MAX_PATH FileNotFoundError — it exited 2, not 0), bad flag exits 64. No litter: git status identical before/after, no __pycache__ entry created. Positive control passes — I injected `private Integer apiCallMax;` with an unread getter into Plan.java and it was auto-discovered and reported FINDING [UNREAD], so the detector does generalize past its four seed examples. BUT IT IS GREEN ON BAD INPUT, three separate ways, each confirmed exit 0 on a sandbox copy at /tmp/ulx: (1) adding one unused local `BigDecimal totalOffered = amount;` to DealService.validateProposalAmount flips F-0399 Campaign.budgetMax from PER_ITEM_ONLY to "OK ... aggregates across siblings inside validateProposalAmount()" — AGG_RE (`\btotal`) is matched anywhere in the method body with no link to the comparison, so a cosmetic rename greens the gate's flagship finding while N creators can still each be offered the full budget; (2) a new service calling an EMPTY `private void enforceTrackedCreatorCap(Integer cap) {}` plus an unused `long totalTracked = 0;` turns Plan.trackedCreatorLimit OK; (3) most damaging because it is realistic and undisclosed: a warn-only guard `if (deliverable.getRevisionCount() > 3) { notifyOpsOfHeavyRework(d); } deliverable.applyRevision(...)` turns F-0417 OK — ACTS_RE accepts `notify*`/`mark*` as rejection, so code that logs and then proceeds reads as a ceiling. Discovery is also defeated by formatting, not just vocabulary: `@Column(name = "webhook_max") private Integer webhookMax;` on ONE line is invisible because FIELD_RE anchors `^[ \t]*private`, and that evasion is not in the NOT_CHECKED list. Vocabulary misses (`dailyPostCeiling`, `storageAllowanceMb`) are disclosed. Finally, provenance: BOTH `.proof-os/gates/unenforced-limit.py` and a DIFFERENT 647-line sibling `.proof-os/gates/unenforced_limit.py` are untracked (`??`, not gitignored), and the recorded rc files are named `unenforced_limit.*.rc` — so the artifact under review is not committed and may not be the file the runner actually executed. The gate is a genuine detector with zero false positives, but it is not a proof: its OK verdict can be manufactured by an unused variable name, an empty method, or a log line, so refuted.

## Required fix (the minimum change that would make this gate real)

1) AGG_RE must be tied to the comparison, not the method: require the ordering guard's non-limit side to be a local bound from a count/sum expression (e.g. `long used = repo.countBy...(); if (used + x >= cap)`), not merely any `total|count|used` token somewhere in the body. 2) ACTS_RE must require control flow to actually stop: throw / return false / early return / status transition only — `notify*` and `mark*` must NOT count as enforcement, or must yield a distinct WARN_ONLY verdict that still fails. 3) E3 check-shaped handoff must follow one more hop and confirm the callee body orders the limit; taking the callee at its name means any empty `enforceX(cap)` greens the field, so E3 alone should not produce OK. 4) FIELD_RE must drop the `^[ \t]*private` anchor (strip annotations/modifiers first) so a same-line `@Column(...) private Integer webhookMax;` is discovered, and should also handle `protected`/package-private and multi-declarator fields. 5) git add the gate and delete or reconcile the divergent `unenforced_limit.py` sibling so the rc record names the file that actually ran. 6) Add a falsify harness that asserts these three BAD fixtures still exit 1, since all three currently exit 0.
