REASON:
Multiple concrete, checkable errors. The fatal one: the plan's "verified, file-by-file" enumeration omits influora-api/src/main/java/com/influora/InfluoraApiApplication.java, which carries an explicit @EnableConfigurationProperties({...}) list; there is NO @ConfigurationPropertiesScan anywhere in influora-api. Creating TrendIngestProperties.java exactly as step 1 describes (plain class, no stereotype, unregistered) reproduces the exact boot failure that file's own comment block documents, and it immediately reds an existing test, ConfigurationPropertiesRegistrationTest. Second: step 6's insertion anchor does not exist — deploy/utho/docker-compose.utho-shared.yml contains zero CREATOR_COPILOT lines, contradicting the plan's "confirmed structurally identical" and "grep found the CREATOR_COPILOT_AI_BASE_URL line". Third: step 5 inverts the meaning of the Critical-graded Q7.4 comment it cites. Plus a miscounted deploy-compose set, a wrong line count and wrong characterisation of application-prod.yml, and a pullCron field that would bind to nothing.

CORRECTIONS:
- FATAL / MISSING STEP — influora-api/src/main/java/com/influora/InfluoraApiApplication.java is never named in the plan, yet it is mandatory. It declares @EnableConfigurationProperties({R2Properties.class, ... ConversionWebhookProperties.class}) at lines 35-107 and there is no @ConfigurationPropertiesScan in influora-api (grep for both returns only this file plus javadoc references). A new @ConfigurationProperties class not added to that list is never made a bean, so any live bean constructor-injecting it fails the context at startup. The file's own comment at L70-95 documents exactly this: 'Everything below was @ConfigurationProperties but registered NOWHERE ... The application has never booted; `mvn` green only ever proved it COMPILED.' Step 1 must add `TrendIngestProperties.class,` to that list.
- FATAL / TEST BREAKAGE — influora-api/src/test/java/com/influora/config/ConfigurationPropertiesRegistrationTest.java classpath-scans com.influora for @ConfigurationProperties and asserts every hit is either in InfluoraApiApplication's @EnableConfigurationProperties list or carries a @Component-meta-annotated stereotype. Step 1's class has neither, so merely creating the file turns this existing, Docker-free unit test red. The plan claims no test or registration impact at all.
- WRONG ANCHOR — step 6: deploy/utho/docker-compose.utho-shared.yml has NO `CREATOR_COPILOT_AI_BASE_URL` line and no `# ---- Creator AI Co-pilot batch` comment. `grep -n 'CREATOR_COPILOT|COPILOT'` on that file returns nothing (exit 1). Its influora-api AI base-URL list runs L185-189 and stops at ANALYZE_SITE_AI_BASE_URL; it also has no CREATOR_COPILOT_ENABLED and no MEERA_CREATOR_ENABLED — the whole block that is utho.yml L171-176 is absent. The plan's 'structurally identical (confirmed...)' and 'grep found the CREATOR_COPILOT_AI_BASE_URL line but the read in this task did not capture its precise line number here' are both false: the line does not exist, so the described insert cannot be performed as written. That file's own header claim ('Differs from docker-compose.utho.yml ONLY by JAVA_TOOL_OPTIONS') is itself stale — do not use it as a landmark.
- MISREAD, INVERTED — step 5 cites deploy/utho/docker-compose.utho.yml L249-253 as support for the bare `${VAR}` form. The comment says the opposite and is graded Critical: 'Q7.4 (Critical) — `:-false`/`:-`, NOT a bare `${VAR}`: see the matching comment in deploy/hostinger/docker-compose.hostinger.yml for why a present-but-empty compose env var crashed API boot'. Neighbouring lines follow that rule (META_CREATOR_MARKETPLACE_ENABLED: ${META_CREATOR_MARKETPLACE_ENABLED:-false}, ADMIN_NOTIFICATION_EMAIL: ${ADMIN_NOTIFICATION_EMAIL:-}, META_SYSTEM_IG_USER_ID: ${META_SYSTEM_IG_USER_ID:-}). Use `${NEWSAPI_KEY:-}` / `${TMDB_API_KEY:-}` / `${YOUTUBE_API_KEY:-}`; the bare-form META_APP_ID/META_APP_SECRET at L239-240 are the older pattern Q7.4 was written to supersede, not the precedent to copy.
- INCOMPLETE ENUMERATION — 'Of the three deploy composes' is wrong: FOUR compose files carry an influora-api service with an explicit-map `environment:` block — deploy/utho/docker-compose.utho.yml, deploy/utho/docker-compose.utho-shared.yml, deploy/hostinger/docker-compose.hostinger.yml, and deploy/hostinger/docker-compose.test.yml (181 lines; influora-api at L78, environment at L82, AI base URLs L99-102). The plan's own stated risk (an explicit-map block forwards only the keys listed) applies there too; it is never mentioned.
- WRONG LINE COUNT / WRONG CHARACTERISATION — step 4 says application-prod.yml is '109 lines, fully read' and 'only overrides two categories'. The file is 108 lines, and it has a third category the plan's reasoning depends on omitting: prod-only opt-in for feature flags that default false in application.yml — `cleanup: dry-run: ${CLEANUP_DRY_RUN:false}` (L100-101) live, plus commented `brand-safety-scoring.enabled` (L93-95) and `meera-interaction-log-retention.enabled` (L106-108) blocks whose comments say prod is the one place the opt-in should be live. A new TREND_INGEST_ENABLED flag is exactly that category, so 'application-prod.yml needs no changes' rests on an incomplete description of the file.
- BINDS TO NOTHING — step 1 adds a `pullCron` field (default "0 0 5 * * *") but step 2's yaml block contains only enabled / newsapi-api-key / tmdb-api-key / youtube-api-key. With no `pull-cron: ${TREND_INGEST_PULL_CRON:...}` line in application.yml, that cron is not overridable by env in any deploy — unlike every other cron in the file (caption-sync-cron L514, theme-tag-batch-cron L515). The two steps contradict each other.
- OVERSTATED READ — step 3 says SecretsStartupValidator.validate() checks 'only signing/encryption secrets, Razorpay's core-payment-path fields, DB credentials, cookie flags, and AI base-URLs'. validate() (L265-335, not L264-337) also calls validateJwksPrivateKey (L372), validateAdminMfaEncryptionKey (L417), validateMeeraPublicStreamUrl (L617) and validateApiPublicUrl (L647). The conclusion (no change) still holds, but the enumeration presented as a full read is not one. Also validateForwardHeadersStrategy (L527) is defined but never called from validate().
- VERIFIED-CORRECT, for the record: application.yml L406-407 blank META_APP_ID/META_APP_SECRET defaults; creator-copilot: at L507 ending at theme-tag-batch-cron L515 with the meera_interaction_log comment starting L516; influora.trendspark has no yaml block (only trendspark-ai: at L231) and TrendSparkProperties' javadoc does disclaim secret discipline; influora-api/env.example is 81 lines ending at MSG91_WELCOME_TEMPLATE_ID= with no META_APP_*, RAZORPAY_*, or CREATOR_COPILOT_* var; hostinger.yml L150/151 anchors exist and that file has no META_APP_ID/META_APP_SECRET; root docker-compose.yml has only mysql/redis/clamav; .env.local.example is VITE-only; SecretsStartupValidator does fail closed on all three Razorpay fields (L461-486). No step is already done — TrendIngestProperties.java does not exist and NEWSAPI_KEY/TMDB_API_KEY/YOUTUBE_API_KEY appear nowhere outside wiki/architecture/trendspark-priya-schema-lock.md:133 and a proof-os report.