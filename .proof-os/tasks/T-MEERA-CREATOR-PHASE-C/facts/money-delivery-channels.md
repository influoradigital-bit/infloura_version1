# Phase C fact sheet (condensed) — money, deliverables, metrics, disputes, notifications, phone @ HEAD 143ca1e

Verified by direct file reads on 2026-09-04. Meera tools/deals/prefs/frontend facts: `../T-MEERA-CREATOR-PHASE-B/facts/*.md`. Email/invites/users/jobs/config/auth: `../T-MEERA-CREATOR-PHASE-E/facts/email-invites-config.md`. Not repeated here.

## 1. Money

- `service/EscrowService.java`: `release(AuthPrincipal, String workspaceId, String milestoneId)` L593 and 4-arg with `idempotencyKey` L1022; `releaseByHoldId(...)` L624/L1039 — **rejects milestone-backed holds** with `ESCROW_HOLD_HAS_MILESTONE` (409) and `ESCROW_HOLD_NOT_LINKED`; `tryReleaseOnApproval(String workspaceId, String milestoneId) -> ReleaseOutcome` L729: blank id → `held("NO_MILESTONE")`; missing → throws `MILESTONE_NOT_FOUND`; `escrowHoldId == null` → `held("MILESTONE_NOT_FUNDED")`; else `releaseInternal` with expected-skip codes `MILESTONE_NOT_FUNDED, INVALID_ESCROW_STATE, RELEASE_CONDITION_NOT_MET, ESCROW_BLOCKED_BY_DISPUTE` (L838-846). `record ReleaseOutcome(boolean released, String heldReason)` L786, `ReleaseOutcome.RELEASED`. `getStatusForCreator(AuthPrincipal, String escrowHoldId)` L1163 (only creator-reachable hold read); `listForWorkspace` L1142; `freezeUnreleasedForDispute(String collaborationId) -> int` L1175; `hasFundedUnreleasedEscrow(String)` L1203; `hasFrozenEscrow` L1217; `countFrozenHolds` L1236; `adminReleaseForDispute/adminRefundForDispute/adminSplitForDispute` L1247/1286/1333; `initiateFund` L213; `deriveFundAmount` L344; `confirmFunded` L451. `releaseInternal` L848-942 order: milestone by workspace → funded check → hold FOR UPDATE → collaboration → cancellation guard → dispute guard → idempotent RELEASED no-op → `requireStatus(FUNDED)` → `assertReleaseConditionSatisfied` → `escrowBackend.release(ReleaseCommand(...))` idempotency `"release:" + holdId` → mark released → `safelyCreateServiceInvoice` → `publishPayoutReleasedEvent`.
- Release gate: `assertReleaseConditionSatisfied` L1545-1586 **fails open when `influora.escrow.release-gate.cutover-instant` is blank (the default)**; reads deliverables by collaboration (`findByCollaborationIdOrderBySlotIndexAsc`), not by milestone; `satisfyingStatusesFor`: ON_APPROVAL → APPROVED/POSTED/METRICS_REPORTED/VERIFIED; ON_POSTED → POSTED/METRICS_REPORTED/VERIFIED; ON_VERIFIED_METRICS → VERIFIED. `PaymentMilestone.releaseCondition` NOT NULL, builder default `ON_POSTED`; `ContractService.generate` never sets it (L314-321 sets id/contractId/collaborationId/sequenceNo/description/amount/dueDate only).
- `domain/entity/Payout.java` (`payouts`): `id`, `milestone_id` (nullable, null for lump-sum withdrawals), `creator_user_id` NN, `razorpay_payout_id` NN unique (64; `"pending:"+id` / `"manual:"+id` sentinels), `fund_account_id` NN, `amount(12,2)`, `currency(3)`, `status` **String** (RazorpayX states `queued, pending, processing, processed, reversed, cancelled, rejected` plus platform `STATUS_PENDING = "PENDING"`, `STATUS_MANUAL_PAID = "MANUAL_PAID"`), `idempotency_key` NN unique, `webhook_payload` TEXT, `created_at`, `updated_at`, `confirmed_at` (null = in flight), `payout_method` (`METHOD_GATEWAY`/`METHOD_MANUAL`), `bank_reference(64)`, `tds_amount(12,2)` (**null = not applied, 0.00 = considered, none due**; written ONLY by admin manual payout). Factories `createPending`, `createQueued`, `createManualPaid(id, creatorUserId, amount, currency, bankReference, tdsAmount, idempotencyKey, now)`; `markGatewayConfirmed`, `confirmStatus(newStatus, rawWebhookPayload)`.
- `service/WalletService.java`: `getBalanceForUser(String userId)` L130; `getSummaryForUser(String userId)` L209 (escrow = `paymentMilestoneRepository.sumAmountByCreatorIdAndStatus(userId, FUNDED)`, inFlight = `payoutRepository.sumAmountByCreatorUserIdAndConfirmedAtIsNull(userId)`); `requestCreatorWithdrawal(String userId, BigDecimal amount, String idempotencyKey)` L250; `getPayoutsForCreator(String userId, int page, int limit) -> PagedCreatorPayouts` L452; `getTransactionsForUser` L502; records `PagedWalletTransactions` L113, `PagedCreatorPayouts` L116. `service/PayoutService.queuePayout(AuthPrincipal, String workspaceId, String milestoneId)` L199 (OWNER/ADMIN).
- `WalletTransaction` (`wallet_transactions`): `wallet_id, group_id, direction (TxnDirection DEBIT/CREDIT), type (WalletTransactionType DEPOSIT, WITHDRAWAL, ESCROW_HOLD, ESCROW_RELEASE, ESCROW_REFUND, PLATFORM_FEE, PAYOUT, ADJUSTMENT, AFFILIATE_COMMISSION), amount(14,2), currency, balance_after, status (TransactionStatus PENDING/COMPLETED/FAILED/REVERSED), reference_type (TxnReferenceType COLLABORATION, ESCROW_HOLD, MILESTONE, CAMPAIGN, DEPOSIT_ORDER, MANUAL, AFFILIATE_EARNING), reference_id, description(300), idempotency_key, gateway_ref(120), created_at`. `EscrowStatus`/`MilestoneStatus`: `PENDING, FUNDED, RELEASED, REFUNDED, FROZEN`.
- `web/dto/money/MoneyDtos.java` (`public final class`): `WalletBalanceResponse` L43, `WalletSummaryResponse(BigDecimal availableBalance, BigDecimal escrowLocked, BigDecimal pendingPayouts, Integer runwayDays)` L75, `CreatorWithdrawRequest` L82, `WalletTransactionRowResponse` L123, **`CreatorPayoutRowResponse(String id, String reference, BigDecimal amount, String currency, String status, boolean failed, Instant requestedAt, Instant settledAt)` L169** with the L148-157 note: TDS and GST fields deliberately absent, "TDS and GST are unimplemented platform-wide"; `EscrowFundRequest(campaignId, milestoneId)` L188 (no amount, SEC MF-1); `EscrowStatusResponse(String escrowHoldId, String workspaceId, String campaignId, String milestoneId, BigDecimal amount, String currency, EscrowStatus status, Instant fundedAt, Instant releasedAt)` L199; `MilestoneDto(String id, String contractId, String collaborationId, int sequenceNo, String description, BigDecimal amount, String currency, LocalDate dueDate, MilestoneStatus status, String escrowHoldId)` L259 (**no releaseCondition on the wire**); `MilestoneWriteRequest(int sequenceNo, String description, BigDecimal amount, LocalDate dueDate)` L281; `ContractGenerateRequest(@NotBlank String collaborationId, @Size(max=16383) String terms, @Valid List<MilestoneWriteRequest> milestones)` L323; `PayoutResponse` L398.
- TDS docs: `src/content/legal/tds-policy.md` = "v0 DRAFT, PENDING CA REVIEW, noindex", rate literally `[TDS RATE — pending CA confirmation]`; `wiki/tech/TASK-TDS-194O-SPEC.md` status "NOT STARTED — blocked on Swapnil sign-off + CA review"; nothing computes TDS; PAN stored plaintext (accepted gap). Open CA question: withholding at release or at payout.
- Platform fee: `application.yml:373 platform-fee-percent: ${PLATFORM_FEE_PERCENT:15.00}` → `RazorpayProperties.getPlatformFeePercent()` → single consumer `service/AmountDerivationService.java:68`.
- Invoices: `CampaignServiceInvoice` (`escrow_hold_id` NN UNIQUE, `gross_amount`, `creator_gstin`, `tcs_amount` (flat 1%), `status MarketplaceInvoiceStatus`, `issued_at`, `pdf_r2_key`; **no tds_amount**); `CampaignServiceInvoiceService.createAtRelease(...)` REQUIRES_NEW L150; failures swallowed by `EscrowService.safelyCreateServiceInvoice` L1430-1450 into table `escrow_invoice_failures` (`V20260828120000`, unique per hold) with `retryFailedInvoiceFailures()` L379. A released milestone can have no invoice.
- Routes: `WalletController` `/wallet`: `GET /wallet/balance` L61, `GET /wallet` L73, `POST /wallet/topup` L95, `POST /wallet/withdraw` L116 (201, `Idempotency-Key` header), `GET /wallet/transactions` L141, `GET /wallet/payouts` L175 → `List<CreatorPayoutRowResponse>`, `GET/POST /wallet/payout-methods`, `PUT .../primary`. `EscrowController` `/wallet/escrow`: `GET` L58, `POST /fund` L68, `GET /{escrowHoldId}` L91 (creator branch → `getStatusForCreator`), `POST /release` L107 (mandatory `Idempotency-Key`, exactly one of milestoneId/escrowHoldId), `POST /refund` L152 (no FE caller by design), `POST /payout` L168. Admin: `AdminFinanceController` `/admin/finance`: `GET /escrow` L50, `GET /reconciliation` L60, `POST /payouts/{id}/retry` L72, `POST /payouts/manual` L91 (`ManualPayoutRequest(creatorUserId, amount, bankReference, tdsAmount, note)`, Idempotency-Key required, SUPER_ADMIN + MFA). `AdminEscrowController` `/admin/escrow`: only `GET /flagged`.

## 2. Deliverables and metrics

- `domain/entity/Deliverable.java` (`deliverables`): `id, collaboration_id NN, creator_profile_id NN, milestone_id (26) **permanently NULL — never set**, slot_index int NN, type (DeliverableType) NN, title(200), description TEXT, status (DeliverableStatus) NN, deadline LocalDate, version_number int, revision_count int, files_json json, caption TEXT, hashtags_json json, creator_notes TEXT, review_notes TEXT, post_url(500), post_id(100), posted_at, submitted_at, approved_at, reviewed_at, created_at, updated_at`. **No max_revisions, no keep_up_until.** Mutators: `applyUpload`, `applySubmit`, `applyApprove` (APPROVED + approvedAt + reviewedAt), `applyRevision(String feedback)` (REVISION_REQUESTED, revisionCount++), `applyReject`, `applyMetricsReport`, `applyMarkPosted(String postUrl)` (POSTED + postedAt), `applyVerify` (VERIFIED, idempotent), `applyMediaCleanup` (nulls filesJson).
- `DeliverableStatus`: `PENDING, DRAFT, SUBMITTED, REVISION_REQUESTED, RESUBMITTED, APPROVED, REJECTED, POSTED, METRICS_REPORTED, VERIFIED`. `DeliverableType`: `INSTAGRAM_POST, INSTAGRAM_REEL, INSTAGRAM_STORY, INSTAGRAM_CAROUSEL, YOUTUBE_VIDEO, YOUTUBE_SHORT, FACEBOOK_POST, FACEBOOK_REEL, TIKTOK_VIDEO`.
- `service/CreatorDeliverableService.java`: `verifyNow(AuthPrincipal, String deliverableId) -> VerificationStateResponse` L193; `uploadContent` L219; `uploadProof` L289; `submitForReview` L327; `reportMetrics(AuthPrincipal, String deliverableId, MetricsReportRequest)` L494; `markPosted(AuthPrincipal, String deliverableId, MarkPostedRequest)` L591 (requires APPROVED else `INVALID_STATE` 409); `getStatus` L717; `listForCollaboration` L728; `listForCollaborations` L763. **Contradiction:** L1134-1148 returns `maxRevisions = null` claiming "NO revision cap" (F-0360) while `BrandDeliverableService.requestRevision` L340-352 enforces `collaboration.getMaxRevisions()` → `REVISION_LIMIT_REACHED` 409 (F-0417).
- `service/BrandDeliverableService.java`: `getDetail` L103; `approve(AuthPrincipal, String deliverableId) -> ReviewResponse` L109 (locks row, `applyApprove`, then `escrowService.tryReleaseOnApproval(workspace.getId(), deliverable.getMilestoneId())` → always `held("NO_MILESTONE")` today, then `collaborationLifecycleService.onDeliverableReviewed`, `applicationHistoryService.record`); `requestRevision(AuthPrincipal, String deliverableId, ReviseRequest)` L325; `reject` L385; events `DeliverableApprovedEvent(deliverableId, collaborationId)` L310, `DeliverableRevisionRequestedEvent` L322 (plain records, not NotificationEvents).
- Routes: `CreatorDeliverableController` `/creator/deliverables`: `GET /` L48, `GET /bulk` L63, `POST /{id}/upload` L72, `GET /{id}/status` L87, `POST /{id}/submit` L94, `POST /{id}/metrics` L103, `POST /{id}/proof` L112, `POST /{id}/mark-posted` L121, `POST /{id}/verify` L135. `BrandDeliverableController` `/deliverables`: `GET /{id}` L39, `POST /{id}/approve` L46, `POST /{id}/revise` L53, `POST /{id}/reject` L64, `GET /{id}/safety-review` L81. `DealController`: `GET /deals/{dealId}/deliverables` L179, `POST /deals/{dealId}/disputes` L186.
- `web/dto/deliverable/CreatorDeliverableDtos.java`: `DeliverableStatusResponse(String id, String collaborationId, DeliverableType type, String title, DeliverableStatus status, LocalDate deadline, int versionNumber, int revisionCount, List<DeliverableFileResponse> files, String caption, List<String> hashtags, String creatorNotes, String reviewNotes, Instant submittedAt, Instant reviewedAt, DeliverableActions actions, String metricSource, Instant lastVerifiedAt, boolean metaConnected)` L29; `VerificationStateResponse(String deliverableId, String outcome, String metricSource, Long reach, Long impressions, Long engagements, Instant lastVerifiedAt, boolean metaConnected, boolean manualFallbackAllowed)` L60; `DeliverableListItem(String id, String title, String description, DeliverableStatus status, boolean completed, Integer currentRevision, Integer maxRevisions)` L77; `MetricsPayload(Integer likes, Integer comments, Integer shares, Integer views, Integer reach, Integer impressions, Integer saves)` L87; `MetricsReportRequest(MetricsPayload metrics, List<String> proofScreenshots, Integer reportedDaysAfterPosting)` L97; `MarkPostedRequest(String livePostUrl)` L116; `MarkPostedResponse(String id, DeliverableStatus status, String postUrl, Instant postedAt)` L118.
- `job/DeliverableVerificationJob.java`: `@Scheduled(cron = "0 30 */6 * * *")` + `@SchedulerLock(name = "DeliverableVerificationJob", lockAtMostFor = "PT2H", lockAtLeastFor = "PT1M")`; candidates `deliverableRepository.findByStatusInAndPostUrlIsNotNull(Set.of(POSTED, METRICS_REPORTED))`; never revisits VERIFIED. `service/verification/DeliverableVerificationService.verify(Deliverable) -> Outcome` L124; `Outcome {VERIFIED, FALLBACK_NO_POST_URL, FALLBACK_NO_MILESTONE, FALLBACK_UNRECOGNIZED_URL, FALLBACK_YOUTUBE_UNSUPPORTED, FALLBACK_NO_TOKEN, FALLBACK_TOKEN_EXPIRED, FALLBACK_RATE_LIMITED, FALLBACK_NOT_FOUND, FALLBACK_API_ERROR, FALLBACK_DATA_INTEGRITY}`; **short-circuits `FALLBACK_NO_MILESTONE` whenever `milestoneId` blank (always today)**; `RECENT_MEDIA_LIMIT = 50`; `RATE_LIMIT_THRESHOLD_PERCENT = 90`; URL parsed by `PostUrlIdentifier.extract(postUrl)`; YouTube unsupported.
- `DeliverableMetric` (`deliverable_metrics`): `milestone_id NN UNIQUE`, `collaboration_id NN`, `reach, impressions, engagements` Long, `source(20)`, `platform_media_id(100)`, `verified_at`, `link(1000)`, `proof_screenshot_r2_key(500)`, `reported_by_creator_id NN`, `reported_at NN`. Repository: `findByMilestoneId`, `findByCollaborationIdIn`, `findByCollaborationId`. **One row per milestone, no series.** `MediaMetric` (`media_metrics`): `time DATETIME(6), media_id(50), creator_profile_id, platform, media_type, caption, permalink(500), impressions, reach, engagement, likes, comments, saves, shares, video_views, avg_watch_time_seconds, posted_at, data_source, fetched_at` — per post time series, **not linked to deliverables**. `CreatorMetric`, `AudienceDemographics` as in Phase B facts.
- `job/MetricsPollingJob.java`: `@Scheduled(cron = "0 0 */6 * * *", zone = "UTC")`, lock `MetricsPollingJob` PT2H/PT1M; iterates `tokenRepository.findByRevokedFalseAndExpiresAtAfter(now)`; `RECENT_MEDIA_LIMIT = 25`; writes `creator_metrics` and, when `metaProperties.isMediaMetricsEnabled()` (default true), `media_metrics` via `InstagramMetricsFetcher`. `AudienceDemographicsJob` weekly `0 30 3 * * SUN`.
- `integration/meta/client/InstagramInsightsClient.java`: constants `USER_FIELDS`, `MEDIA_FIELDS = "id,caption,media_type,media_url,permalink,timestamp,like_count,comments_count"`, `INSIGHTS_METRICS = "reach,likes,comments,saved,shares,views,total_interactions"`, `ACCOUNT_METRICS`, `AUDIENCE_METRICS`; methods `getProfile(igUserId, token[, authPath])` L45/50; `getMedia(igUserId, token, int limit[, authPath])` L61/66; `getMediaInsights(mediaId, token, businessAccountId[, authPath])` L81/86; `getAudienceDemographics` L97/102; `businessDiscovery` L115; `getAccountInsights(igUserId, token, since, until[, authPath])` L137/144. **No single-media GET, no stories list, no story insights.** `MetaGraphApiClient.get(String path, String accessToken, Class<T> responseType, String businessAccountId[, MetaAuthPath])` L92/104; throttles at `rateLimitThrottleThreshold` (90) via `MetaRateLimitTracker` keyed by `igBusinessAccountId`; 429 → `MetaRateLimitException`, 401 → `MetaTokenExpiredException`, 403 → `MetaPermissionDeniedException`. `MetaApiProperties` (`influora.meta`): `graphApiVersion` default `v25.0`, `rateLimitAlertThreshold 80`, `rateLimitThrottleThreshold 90`, `mediaMetricsEnabled true`. Scopes: `REQUIRED_SCOPES = instagram_basic, instagram_manage_insights, pages_show_list`; `INSTAGRAM_LOGIN_SCOPES = instagram_business_basic, instagram_business_manage_insights`.
- `repository/DeliverableRepository.java`: `findByIdAndCreatorUserId(id, creatorUserId)` (JPQL via Collaboration.creatorId), `findByCollaborationIdOrderBySlotIndexAsc`, `@Deprecated findByMilestoneId` (always empty), `findByCollaborationIdIn`, `@Lock(PESSIMISTIC_WRITE) findByIdAndWorkspaceId`, `findByStatusInAndApprovedAtBefore`, `findByStatusInAndPostUrlIsNotNull`, `findByStatusInAndUpdatedAtBefore`.
- `service/ContractService.java`: `generate(AuthPrincipal, String workspaceId, ContractGenerateRequest)` L146; `materializeDeliverables` L734-778 builds deliverables from the last proposal's `metadataJson` slots (`latestAgreedDeliverableSlots`) with `Deliverable.builder().id().collaborationId().creatorProfileId().slotIndex().type().title(humanizeType(type) + " #" + n).build()` — **milestoneId never set**; milestones built L310-323. `Collaboration.DEFAULT_MAX_REVISIONS = 2`; `max_revisions` set only via `DealService.applyDealTermsIfPresent` (`terms.maxRevisions() != null ? ... : 2`).
- `job/DeliverableCleanupJob.java`: `@Value("${influora.cleanup.dry-run:true}")` (dry run default); `cleanupSupersededRevisions` `0 0 2 * * *` (approvedAt older than 30 days); `cleanupAbandonedDrafts` `0 30 2 * * *` (updatedAt older than 90 days); deletes R2 media; guarded by active dispute and unreleased escrow.

## 3. Disputes

- `Dispute` (`disputes`): `id, collaboration_id NN, opened_by_type (DisputeOpenerType CREATOR/BRAND), opened_by_user_id NN, reason TEXT NN, status (DisputeStatus OPEN, UNDER_REVIEW, RESOLVED_BRAND, RESOLVED_CREATOR, RESOLVED_SPLIT), resolved_by_admin_id, resolution_notes TEXT, resolved_at, created_at, updated_at, version (@Version)`. **No evidence, attachments, or responses.** `UNDER_REVIEW` never written by any route.
- `service/DisputeService.java`: `openDispute(AuthPrincipal, String dealId, OpenDisputeRequest) -> DisputeResponse` L107 (requires `escrowService.hasFundedUnreleasedEscrow` else `NO_FUNDED_ESCROW` 409; `DISPUTE_ALREADY_OPEN` 409; freezes escrow BEFORE save; `collaboration.transitionTo(DISPUTED)`); `resolveDispute(AuthPrincipal, HttpServletRequest, ...)` L195 (admin); `list` L382; `listDisplayForBrand` L483; `listDisplayForCreator` L494. **No respond method.**
- DTOs `web/dto/dispute/DisputeDtos.java`: `OpenDisputeRequest(@NotBlank @Size(max=2000) String reason)` L18; `DisputeResponse(String id, String collaborationId, String openedByType, String openedByUserId, String reason, String status, Instant createdAt, String resolvedByAdminId, String resolutionNotes, Instant resolvedAt)` L20; `ResolveDisputeRequest` L48; `DisputeListItemResponse(String collaborationId, String campaignName, String counterpartyName, BigDecimal dealValue, String currency, String disputeStatus, Instant openedAt, String reason)` L60.
- Routes: `POST /deals/{dealId}/disputes` (201); `CreatorDisputeController` `/creator/disputes`: `GET` L33, `GET /eligible-deals` L45 (read-only); `BrandDisputeController` `/brand/disputes/list`; `AdminDisputeController` `/admin/disputes` `GET` L44, `POST /{id}/resolve` L73.

## 4. Notifications and channels

- `Notification` (`notifications`): `id, user_id NN, workspace_id nullable, event_type(64) NN, title NN, body TEXT, link(512), is_read boolean NN, created_at`. **No channel column.** `EmailPreference` (`email_preferences`): `user_id, event_type(64), unsubscribed boolean`. **No NotificationPreference entity, no channel dimension, no phone/WhatsApp consent, no STOP.**
- `NotificationController` `/notifications`: `GET` L82 → `NotificationListResponse(List<NotificationResponse> notifications, long unreadCount, int page, int size)`; `POST /read` L103; `POST /read-all` L133; `GET /unsubscribe-link` L192; `GET /preferences` L253 → `PreferencesResponse(List<NotificationPreferenceItem(eventType, unsubscribed)>)` (sparse); `POST /preferences` L270 `SetPreferenceRequest(eventType, subscribed)`. `NotificationResponse(String id, String eventType, String title, String body, String link, boolean isRead, Instant createdAt)`.
- `NotificationService.notify(event, title, body, link, toEmail, templateKey, templateData)` L69 and `notifyInApp(event, title, body, link)` L98. `NotificationListener` handlers relevant: `EscrowFundedEvent` L245, `PayoutReleasedEvent` L291, `ContractPendingSignatureEvent` L276, `ContractReadyForEscrowEvent` L376, `DeliverableSubmittedEvent` L391, `MonthlyStatementEvent` L607 (**no publisher**). No `MilestoneUnfundedEvent`, `ApprovalOverdueEvent`, `DisputeOpenedEvent`.
- **SMS: none.** `integration/msg91/` has only `EmailPreviewResult, EmailTemplateRegistry, Msg91EmailClient`; `msg91.sender-id/route/widget-id/token-auth` have no read site. OTP is email-only (`EmailOtpChallenge`, `BrandEmailOtpService`, `V5__email_otp.sql`). **WhatsApp: none** (marketing copy only). **Push: none** (no web-push/FCM/VAPID dependency, no manifest.json, no service worker in `public/`).
- Phone: `User.phone_number` unique, `phone_verified` NN **never set true anywhere**; `service/UserPhoneService`: `normalizeAndValidate(String rawPhone)` L53 (`INVALID_PHONE` 400), `isTaken(String)` L68, `applyPhone(User, String rawPhone)` L103 (`PHONE_ALREADY_EXISTS` 409); `common/IndianPhoneUtils`: `VALID = ^[6-9]\d{9}$`, `normalize(String)` L32, `isValid(String)` L46. `EmailPhonePiiCipher` exists. No phone consent record.

## 5. Jobs and misc

- 20 jobs; only nudge job is `CreatorConnectNudgeJob` (email, `enabled:false` default, `0 30 9 * * *` IST, per-user try/catch, stage ladder `dueStage(createdAt, now)`). No late-approval or unfunded-milestone nudge, no SLA clock, no de-dup ledger for deal nudges. `NudgeLog`/`CreatorNudgeLog` are TrendSpark tables.
- `service/ErrorLogService.record(...)` REQUIRES_NEW L50; `ErrorLog` (`error_log`) entity.
- Admin manual payout is the only place a TDS number is ever recorded (typed by a human).

## 6. AI service

- No scheduler, no digest route, no delivery module in `influora-ai`. `outcome_digest` is a per-conversation Block-B render (`assembler.py:317-385`, `_render_outcome_digest`), produced by `MeeraContextService.buildOutcomeDigest` L190 and `BrandContextAssembler`. Creator `deals_summary` render `assembler.py:589-599` uses only `active_count`, `completed_count`, `total_earned_inr`; `metrics_summary` L581-587; floors L601-613.

## 7. Frontend

- `src/pages/creator-wallet.tsx`: `api.wallet.platformFee()` L429, `api.wallet.get('creator')` L450, `api.wallet.transactions('creator', 1, 20, period)` L485, `api.wallet.payouts(1, 20)` L522, `getPayoutMethods` L548, `withdraw(amount, idempotencyKey)` L596, `addPayoutMethod` L627, `setPrimaryPayoutMethod` L646; types `CampaignServiceInvoice, PlatformCommissionInvoice, WalletTransactionRow, CreatorPayoutRow, PayoutMethod`; `useServiceInvoices`, `PaymentsUnavailableNotice`.
- `src/lib/api.ts` money: `MoneyOperation` L65; `PAYMENTS_IN_ENABLED` L72, `PAYOUTS_ENABLED` L105 (payouts OFF: "collection live, payouts manual"); `WalletSummaryResponse` L3102; `CreatorPayoutRow { id, reference, amount, currency, status: string, failed, requestedAt, settledAt }` L3122 (TDS note L3114-3121); `WalletTransactionRow` L3137 (**omits AFFILIATE_COMMISSION**); `EscrowHoldRow { escrowHoldId, status: 'PENDING'|'FUNDED'|'RELEASED'|'CANCELLED', amount, currency, campaignId, milestoneId, fundedAt }` L3163 (**drift: server has REFUNDED/FROZEN, no CANCELLED; omits workspaceId, releasedAt**); `PayoutMethod` L3179; `wallet.*` L3239-3370; `payments.releasePayout(request)` L3651; deliverable review `approve/requestRevision/reject` L3014-3057 (`approve` returns `{ status, paymentReleased, paymentHeldReason }`).
- Creator deal-room components: `deliverable-card.tsx` props L21-37 (status union of 4 values, `revision?: {current, max}`); `deliverable-lifecycle-panel.tsx` props L31-36; `deliverable-submission.tsx` L16-36 (`Deliverable { id, title, description, completed, currentRevision?, maxRevisions? }`); `revision-handler.tsx` L16-28; hook `src/hooks/creator/useCreatorDeliverableLifecycle.ts` L30/L52. Brand review: `src/components/brand/timeline/panels/deliverable-review-panel.tsx` `onApprove?(eventId, feedback)` L38; `src/components/brand/contracts/contracts-and-deliverables.tsx` hard-codes `maxRevisions: 2` (L203, 216, 226, 273, 283, 330), payments tab L1241/1575; `src/components/brand/deal-room/deal-payments-tab.tsx` (`DealPaymentsTabProps { dealValue, contractStatus, deliverablesDone, deliverablesTotal, escrowFunded?, campaignId?, milestones?, onFunded? }`, `FundEscrowButton` from `src/components/feature/meera/FundEscrowButton.tsx`, `useEscrowFund` hook, `handleRelease(milestoneId)` → `api.payments.releasePayout`).
- `src/pages/creator-disputes.tsx`: `api.creatorDisputes.list()` L64, `listEligibleDeals()` L78, `open(selectedDealId, reason)` L106; form = deal select + `Textarea#dispute-reason`; **no file input**.
- `src/hooks/useNotifications.ts`: `REFRESH_INTERVAL_MS = 60_000`; `Notification { id, type: 'info'|'success'|'warning'|'meera_nudge', title, body?, read, createdAt, link?, surfaceInChat? }`; result `{ notifications, unreadCount, loading, error, refresh, markRead, markAllRead }`. `creator-notifications.tsx` uses `useNotifications('creator')`. `api.notifications` L3753-3811 (`list, markRead, markAllRead, getPreferences, setPreference`; note L3789-3791 "no backend model for push/SMS/digest channels"). Preferences UI only in `brand-settings.tsx` L418-490; `creator-settings.tsx` L62-83 has local-only switches (`proposals, deadlines, payments, marketing, sms, email`; only `email` persists).
- Phone: `src/lib/phone.ts` exports `normalizePhone`, `isValidPhone`, `filterPhoneInput`; `creator-settings.tsx` phone dialog L91-191 → `api.creatorProfile.patchMe({ phone })`; no OTP, no consent.
- No PWA: no manifest, no service worker, no push dependency.

## 8. Gaps Phase C must build (37, condensed)

Money: no per-deal money timeline; TDS never computed (recorded only on manual payouts; wire omits it by decision); no fee/TCS/TDS/net join; no UTR on gateway rail; **milestone-deliverable link absent**; release gate disabled by blank cutover; payout status raw string; invoices can be missing (failure table). Delivery: no per-post snapshots; no Story support; no single-media probe; no keep-up-until; no YouTube; revision-cap drift (creator null vs brand enforced vs FE hard-coded 2). Channels: no SMS, no WhatsApp, no push, no channel dimension, no opt-in/STOP, no phone verification, no creator prefs UI. Nudges: none for late approval or unfunded milestones; no SLA clock; no de-dup ledger; `MonthlyStatementEvent` has no publisher. Disputes: no evidence model, no respond path, UNDER_REVIEW unreachable, requires funded escrow, revisions default not surfaced. Evidence pack: nothing exists; cleanup job races retention. AI: `deals_summary` carries no money state; no digest scheduler. Client drift: `EscrowHoldRow.status`, `WalletTransactionRow.type`, `deliverable-card` status union.
