# Phase E fact sheet (condensed) — email, invites, users, jobs, config, search, auth, frontend @ HEAD 8c7b18b

Verified by direct file reads on 2026-09-04. The Meera tool loop, deals, preferences and creator frontend facts live in `../T-MEERA-CREATOR-PHASE-B/facts/*.md` and are not repeated.

## 1. Email sending (influora-api)

- `service/notification/EmailWorker.java` — `@Component` L117; `@Scheduled(fixedDelay = 30000)` + `@SchedulerLock(name = "EmailWorker_processOutbox", lockAtMostFor = "PT5M", lockAtLeastFor = "PT10S")` on `processOutbox()` L188-190; batch 50; claim lease 3 min; send at L298 `msg91Client.sendTemplateEmail(toEmail, templateKey, templateData, userId)`.
- `integration/msg91/Msg91EmailClient.java` — **SMTP relay via Spring `JavaMailSender`** (`smtp.mailer91.com`), NOT an HTTP API. Constructor L71-79 injects `ObjectProvider<JavaMailSender>`, `@Value("${influora.msg91.email.from-email:noreply@mail.influora.in}")`, `@Value("${influora.msg91.email.from-name:Influora}")`, `@Value("${influora.api.public-url}")`, `@Value("${server.servlet.context-path:}")`, `InfluoraEnvironment`, `ObjectMapper`, `UnsubscribeTokenService`. Methods: `isConfigured()` L94; `sendTemplateEmail(String toEmail, String templateKey, String templateDataJson)` L104 (pre-account recipients); `sendTemplateEmail(toEmail, templateKey, templateDataJson, userId)` L129; `renderPreview(templateKey, Map)` L199. Send body L158-167: `MimeMessageHelper(message, true, "UTF-8")`, `setFrom(fromEmail, fromName)`, `setTo`, `setSubject`, `setText(plain, html)`, `mailSender.send`. **No `setReplyTo` anywhere. One fixed From. The relay only accepts a domain verified with MSG91 as envelope sender** (javadoc L38-47); only `mail.influora.in` has SPF `include:mailer91.com` + DKIM.
- `integration/msg91/EmailTemplateRegistry.java` — package-private; `SPECS` static map L57 populated in a static block from L59; `render(templateKey, data, unsubscribeUrl)` L406; unknown key still sends with a generic subject L415-425. 37 keys, incl. `creator.join_invitation` (L336), `brand.workspace_invite_new_user` (L317), `admin.creator_connection_requested` (L327), `brand.connected_creator_joined` (L345). `NO_UNSUBSCRIBE_FOOTER` L394.
- `domain/entity/EmailOutbox.java` — `MAX_RETRIES = 5` L23; columns `user_id` NOT NULL, `to_email`, `template_key`(64), `template_data` JSON, `status`, `retry_count`, `next_retry_at`, `sent_at`, `error_message`(512), `created_at`, `idempotency_key`(128). `EmailOutboxStatus {PENDING, SENT, FAILED}`.
- `service/notification/NotificationEvent.java` — **sealed interface**, `permits` list L9-49 (34 records; last three `CreatorConnectionRequestedEvent`, `ConnectedCreatorJoinedEvent`, `CreatorNotConnectedEvent`). Methods `eventType(), userId(), workspaceId(), entityId()`. Adding an event = new record + permits entry + a handler in `NotificationListener`.
- Example record `event/CreatorConnectionRequestedEvent.java` L12-25: `record CreatorConnectionRequestedEvent(String userId, String workspaceId, String entityId, String brandName, String igUsername, Long followers, String message) implements NotificationEvent { eventType() -> "admin.creator_connection_requested" }`.
- `service/notification/NotificationService.java` — no `publish`. Entry points: `notify(NotificationEvent event, String title, String body, String link, String toEmail, String templateKey, Map<String,Object> templateData)` L68-76 (@Transactional) and `notifyInApp(event, title, body, link)` L97-98. `EMAIL_ONLY_EVENTS` L41-42 = `auth.otp, auth.reset, cron.monthly_statement, creator.not_connected`; `IN_APP_ONLY_EVENTS` L45-46. Idempotency key = `eventType:entityId:userId` L181-183. Domain services publish via `ApplicationEventPublisher.publishEvent(...)`; `NotificationListener` (`@Component` L89) handlers are `@Async @TransactionalEventListener(phase = AFTER_COMMIT)`; deps L94-112 include `Msg91EmailClient`, `String adminNotificationEmail`, `String webBaseUrl`.
- application.yml: `spring.mail.host/port/username/password` from `SMTP_HOST/SMTP_PORT(587)/SMTP_USERNAME/SMTP_PASSWORD` L23-27; starttls L38, ssl L40. `influora.msg91.*` L315-333 incl. `email.domain ${MSG91_EMAIL_DOMAIN:mail.influora.in}`, `email.from-email`, `email.from-name`. `influora.web-base-url` L131-133; `influora.api.public-url` L134-140; `influora.admin.notification-email ${ADMIN_NOTIFICATION_EMAIL:}` L287. env.example has MSG91_* (L65-81) but NO SMTP_* keys; SMTP only in `deploy/hostinger/docker-compose.hostinger.yml` L179-184.
- **Inbound email: none.** No IMAP, no mail webhook, no bounce handler. Webhook controllers that exist: `ConversionWebhookController` (`POST /webhooks/redemption`, `/webhooks/conversion`, `GET /track/click/{id}`), `ShopifyWebhookController` (`/webhooks/shopify`), `WooCommerceWebhookController` (`/webhooks/woocommerce`), `RazorpayWebhookController` (`/webhooks/razorpay`), `MetaPlatformCallbackController` (`/webhooks/meta/deauthorize`, `/webhooks/meta/data-deletion`).

## 2. Connection requests and invites

- `domain/entity/CreatorConnectionRequest.java` (`creator_connection_requests`): `id, workspace_id, requested_by_user_id, external_creator_id, message(1000), status(ConnectionRequestStatus {PENDING, CONTACTED, JOINED, DECLINED}), admin_notes, handled_by, handled_at, joined_notified_at, created_at, updated_at`. Mutators `markContacted/markDeclined/markInvited/markJoined/markJoinedNotified`. This is **brand -> external creator**, admin-brokered. There is no creator -> brand request entity.
- `domain/entity/ExternalCreator.java` (`external_creators`): `source (ExternalCreatorSource {META_MARKETPLACE, BUSINESS_DISCOVERY, ADMIN_IMPORT})`, `ig_username`, `email`(255), `status (ExternalCreatorStatus {UNVERIFIED, INVITED, JOINED})`, `linked_creator_profile_id`, `invited_at`, `joined_at`. `markInvited(String email)` L232, `markJoined(String creatorProfileId)` L251.
- `service/admin/AdminCreatorConnectionService.java` — `invite(AuthPrincipal, HttpServletRequest, String id, String email, String rawNotes)` L208-210; builds `signup_url = webBaseUrl + "/creator/register?ref=influora-invite&handle=" + ig + "&invite_token=" + inviteTokenService.issue(external.getId(), external.getInvitedAt())` L277-287; sends `creator.join_invitation` via the 3-arg `sendTemplateEmail` L293-294; admin audit via `adminAuditLogService.record(principal, httpRequest, action, entityType, entityId, before, after, notes)` (8 args).
- `service/InviteTokenService.java` — `@Service`; `TOKEN_TTL = 30 days` L47; secret from `@Value("${CREATOR_INVITE_TOKEN_SECRET:dev-...}")` L77-81 (bare env var, min 32 chars, fail-closed outside dev/test L85); `issue(String externalCreatorId, Instant invitedAt)` L118 (payload `id:invitedAtSeconds:expiresAtMillis` + HmacSHA256 hex, base64url); `verify(String token) -> Optional<Parsed>` L132; `record Parsed(String externalCreatorId, Instant invitedAt, Instant expiresAt)` L185. **Stateless HMAC token, no token column.**
- `service/RegistrationService.java` — `consumeInviteToken(String token, String creatorProfileId)` L57, never throws; called from `AuthService.creatorRegister` L373.
- `domain/entity/WorkspaceMemberInvite.java` (`workspace_member_invites`): `email`, `role (MemberRole)`, `invite_token_hash`(64) = `JwtService.hashToken(raw)`, raw = `jwtService.createRefreshTokenValue()`, `expires_at`, `status (MemberInviteStatus {PENDING, ACCEPTED, EXPIRED, REVOKED})`. `WorkspaceMemberService.inviteMember(AuthPrincipal, String rawEmail, MemberRole role)` L126-127 (OWNER/ADMIN only; role != OWNER); `acceptInvite(AuthPrincipal, String rawToken)` L190-191; `INVITE_TTL_DAYS = 7`. `MemberRole {OWNER, ADMIN, MANAGER, MEMBER, VIEWER}`; MANAGER is brand-side only.
- Controllers: `ExternalCreatorController` `@RequestMapping("/creators/external")` (`GET`, `GET /lookup`, `POST /{id}/connect`, `GET /connection-requests`); `AdminCreatorConnectionController` `/admin/creator-connections`; `WorkspaceMemberController` `/workspace/members` (`POST /invite`, `POST /accept`, `GET`, `GET /invites`, `POST /invites/{id}/revoke`, `POST /switch`).

## 3. Users and workspaces

- `domain/entity/User.java` (`users`): `email` `@Column(unique = true)` L21-22 (no explicit name, nullable); `user_type` NOT NULL (`UserType {BRAND, CREATOR, ADMIN}`) L31-32; `status`, `email_verified`, `display_name`, `first_name`, `last_name`, `timezone`, `deleted_at`. `softDelete()` blanks email.
- `repository/UserRepository.java`: **only email finder is `Optional<User> findByEmailIgnoreCase(String email)` L61**; `existsByEmailIgnoreCase` L63; `existsByPhoneNumber` L71.
- `domain/entity/Workspace.java` (`workspaces`): `name`(200), `slug` unique, `type (WorkspaceType)`, `logo_url`, **`website_url`(500) L34**, `industry`(100) L37, `company_size`, `description`, `verification_status`, `billing_email`, `phone`, `gstin`, `pan`, suspension fields. `WorkspaceRepository`: `existsBySlug`, `countByType`, `findByTypeAndVerificationStatusOrderByCreatedAtAsc`, `findIdsByType(WorkspaceType)` L46-47, `findBySuspendedTrue`. **No domain lookup, no website-domain column.**
- `domain/entity/BrandProfile.java` (`brand_profiles`): `workspace_id`, `website_url`(500) L25-26, `analysis_status`, `product_catalog`, `brand_aesthetic`, `tone_profile`, `niche_tags`, `competitor_urls`, `theme_tags`. **No `industry`, no display name** (both on `Workspace`). `BrandProfileRepository` has only `findByWorkspaceId`.
- No URL/host normaliser helper in `com.influora.common` (only `SlugUtils`, `UsernameUtils`, `TextSanitizer`, `JsonLists`, `Ulids`, `IndianPhoneUtils`, ...).
- `web/dto/auth/BrandRegisterRequest.java` L28-37: `record BrandRegisterRequest(@NotBlank @Size(max=50) String firstName, @NotBlank @Size(max=50) String lastName, @NotBlank @Email String email, @NotBlank @Size(min=8,max=128) String password, @NotBlank @Size(max=200) String companyName, @Size(max=100) String industry, @Size(max=50) String companySize, @AssertTrue Boolean acceptedTerms, @Size(max=20) String phone)`.
- `service/AuthService.java` — `brandRegister(BrandRegisterRequest req)` L120-121: `existsByEmailIgnoreCase` guard; `User.newBrand(...)` L162-169; `Workspace.newBrand(workspaceId, req.companyName(), slug, req.industry(), req.companySize())` L185-186; saves user, workspace, `WorkspaceMember.owner(...)`, `Wallet.forWorkspace(...)` L198-201; `eventPublisher.publishEvent(new UserCreatedEvent(userId, workspaceId, userId, displayName, "brand"))` L222; `issueTokens(user, workspace)` L224. **Does NOT create a BrandProfile** (only `AnalyzeSiteTriggerService` does, L111/L224).

## 4. Audit and jobs

- `service/AuditLogService.java`: constants `ACTOR_MEERA_AI, ACTOR_HUMAN, ACTOR_SYSTEM, ACTOR_SERVICE`, `OUTCOME_ALLOWED, OUTCOME_REJECTED, OUTCOME_FAILED` L27-34. Methods (all REQUIRES_NEW): `recordToolCall(String workspaceId, String toolName, String toolTier, String outcome, String reasonCode, String idempotencyKey, BigDecimal serverAmount, Map<String,Object> detail)` L43-52; `recordAuthRejection(String workspaceId, String eventType, String reasonCode, String actorId)` L70-71; `recordAdminAction(String actorId, String eventType, String outcome, Map<String,Object> detail)` L94-96; `recordMoneyEvent(...)` L109-117. `detail` must never contain PII, prompts or transcripts.
- `config/SchedulerLockConfig.java`: `@EnableSchedulerLock(defaultLockAtMostFor = "PT10M")`, `JdbcTemplateLockProvider` (shedlock table, V66).
- Job pattern `job/CreatorConnectNudgeJob.java`: `@Component`; constructor `@Value("${influora.web-base-url}")`, `@Value("${influora.creator-connect-nudge.enabled:false}")`, batch-limit; `@Scheduled(cron = "${influora.creator-connect-nudge.cron:0 30 9 * * *}", zone = "Asia/Kolkata")` + `@SchedulerLock(name = "CreatorConnectNudgeJob", lockAtMostFor = "PT20M", lockAtLeastFor = "PT1M")` L86-88. `@EnableScheduling` at `InfluoraApiApplication.java:111`, `@EnableAsync` :119.

## 5. Config patterns

- application.yml: every key `${ENV:default}` under `influora.*`; placeholder defaults `REPLACE_WITH_...`. Two idioms: `@ConfigurationProperties` class (MUST be registered in `InfluoraApiApplication` `@EnableConfigurationProperties({...})` L36-108, 26 entries, guarded by `ConfigurationPropertiesRegistrationTest`) or `@Component` + `@Value` (preferred for flags; `MeeraCreatorFeatureProperties`). Examples: `MeeraStreamProperties` (`influora.meera.stream`), `AnalyzeSiteAiProperties` (`influora.analyze-site-ai`, `baseUrl`, `connectTimeoutSeconds 5`, `requestTimeoutSeconds 60`, defensive setters).
- Deploy: root `docker-compose.yml` has only mysql/redis/clamav/n8n. Real compose: `deploy/hostinger/docker-compose.hostinger.yml` (`influora-api` service L103-232, `environment:` L107; `influora-ai` service L244+), `deploy/utho/docker-compose.utho.yml`. `CREATOR_INVITE_TOKEN_SECRET` is in the hostinger compose L194 but not in env.example.

## 6. Security config

- `config/SecurityConfig.java` permitAll entries (ordered, first match wins): `/health`, `/auth/**` variants, `POST /webhooks/razorpay` L105-106, `/webhooks/shopify` L118-119, `/webhooks/woocommerce` L120-121, `/webhooks/conversion` L122-123, `/webhooks/redemption` L124-125, `/webhooks/meta/deauthorize` L134-135, `/webhooks/meta/data-deletion` L136-137, `GET /.well-known/jwks.json` L140-141, `GET /notifications/unsubscribe-link` L146-147, `GET /track/click/**` L152-153, `GET /portfolio/*` L168-169, `POST /portfolio/*/contact` L170-171, `GET /public/creators/*/verified` L177-178, `POST /client-errors` L185-186, `POST /admin/auth/login|refresh` L194-197. Role gates: `/admin/**` hasRole ADMIN L207-208; `/creator/**` hasRole CREATOR L227-228; `anyRequest().authenticated()` L230. New public webhooks go BEFORE the role gates.
- `security/AuthRateLimitFilter.java`: `bucketFor` L296; GET branch L300-316; POST branch L322-402; `limitFor` L405-427; `windowSecondsFor` L430; `isUserKeyedBucket` L440; only POST and GET throttled L291-293. 5 edits per new bucket (pattern constant, bucketFor branch, limitFor case, @Value limit field, isUserKeyedBucket).
- `security/JsonAuthErrorHandler.java`: 401 `UNAUTHENTICATED`, 403 `FORBIDDEN` via `ApiResponse.fail(ApiErrorBody.of(code, message))`.

## 7. Referral attribution: none exists

No `referred_by`, `introduced_by`, signup `source` column on users, workspaces, brand_profiles or campaigns. Documented as deliberately absent at `web/dto/admin/AdminMarketingDtos.java:9-14`, `AdminMarketingController.java:20-21`, `AdminDashboardController.java:30`. Closest precedent: `CreatorConnectionRequest.requestedByUserId` + `ExternalCreator.source` + the stateless `InviteTokenService` HMAC.

## 8. AI service: site analyzer, clients, SSRF

- `app/routes/analyze_site.py`: `async def perform_site_analysis(url: str, workspace_id: str, request_id: str | None = None) -> dict` L142-144; never raises; spend gate first (`check_spend_gate(workspace_id=..., reserve_usd=...)` L158-178); fetch `await anyio.to_thread.run_sync(lambda: guarded_fetch(url))` L185-190; `extract_structured_facts(html)` L224; `strip_active_content` L233; `wrap_untrusted_scrape(text[:20000])` L261; `gemini.classify_site(...)` L268; records spend with `estimate_cost_usd(GEMINI_MODEL, usage)` L270-289. Return shapes: `{"success": False, "error": {...}, "spend_blocked": True}`; `{"success": False, "error": {"code": "url_rejected"|"empty_page"|"classify_failed", ...}, "degraded": "paste_a_link"}`; success `{"success": True, "data": {"source_url", "niche_tags", "tone_dial", "brand_color", "product_catalog"}}`. Route `POST /analyze-site` L314-341 uses `verify_token(token, endpoint="analyze_site", body_workspace_id=workspace_id)` in a thread L332-334.
- `app/prompt/structured_extract.py`: `ScrapedProduct` dataclass L71; `extract_json_ld_products`, `extract_opengraph_facts` (L275, returns dict of og:* props), `extract_opengraph_product`, `extract_microdata_product`, `extract_structured_facts(raw_html) -> list[ScrapedProduct]` L327.
- `app/clients/` has ONLY `spring.py`: `SpringCallError(status_code, code, message, body)` L43; `SpringResponse` L53; `SpringInternalClient` L79 with `httpx.AsyncClient(base_url=settings.spring_internal_base_url, timeout=httpx.Timeout(connect=..., read=..., write=..., pool=...))` L80-91; `_build_signed_headers` L96 sets `X-Meera-Service-Token` (from `mint_service_token()`), `X-Onbehalf-Authorization`, `X-Meera-Signature`, `X-Meera-Timestamp`, `X-Meera-Nonce`, `X-Meera-Key-Id`, optional `Idempotency-Key`; methods `call_tool_endpoint` L129, `persist_assistant_message` L199, `persist_analyze_site_result` L232, `get_meera_context` L269, `log_interaction` L303, `release_turn_credit` L344.
- `app/security/ssrf_guard.py`: `def guarded_fetch(url: str, *, max_bytes: int | None = None, timeout: float | None = None) -> tuple[bytes, str]` L150 — **synchronous**, https-only (`settings.ssrf_allowed_schemes = ("https",)`), private/metadata IP denylist L36-48, DNS pin, `follow_redirects=False` with manual hop loop capped by `settings.ssrf_max_redirects` (2), body cap `settings.ssrf_max_response_bytes` (5 MB), `SsrfBlockedError`. No host allow/deny list. Outbound HTTP is `httpx`.

## 9. AI service settings and auth

- `app/config.py`: `@dataclass(frozen=True) class Settings` L213-214; provider keys `anthropic_api_key`, `gemini_api_key`, `sarvam_api_key` via `field(default_factory=lambda: os.getenv("...", ""))` L230-232. **No search-provider key exists.** `ProviderTimeouts` L168-194 (`claude_connect 3.0, claude_read 30.0, gemini_read 20.0, spring_connect 2.0, spring_read 5.0, scrape_total 30.0`). `require_boot_secrets()` L531-559 lists ANTHROPIC/GEMINI/SARVAM/SPRING_JWKS_URL/INTERNAL_HMAC_KEY/SERVICE_TOKEN_SIGNING_KEY. `get_settings()` is `@lru_cache(maxsize=1)` L562-564. New key = Settings field + optional boot check + `main.py` `/readyz` `keys_loaded` L198-205 + `influora-ai/env.example` + compose env.
- `app/main.py`: unconditional routers `chat, analyze_site, voice` L70-72; optional routers in try/except blocks L74-107 (`trendspark, brand_safety, trend_tag, creator_suggestion`); `/healthz` L137, `/readyz` L191.
- `app/auth/service_token.py`: `SCOPE_SERVICE = "service"`, `SCOPE_CHAT_STREAM = "chat:stream"`, `SCOPE_CREATOR = "creator"` L40-50; `ENDPOINT_SCOPES` L52-69 = `{"chat": (service, chat:stream), "analyze_site": (service,), "voice_transcribe": (service,), "voice_speak": (service,), "brand_safety": (service,), "trendspark": (service,), "creator_suggestion": (creator,)}`; `verify_token(token, *, endpoint, body_workspace_id) -> VerifiedToken` L366-371 (sync; 401 `missing_scope`, 403 `scope_mismatch`, 401 `missing_workspace_claim`, 403 `tenant_mismatch`); `verify_creator_token(token, *, endpoint, body_creator_profile_id) -> VerifiedCreatorToken` L420-425 (claim `creator_profile_id` snake_case only); `verify_token_async(...)` L489-494 adds jti replay guard for chat:stream; `auth_error_to_http(exc)` L546; `extract_bearer_token` L553.
- `app/auth/service_token_minter.py`: `mint_service_token(*, ttl_seconds=None) -> str` L51, HS256 with `settings.service_token_signing_key`, `iss "meera-python"`, `aud "influora-internal"`, `sub "meera-ai-service"`, max TTL 60 s. Java side `security/InternalServiceTokenFilter.java` only guards `/internal/**`.

## 10. Frontend

- `src/lib/api.ts`: `externalCreators` namespace L1903-1974 (`list`, `lookup`, `connect(id, {message})`, `connectionRequests()`), types `ExternalCreator` L1849, `ConnectionRequest` L1876; `workspaceMembers` L1125-1157 (`list`, `invite(email, role)`); `notifications` L3712-3770 (`list(role)`, `markRead`, `markAllRead`, `getPreferences`, `setPreference`); aggregate `api` object at L6313.
- Brand connection-requests panel: `src/components/brand/discover/creator-discovery.tsx` (`fetchRequests` L2167-2176, `api.externalCreators.connect` L2228). Brand notifications: `src/pages/brand-notifications.tsx` (`useNotifications('brand')` L39). Brand invite UI: `src/pages/brand-settings.tsx` L484-501, role select L1015-1027.
- `src/pages/brand-register.tsx` and `brand-login.tsx` read **no query params**. `src/pages/creator-register.tsx` L32-35 reads `ref`, `handle`, `invite_token` via `useSearchParams` and passes `inviteToken` L112.
- `src/pages/creator-settings.tsx`: container L449; `<ConnectedAccounts />` L458; section cards `<Card className="mb-6">` with `<CardTitle className="text-base">`. `src/components/creator/connected-accounts.tsx` `export function ConnectedAccounts()` L47, root `<Card className="mb-6">` L110.
- Admin: `src/pages/admin-console.tsx` nested routes L63-79 (`users/*, campaigns, finance, revenue, support, moderation, creator-connections, disputes, billing, audit, errors, emails, emails/compose, creator-agent`); nav in `src/admin/components/AdminLayout`. `src/admin/services/api-contracts.ts` namespaces incl. `creatorConnectionsApi` L696-754, `emailApi` L854-946 (`getStats()` L876-882 returns `{ sent24h, failed24h, pending, avgDeliveryTime }`), `creatorAgentApi` L1039.

## 11. Gaps Phase E builds from scratch

1. Inbound email (no webhook, no IMAP, no reply-to, no per-message From).
2. Per-creator outreach aliases (no entity, no column).
3. Web search / brand discovery (no provider key, no client, no host allowlist; `guarded_fetch` is the only sanctioned fetch and is synchronous).
4. Referral attribution (no columns anywhere).
5. Brand-side `?ref=` / token handling on register.
6. Creator -> brand pitch entity and brand inbox for it.
7. Level-2 auto-decline (nothing exists).
