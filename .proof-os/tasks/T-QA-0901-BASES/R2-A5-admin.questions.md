# Admin Base — 100 Questions

## Authentication, Sessions & Roles

1. `AdminContextService.requireMfaSatisfied` checks `admin.isMfaEnabled()` on every privileged call, and `AdminAuthService`'s class javadoc states role/MFA state is deliberately NOT carried in the JWT. Once an admin has enabled MFA once, does any later request ever force a fresh TOTP challenge again, or does a bare valid access token forever satisfy the MFA gate for the life of the account?
2. `AdminAuthService.login` enforces MFA-required-at-login for `SUPER_ADMIN`/`ADMIN` only when `AdminSecurityProperties.isMfaEnforceOnLogin()` is true. Trace what actually happens to a pre-existing `SUPER_ADMIN` row with `mfaEnabled=false` when that flag is true and no admin-user-creation/reset endpoint exists — does the account become permanently unable to authenticate?
3. `AdminAuthController.refresh` reads the refresh token from the HttpOnly cookie first and falls back to a request-body token only when the cookie is absent. Does `AdminRefreshTokenRepository`/`AdminAuthService.refresh` reject or accept a body-supplied token that doesn't match the most-recently-rotated cookie token, and can a stale body token from `authApi.refreshToken()` silently mint a new session for a token that was already rotated out?
4. `apiRequest()` in `api-contracts.ts` reads `body?.error?.message` off the raw JSON while `GlobalExceptionHandler` always wraps errors as `{ error: { code, message } }` even on controllers (like `AdminAuthController`) that return raw unwrapped success DTOs. For a failed login (wrong password, MFA lockout, `MFA_ENROLLMENT_REQUIRED`), what string does the SPA actually render to the admin?
5. `requireRoleWithMfaSatisfied` throws distinct codes (`ADMIN_NOT_FOUND`, `INSUFFICIENT_ROLE`, `MFA_SETUP_REQUIRED`) for three different failure reasons. Does every admin page/hook that calls an admin endpoint actually branch on these codes, or do all three collapse into one generic "access denied" toast on the frontend?
6. `AdminAuthService`'s lockout increments `failed_login_attempts` on both a wrong password AND a wrong MFA code, sharing one counter and one cooldown window (`AdminSecurityProperties`). Does a `SUPER_ADMIN` who repeatedly mistypes their TOTP code (e.g. a stale app) get locked out of the account by the same threshold as a brute-force password attacker, and is there any in-app way to clear that lockout before the cooldown expires?
7. `AdminMfaSecretCipher` AES-256-GCM-encrypts the TOTP secret, and `AdminAuthService` is documented as "the ONLY place that ever handles the plaintext TOTP secret." Trace `setupMfa`/`verifyMfa` end to end — does the decrypted plaintext secret ever get logged, returned in a response body, or held in a field longer than the verification call needs it?
8. `AdminRole` has three tiers (`SUPER_ADMIN`, `ADMIN`, `SUPPORT`). For every `Admin*Controller` in the codebase, is there a role tier for which NO endpoint exists at all — i.e., an admin logged in with that role who can authenticate successfully but has zero working screens?
9. `src/admin/__tests__/rbac-permission-matrix.test.ts` presumably encodes an expected role→endpoint permission matrix. Pick three endpoints from that matrix and verify each one's actual backend `requireRoleWithMfaSatisfied(...)` call lists the exact same roles the test file expects — where do they diverge?
10. `AdminAuthCookieService.writeRefreshCookie` sets the refresh token as an HttpOnly cookie on login AND on every refresh. If an admin's browser is closed without calling `/auth/logout`, does the refresh token in `admin_refresh_tokens` ever get invalidated, or does a captured cookie remain valid indefinitely until natural expiry?

## Brand & Creator Moderation

11. `ApprovalWorkflowService.processApproval` throws `APPROVAL_ACTION_NOT_IMPLEMENTED` (HTTP 501) for any `CONTENT_MODERATION` composite id, yet `getPendingApprovals` happily lists `CONTENT_MODERATION` rows in the same unified queue as actionable `BRAND_KYC`/`CREATOR_APPLICATION` rows. Does the moderation-queue UI (`ApprovalQueue.tsx`) distinguish these rows before rendering an action button, or can an admin click "Approve" on a content-flag row and get a 501?
12. `AdminModerationService.actionFlag`'s `"REMOVE"` branch calls `flag.markActioned("REMOVE", adminId)` and writes an audit log entry — trace whether it touches the actual flagged entity (`contentType`/`contentId` — a post, portfolio item, campaign description, etc.) anywhere. Does clicking "Remove" in `FlagQueue.tsx` actually take the offending content off the platform, or only change the `ContentFlag` row's own status while the content stays live?
13. `AdminModerationController`'s class javadoc says `"ESCALATE" (P2+, not yet implemented)`, but `AdminModerationService.actionFlag`'s switch statement fully implements an `"ESCALATE"` case that flips the flag to `ESCALATED`. Which is true today — does POST `/admin/moderation/flags/{id}/action` with `action: "ESCALATE"` succeed or fail, and does `FlagQueue.tsx` even expose an Escalate control?
14. Escalating a flag requires the same role tier (`SUPER_ADMIN`/`ADMIN`/`SUPPORT`) as resolving one — the same `SUPPORT`-tier admin who escalated a flag "for senior review" can immediately resolve it themselves. Does any code path prevent the escalating admin from being the one who later actions their own escalation?
15. `moderationApi.getSuspensions(status?)` sends an optional `status` query param, and `AdminModerationController.listSuspensions` accepts a `status` `@RequestParam` — but `AdminModerationService.getSuspensions` never reads it. Confirm: does changing the status filter dropdown in the suspensions UI change the returned list at all?
16. `AccountSuspensionDto.appealStatus` is hardcoded to `"NONE"` for every row (`AdminModerationService.getSuspensions`), and `moderationApi.reviewAppeal` is wired to `unavailable()` client-side. Is there ANY code path anywhere in the backend that can move a suspended account's appeal status off `NONE`, or is the appeal column permanently dead UI?
17. `AdminBrandService.suspend`/`AdminCreatorService.suspend` both throw `ALREADY_SUSPENDED` (409) if the entity is already suspended. Does the frontend's suspend button disable itself once a brand/creator is already suspended, or can a double-click / stale cache produce a visible 409 error toast on an already-suspended row?
18. `ContentFlagRepository.findPendingReviewQueue` sorts ESCALATED flags ahead of PENDING ones. Does `PagedFlagsDto`'s `total` field (used for pagination controls in `FlagQueue.tsx`) correctly reflect this combined PENDING+ESCALATED count, or could the page-count UI disagree with the number of rows actually rendered?
19. `ApprovalWorkflowService`'s class javadoc explicitly states `DELIVERABLE_DISPUTE`/`ESCROW_RELEASE`/`ACCOUNT_SUSPENSION` return an empty slice for those `type` filters. Does the moderation-queue UI's type-filter control (if it exposes those three values) render an empty state, or does it hide those filter options entirely — and if it doesn't hide them, is there a UI path where an admin filters to one of these types and sees a permanently-empty "0 items" queue mistaken for "nothing needs review"?
20. `AdminCreatorService.reviewApplication` and `AdminBrandService.verifyKyc` are called BOTH directly (`AdminCreatorController`/`AdminBrandController`) AND indirectly via `ApprovalWorkflowService.processApproval`. Do both call paths write the exact same audit-log entity type/action strings, or could the same underlying mutation show up under two different audit-log signatures depending on which UI screen triggered it?

## Suspension & Reinstatement

21. `Workspace.suspend(reason, adminId)`/`CreatorProfile.suspend` set a boolean flag plus reason/by/at columns. Trace every authenticated brand/creator endpoint's entry path (not just login) — does a `SUSPENDED` workspace/creator actually get blocked from campaign creation, deliverable submission, wallet withdrawal, and every other mutating action, or only from a subset the suspension check happens to guard?
22. Given the repo's recent commit "suspension now blocks auth (F-0451/0457/0458) + in-flight phone persistence" — before that fix, could a brand/creator admin-suspended mid-session keep using an already-issued JWT for the rest of its lifetime? Does the current code invalidate outstanding tokens on suspend, or only block future logins?
23. `AdminCreatorService.reinstate`/`AdminBrandService.reinstate` throw `NOT_SUSPENDED` (409) if the entity isn't currently suspended. Does the creator/brand detail page's reinstate button hide itself for a non-suspended entity, or is it always rendered regardless of suspension state?
24. Reinstating a brand/creator clears the suspended flag — does it also clear or preserve `suspendedReason`/`suspendedBy`/`suspendedAt` on the entity row, and if preserved, does any UI read those stale fields on a reinstated account and mislabel it as still suspended?
25. Is there a maximum-suspension-duration or auto-reinstatement mechanism anywhere in the codebase, or does a suspension persist indefinitely until a human admin manually calls reinstate — and if indefinite, does the suspensions list (`getSuspensions`) ever paginate, or does it load every suspended account unbounded in one response as the platform grows?
26. `CreatorTierAdjustment`/`adjustTier` — does adjusting a creator's tier down (e.g., demoting) have any interaction with an active suspension, or can a suspended creator's tier still be silently adjusted while they're locked out?
27. `BrandKycAction`/`verifyKyc` and `suspend` both mutate the same `Workspace` row. If a brand is suspended while its KYC review is still PENDING, does the KYC approval flow later still succeed and silently un-suspend nothing (leaving the brand KYC-verified but still suspended), or does it interact unexpectedly with the suspension state?
28. `CampaignSummaryDto`/campaign detail views show a `brandId` reference — when a brand is suspended, do that brand's already-ACTIVE campaigns keep running (creators can keep submitting deliverables, escrow keeps releasing) or does suspension cascade to pause its campaigns? Trace the actual code path, not the intended behavior.

## Campaign Oversight

29. `campaignApi.list(filters, page, pageSize)` explicitly voids `filters` and sends only `page`/`pageSize` — `AdminCampaignController.list()` takes no `@RequestParam`s for search/status/type/brandId/atRisk. Does any admin campaign screen's filter UI still render input controls for these fields, and if so, what happens to a value typed into them?
30. `campaignApi.listAll()` returns a bare `CampaignSummary[]` array while `campaignApi.list()` returns a `PaginatedResponse<CampaignSummary>` envelope with `total`/`totalPages`. Which of these two does `useCampaignList.ts` actually call, and does the admin UI's pagination control compute page counts from a real total or from `array.length` of whatever page happened to load?
31. `AdminCampaignService`'s class javadoc documents `spent`, `creatorCount`, `deliverablesPending`, `deliverablesApproved`, `slaBreachRate` as previously hardcoded 0/0.0 values now fixed (H-22). Verify each of those five fields is actually computed from a real query in the current file, not still defaulted for at least one of them.
32. `timelineStatus` on `CampaignSummaryDto` is hardcoded to the literal `"ON_TRACK"` string for every single campaign, per the class javadoc's own "DEFERRED" note. Does `CampaignTable.tsx` render this status as a colored badge (e.g., green "On Track"), and would an admin be able to tell a genuinely on-track campaign from a badly-delayed one just by looking at that column?
33. Campaign `"type"` is hardcoded to the literal `"STANDARD"` for every campaign regardless of whether it's actually a HYPE campaign. Does `campaignApi.getHypeOps()`'s dashboard (active HYPE count, fill rate) agree with what `CampaignTable.tsx`'s type column shows for the same campaigns, or do the two screens disagree about which campaigns are HYPE?
34. `campaignApi.getAtRisk()` defines "at-risk" as `slaBreachRate > 0` on an ACTIVE campaign per the service javadoc, while `AdminDashboardService.getOperationsSummary`'s `campaignsAtRisk` field is a hardcoded `0` with a TODO. Do the admin dashboard's "at-risk" KPI tile and the campaigns "At Risk" list ever agree on a nonzero count, or does the dashboard tile always read zero while the list beneath it is nonempty?
35. `escrowBalance` on campaign detail is defined as FUNDED-only holds, while this same service's `spent` field is defined as FUNDED+RELEASED. Does the campaign detail page label these two fields clearly enough that an admin wouldn't mistake `escrowBalance` for total money spent on the campaign?
36. `AdminCampaignController` now returns the true row count via an `X-Total-Count` response header per the class javadoc. Does `campaignApi.list()`'s `apiRequest()` wrapper actually read response headers at all, or does it only ever parse the JSON body — meaning is `X-Total-Count` reachable from any admin UI code, or a header nothing consumes?
37. Given `CampaignFilters` includes `search`/`status`/`type`/`brandId`/`atRisk` and none reach the backend on `list()`, does `useCampaignList.ts` fall back to filtering the FULL unfiltered dataset returned by `listAll()` client-side for these fields — and if the backend later adds pagination to `listAll()`, would that client-side filter start silently searching only the current page instead of the whole campaign set?

## Escrow & Payout Operations (including the Manual Rail)

38. `AdminFinanceService.recordManualPayout` requires a non-blank `Idempotency-Key` header and rejects a duplicate `bankReference` with 409. If an admin's browser retries the exact same form submission (network timeout, double-click) WITHOUT regenerating the idempotency key but the first request actually succeeded server-side before the client saw the response, does the ledger post get de-duplicated by the idempotency key, or does `payoutRepository.findByBankReference` catch the duplicate before the ledger post even runs?
39. `financeApi.recordManualPayout` in `api-contracts.ts` — trace `ManualPayoutPanel.tsx`: does it mint a fresh `idempotencyKey` on every render/mount, or does it generate one key and reuse it correctly across a submit-fail-resubmit cycle within the same form session (per the API's own documented "reused across retries of the SAME submission" contract)?
40. `recordManualPayout` validates `tdsAmount` is between zero and the payout `amount`, but does it ever actually deduct or ledger-post the TDS amount anywhere, or does it just store the number on the `Payout` row for later reporting while the full `amount` moves through `WalletLedgerService.post`?
41. `escrowApi.hold`/`escrowApi.release`/`escrowApi.refund` are all wired to `unavailable()` — `AdminEscrowController` exposes ONLY `GET /flagged`, no mutating routes. Given a FROZEN escrow hold with no linked dispute (`FlaggedEscrowDto`'s `FROZEN_NO_DISPUTE_REASON` fallback), is there ANY admin-reachable code path that can ever move that money, or does it stay frozen forever since the dispute-mediated release path requires a `Dispute` record that was never created?
42. `AdminFinanceService.getEscrowSummary`'s `pendingRelease` field is defined as `countByStatus(EscrowStatus.FUNDED)`. Does "pending release" actually mean escrow that is queued/scheduled for imminent release, or does it just count every currently-funded hold regardless of whether release is anywhere near — could this KPI badly overstate an operational backlog that doesn't exist?
43. `AdminFinanceService.retryPayout` delegates to `PayoutReconciliationService.retryFailedPayout` and is deliberately NOT `@Transactional` because it makes a live RazorpayX call. If that outbound call throws partway through, does the admin see a clear failure, or could the payout end up in an inconsistent state where the retry attempt is recorded but the RazorpayX call never actually fired (or vice versa)?
44. `financeApi.retryPayout(id)`'s response type includes `razorpayPayoutId` — does `ManualPayoutPanel`/`ReconciliationPanel`'s retry button actually surface this new payout id back to the admin after a successful retry, or does the UI just show a generic success toast with no way to trace the new attempt?
45. `getReconciliation`'s `resolvePayoutGatewayFigure` falls back to a live RazorpayX call only when `webhookPayload` is absent, and on ANY exception (transport error, RazorpayX outage) silently returns `null`, which the caller maps to `RECON_STATUS_PENDING`. Can a genuinely MISMATCHED payout (money actually diverges) get masked as merely "PENDING" if RazorpayX happens to be flaky at the exact moment an admin loads the reconciliation tab, causing them to skip investigating it?
46. `financeApi.resolveReconciliation` (MATCH/WRITE_OFF) is wired to `unavailable()` — the comment says "webhook-driven, no admin mutation." Once `getReconciliation` reports a row as `MISMATCH`, is there ANY way for an admin to actually resolve, dismiss, or acknowledge it inside the admin panel, or does every mismatch remain permanently flagged on every future reconciliation-tab load for that date?
47. `financeApi.getTdsReport(quarter)` is wired to `unavailable()` with the comment "TDS engine unimplemented." Given `recordManualPayout` DOES capture a `tdsAmount` per payout, where — if anywhere — does that captured TDS data actually get aggregated for a compliance filing, or does it just accumulate in individual `Payout` rows with no reporting surface at all?
48. `WalletRepository.findByOwnerIdForUpdate` row-locks the creator's wallet before `recordManualPayout` checks `wallet.getBalance().compareTo(amount) < 0`. Does the equivalent live-RazorpayX payout path (`retryFailedPayout`/`PayoutService`) apply the same row-level lock before debiting, or could a manual payout and an automated payout racing on the same creator wallet double-spend the balance?
49. Trace `PlatformWalletService.requireClearingWallet()`, called by `recordManualPayout` — if the clearing wallet doesn't exist or is misconfigured in a given environment, does the manual-payout endpoint fail loudly with a clear error before touching the creator's wallet, or could it partially debit before discovering the clearing wallet is missing?

## Dispute Adjudication

50. `AdminDisputeController` exposes `list()` and `resolve()` but explicitly has NO `GET /admin/disputes/{id}`. When an admin clicks into a specific dispute from the list, does the detail view reuse the row already loaded from `list()`, or does it silently fail to load fresh detail (and if it silently reuses stale list data, can it go stale between the list load and the resolve action)?
51. `resolve()`'s `creatorSplitPercent` field on `DisputeResolution` is only meaningful when `resolution === RESOLVED_SPLIT`, and the FE javadoc states client-side 0-100 validation is the UI's responsibility while `EscrowService.adminSplitForDispute` is "the authoritative server-side validation." Confirm the server actually re-validates 0-100 independent of the client — what happens if a crafted request sends `creatorSplitPercent: 150` or a negative number directly to the API, bypassing the UI?
52. `DisputeService.resolveDispute` is documented as translating a lost-update race into a 409 via `saveAndFlush`, with `AdminDisputeController.resolve` catching `ObjectOptimisticLockingFailureException` as defense-in-depth. If two admins load the same PENDING dispute and both submit different resolutions (one RELEASE, one REFUND) within the same request window, does the loser's request get a clean 409 telling them to refresh, or could their submitted resolution partially apply before the version check fails?
53. Resolving a dispute is documented as settling escrow (release/refund/split) AND writing to `admin_audit_log` "both done inside `DisputeService.resolveDispute`'s single transaction." Does the escrow settlement and the audit-log write actually share the same transaction, or — consistent with `AdminAuditLogService.record`'s outer try/catch pattern seen elsewhere in this codebase — could the escrow money move successfully while the audit trail for that exact fund movement silently fails to write?
54. `disputeApi.list(filters)` sends `status`/`campaignId`/`brandId`/`creatorId` as optional params. Does `AdminDisputeController.list` combine these as AND filters (all must match) or OR filters, and does the disputes-list UI's filter combination actually communicate that semantic to the admin, or could selecting a brand AND a creator that never had a dispute together silently return zero rows with no explanation?
55. A dispute is created when? Trace what actually opens a `Dispute` row and freezes the linked `EscrowHold` — is there any admin-initiated way to open a dispute on a campaign/collaboration, or can disputes ONLY be created by the brand/creator flagging it themselves, meaning the admin console is purely reactive with no way to intervene on a problem it notices first?
56. Given `ApprovalWorkflowService`'s explicit note that `DELIVERABLE_DISPUTE` returns an empty slice because "nothing in this codebase yet distinguishes 'an admin has been notified and needs to act' from 'a dispute exists,'" is there ANY notification/alert mechanism that tells an admin a new dispute needs review, or does the disputes queue require an admin to manually and periodically poll `GET /admin/disputes` to discover new ones?
57. When a dispute resolves as `RESOLVED_SPLIT`, does the resulting split get posted as two separate ledger entries (one release leg, one refund leg) that together equal the original frozen amount exactly, or is there any rounding/remainder path where the two legs could sum to slightly more or less than the escrow hold's actual amount?

## Finance & Revenue Reporting

58. `AdminDashboardService`'s three `*Change` week-over-week fields (`gmvChange`, `revenueChange`, `activeCampaignsChange`) are documented as permanently `null` — "no snapshot mechanism to diff against yet." Does `PulseDashboard.tsx`'s KPI tile component render a null change value as a blank/dash, or does it coerce `null` to `0` and display a misleading "0% change" trend arrow?
59. `financeApi.getRevenue(startDate, endDate, groupBy)` — does `AdminFinanceController`/whatever backs `/finance/revenue` actually exist, given `PlatformFeeAdminController`'s class javadoc lists `financeApi.getRevenue`/`getEscrowSummary`/`getPayoutQueue`/`getReconciliation` as all mounted under `/finance/*` even though "none of those have a backend controller yet" at the time that comment was written — has a real controller since been built for revenue, or does `RevenuePage.tsx`'s revenue chart still call a route with no handler?
60. `AdminDashboardService`'s red-flag types (`ESCROW_LOW`, `SLA_BREACH`, `PAYOUT_DELAY`, `REVIEW_BACKLOG`) are documented as "not implemented this cycle — need business thresholds." Does `RedFlagDto`'s list ever populate with any of these four types under current code, or is the CEO Pulse dashboard's red-flags panel permanently empty regardless of how bad things actually get operationally?
61. `AdminDashboardStatsCache` — how long is the cached dashboard data considered fresh, and does a `SUSPEND`/`resolve`/payout action taken by an admin invalidate the cache immediately, or could the CEO Pulse dashboard show stale KPI numbers for the length of the cache TTL after a material change?
62. `avgReviewTimeHours()` backing `AdminDashboardService.getOperationsSummary` was fixed from a hardcoded 0.0 to a real mean over a `REVIEW_TIME_WINDOW_DAYS` window per the class javadoc. Confirm what the actual window length is and whether it silently returns 0 (indistinguishable from "reviews are instant") when zero tickets were resolved in that window, rather than an honest null/no-data state.
63. `getEscrowSummary`'s `avgReleaseSeconds` divides by `SECONDS_PER_HOUR` to report hours — does `escrowHoldRepository.avgReleaseSeconds()` compute this average only over RELEASED holds, or could a hold that's been sitting FUNDED (never released) for months skew or be excluded from this average in a way that makes release times look faster than they really are?
64. `financeApi.getFinancialSummary`/`dashboardApi.getFinancialSummary(period)` accepts `'day' | 'week' | 'month' | 'quarter'` — does the backend `/dashboard/financial` endpoint actually support all four period granularities, or only a subset, with the rest silently falling back to one of the supported values?
65. `AdminRevenueService`/`AdminRevenueController` (`RevenueSnapshotDto`) — does the revenue figure reported here include or exclude the platform fee revenue that `PlatformFeeService.deductAtRelease` posts into the platform revenue wallet, and does it agree with the number `AdminFinanceService`'s escrow/reconciliation views would imply for the same period?

## Platform Fee Configuration

66. `PlatformFeeConfig`'s class javadoc states the brand-side fee integration for actually charging brands "is separate, not-yet-built scope," yet `BrandCampaignFeeService`/`BrandPlatformFeeService` both call `requireConfig().getBrandFeeBps()` directly. Which is actually true in the current code — does a SUPER_ADMIN changing the brand fee percentage in `FeeControlPanel.tsx` change what brands are actually charged on their next campaign funding, or is that charging path still using a separate/stale source?
67. `PlatformFeeAdminController.update` requires the client to send `expectedEffectiveDate` as an optimistic-concurrency token, enforced `@NotNull` server-side. If `FeeControlPanel.tsx` fails to refresh its locally-cached `effectiveDate` after some other admin's fee change, does the resulting 409 conflict clearly tell the admin to reload, or could the UI retry the exact same (now-stale) `expectedEffectiveDate` and loop on 409 indefinitely?
68. `PlatformFeeAdminController` is gated `SUPER_ADMIN`-only server-side, while the class javadoc notes `FeeControlPanel.tsx` also has "a client-side role gate (real enforcement lives here, not there)." Does the fee-config screen's UI actually hide the edit form for an `ADMIN`-tier user, or does it render the form and only fail when they submit — and if it renders, does the disabled/hidden state correctly re-check on every navigation or only once at mount?
69. `PlatformFeeAdminService.update` validates `brandFeePercent`/`creatorFeePercent` against `minFeeBps`/`maxFeeBps` bounds stored on the same row. Can a SUPER_ADMIN set `minFeeBps` and `maxFeeBps` to a range that excludes the CURRENT `defaultFeeBps`/`brandFeeBps`, leaving the live fee outside its own configured valid range until the next update?
70. `platformFeeAdminService.history()` returns `PlatformFeeHistoryEntryDto[]` — is a history row written for EVERY successful `update()` call including ones that end up being fee changes of zero net effect (e.g., toggling `razorpayAbsorbedByPlatform` alone), or could some field-only changes update the config without leaving a corresponding history entry an auditor could reconstruct?
71. `PlatformFeeService.resolveCreatorFeeBps()` reads live from the DB "no Java fallback constant" on every escrow release. If a SUPER_ADMIN changes the creator fee mid-collaboration (after a campaign is funded but before its milestone is released), does the NEW rate apply to that in-flight release, and is that the behavior `BudgetOverrideRequest`/campaign-funding flows assume when they calculated the brand's total spend upfront?

## Invoicing

72. `CommissionInvoiceService`/`CampaignServiceInvoiceService` — per the known escrow-invoice-backfill gap (CR-51), a GST invoice can silently fail to generate at escrow release with "only a log.error, no marker/retry/metric." Does any admin screen (finance console, campaign detail, error log) surface WHICH releases are missing their invoice, or is a missing invoice invisible everywhere except grep-ing application logs?
73. Does the admin panel expose ANY read surface for invoices at all (a list, a per-campaign/per-payout invoice reference), or is invoice generation a fully backend/log-only concern with zero admin visibility into whether compliance documents actually exist for completed money movements?
74. When `AdminFinanceService.recordManualPayout` records a bank transfer for a creator, does that manual-rail payout ever trigger the same invoice/TDS-documentation generation that the RazorpayX-automated payout path triggers, or does the manual rail bypass invoicing entirely since it was built as an emergency workaround?
75. `DealService`/`CampaignServiceInvoiceService` — for a dispute resolved as `RESOLVED_SPLIT`, does invoice generation correctly handle a partial release amount (less than the full campaign budget), or was invoicing built assuming a full-amount release and could misfire or under-invoice on a split?
76. Is there a reconciliation between "escrow holds released" (a count `AdminFinanceService` can query) and "invoices generated" (whatever `CommissionInvoiceService` tracks) anywhere in the admin console, or would an admin have no code-level way to answer "how many of this month's releases are missing their invoice" without directly querying the database?

## Marketing & Email Operations

77. `emailApi.sendBulk(templateId, recipientFilters)` — does `AdminEmailController`'s backing endpoint validate `recipientFilters` against a real recipient query before queueing, or could a malformed/overly broad filter object queue an email to every user in the platform with no preview/confirmation step showing the actual recipient count first?
78. `AdminEmailService.getTemplates` returns "the DISTINCT keys actually present in the outbox" rather than a real template catalog. If a template was defined in MSG91 but has never actually been sent yet, does it appear in the admin's template dropdown for `sendBulk`, or is it invisible until the first time it's used — making the admin's own bulk-send tool the only way to discover templates exist?
79. `AdminEmailService`'s `stats.failed24h` is documented as approximate — it counts FAILED rows CREATED in the window, not rows that FAILED in the window (no `failed_at` column). Could an email that was queued 3 days ago and only just failed today be excluded from `failed24h`, understating the actual daily failure rate an admin is monitoring?
80. `emailApi.retry(id)` — does retrying a failed email re-send through the exact same MSG91 template/recipient, or could retrying an email whose underlying campaign/user data has since changed (e.g., recipient deleted their account) silently fail again with a different, unlogged error?
81. `marketingApi.getAcquisition` is wired to `unavailable()` ("marketing analytics not built"), but `marketingApi.getGrowth()` IS live and returns a real `funnel.signups`/`firstCampaign`/`repeatCampaign` plus two conversion rates, explicitly omitting `cohortRetention`/`referralStats`/`funnel.profileComplete`. Does the growth-metrics UI component render placeholder/zero values for these omitted fields, or does it correctly hide those specific chart segments when the data is structurally absent from the response?
82. `marketingApi.getReferrals` was REMOVED per the class comment ("phantom route... zero UI callers... per PROJECT-DEEP-AUDIT"). Is there truly no remaining UI reference anywhere in `src/admin` that still imports or calls a referrals API, or did the removal miss a caller that now fails silently/breaks at runtime?
83. `AdminMarketingService.getReputation`'s `PlatformReputationScoreDto` — trace what real signals (dispute rate, refund rate, support ticket volume, etc.) actually feed this "reputation score," and confirm whether it's a genuinely computed composite or a single proxy metric being presented to admins as a broader reputation signal than it actually measures.
84. Given `getGrowth`'s conversion rates "coalesce to 0.0 on a zero denominator," could a platform with genuinely zero signups in a period display an identical `0.0%` conversion rate to a platform with signups but zero conversions — i.e., does the UI distinguish "no data" from "0% real rate" anywhere on this metric?

## Support

85. `supportApi.update(id, data: Partial<SupportTicket>)` sends an arbitrary partial object as the PUT body, but the backing endpoint's DTO is `UpdateStatusRequest`. If a caller in `TicketList.tsx`/`useTicketList.ts` ever constructs a `Partial<SupportTicket>` payload with fields OTHER than `status` (e.g., `priority`, `category`), does the backend silently ignore them, or does Jackson/validation reject the whole request — and does the FE ever actually attempt to send those extra fields, expecting them to persist?
86. `AdminSupportService`'s `stats.avgResponseTime` is documented as an honest hardcoded `0` — `SupportTicket` has no first-admin-reply timestamp column. Does `SupportStatsDto`'s consumer (`TicketList.tsx`'s stats header, or a dashboard tile) render this `0` as "0 minutes average response" (implying instant support), and would an admin reading that number reasonably but wrongly conclude support response times are excellent?
87. `AdminSupportController.assign(id, adminId)` — does the backend validate that `adminId` actually refers to an existing, active admin user before assigning, or can a ticket be assigned to an arbitrary/stale/deactivated admin id sent by the client with no server-side existence check?
88. `escalate(id, reason)` on a support ticket — does escalating actually change who can see/act on the ticket (e.g., restrict it to `SUPER_ADMIN`), or does it just set a status/flag while any `SUPPORT`-tier admin (including the one who escalated it, or the very admin whose handling prompted the escalation) can still reply to and resolve it exactly as before?
89. `TicketFilters` includes `assignedTo` — does filtering tickets by `assignedTo` in the admin UI use the currently-signed-in admin's own id as a "my tickets" shortcut anywhere, and if so, does that id come from a fresh `/auth/me` call or a potentially-stale locally cached admin identity?
90. Does `reply(id, content)` on a support ticket ever notify the brand/creator who opened it (email, in-app notification), or does the admin's reply only ever become visible the next time that user happens to check their own ticket view — i.e., is there a push notification path at all, or is this a pure pull model with no code enforcing timely visibility?
91. `getStats()`'s `waitingUser` count — is a ticket ever automatically transitioned out of `WAITING_USER` status back to something an admin needs to act on if the user replies, or does it require the admin to manually notice and re-open it, meaning a user reply could sit invisible in a "waiting on them" bucket the admin has stopped checking?

## Audit Log

92. `AdminAuditLogService.record(...)` is called from money-moving code (`recordManualPayout`, `resolve` dispute, `grantComp`/`overridePlan`) with an outer try/catch that swallows any write failure ("Rule 5, AUDIT-LOG-WRITE-SPEC.md"). If the audit-log write actually fails for one of these financial mutations, is there ANY signal anywhere (a metric, a log line an alert watches, a flag on the entity) that a human would ever see, or does the money move with a permanently silent gap in the compliance trail?
93. `auditApi.list(filters)` supports `adminId`/`entityType`/`action`/`startDate`/`endDate` — does `AuditLogController.list()` apply these as a combined AND filter server-side matching exactly what `AuditLogPage.tsx`'s filter form implies to the admin, and does the page correctly reset pagination to page 1 when a filter changes (or could changing a filter mid-pagination silently request a now-nonexistent page N of a much smaller filtered result)?
94. `auditApi.getByEntity(entityType, entityId)` — for the same underlying mutation (e.g., a brand KYC approval routed through `ApprovalWorkflowService.processApproval`), does the audit trail get recorded under a consistent `entityType`/`entityId` pair regardless of which controller/UI path triggered it, or could looking up a brand's audit history via `getByEntity('BRAND', id)` miss entries that were logged under a different entity type by the approval-queue code path?
95. Given `AdminModerationService.actionFlag`'s audit entries log `field allow-list: id, status, actionTaken, reviewedBy — no contentPreview... to avoid capturing user PII,` if an admin later needs to investigate WHY a piece of content was removed, does the audit log retain enough context (the actual flagged content, or just its id) to reconstruct the decision, or is the audit trail for content moderation effectively "something was removed, no record of what"?
96. Is the `admin_audit_log` table itself ever mutable/deletable through any admin-reachable endpoint (directly or as a side effect of some other action, e.g., deleting the entity it references), and if a referenced entity is later hard-deleted, does the audit log entry survive as an orphaned but still-readable record, or does it break/404 when an admin tries to view it?

## Error Logs

97. `AdminErrorLogService.getStats`'s `critical24h` field counts rows with `ErrorLogSeverity.CRITICAL`, but the class javadoc states the only producer (`GlobalExceptionHandler`'s catch-all) "always writes `ErrorLogSeverity.ERROR`" — meaning no code path can currently ever write a CRITICAL row. Does `ErrorStatsDto`'s consumer in `ErrorLogPage.tsx` present `critical24h: 0` as a genuinely reassuring "zero critical errors" signal, when structurally it can never be anything else regardless of how severe the platform's actual errors are?
98. `AdminErrorLogService.getRecent`/`findAllByOrderByCreatedAtDesc` has no `resolved` filter param even though `resolve(id)` exists and sets `isResolved`. Once an admin marks an error resolved, does it disappear from the "recent errors" list, or does a resolved error keep occupying a slot in the top-N recent list — potentially crowding out a genuinely new unresolved error once the list is full of already-handled ones?
99. `errorApi.resolve(id)` is `SUPER_ADMIN`-only per `AdminErrorLogService`'s class javadoc ("the tightest tier — not ADMIN/SUPPORT"). Does `ErrorLogPage.tsx` hide the resolve button/action for a logged-in `ADMIN`-tier viewer, or does it render the control and let the click fail with a 403, and if it fails, does the UI's optimistic state (if any) roll back the row to unresolved or leave it stuck showing "resolved" client-side despite the server rejecting it?

## Admin Dashboard

100. `AdminDashboardStatsCache` caches CEO-pulse data server-side, and `useAdminSocket`'s `DASHBOARD_PULSE` event is documented client-side as feature-flag-gated OFF by default because "no backend WS endpoint exists yet" (`websocket.ts`'s own BACKEND STATUS note, confirmed by zero `@ServerEndpoint`/STOMP/WebSocket server-side hits in `influora-api`). Given both the cache TTL and the permanently-disabled socket, how stale can the numbers on `PulseDashboard.tsx` actually get before an admin manually refreshes the page, and does anything in the UI indicate to the admin that the dashboard is not, in fact, live?
