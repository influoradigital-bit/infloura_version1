# Brand Base — 100 Questions

## Workspace, Company Profile & Team

1. When a brand edits company profile fields on `brand-settings.tsx` and saves, does every changed field actually reach a PATCH/PUT request body, or does the form silently drop fields the backend `Workspace` entity has no column for?
2. `WorkspaceMemberController.invite` (`POST /workspace/members/invite`) — does the invited email actually receive a message, or does the row get created with no notification/email ever dispatched, leaving the invite undiscoverable?
3. `WorkspaceMemberController.accept` (`POST /workspace/members/accept`) — is the invite token single-use and expiry-checked, or can the same invite link be replayed after acceptance to re-add a removed member?
4. `WorkspaceMemberController.revoke` (`POST /workspace/members/invites/{inviteId}/revoke`) — if a member's session/JWT was already minted before revoke, does any subsequent request from them actually get rejected, or does the stale token keep working until natural expiry?
5. `DeleteMapping("/{memberId}")` in `WorkspaceMemberController` — does removing a member also invalidate their currently-issued refresh tokens, or can a removed member keep refreshing a session indefinitely?
6. `WorkspaceMemberController.switch` (`POST /workspace/members/switch`) — when a user with membership in multiple workspaces switches, does every subsequent brand-scoped call (`BrandContextService.requireBrandWorkspace`) actually resolve against the newly switched workspace, or can stale JWT claims route a request to the previous workspace?
7. `MemberRole` enum (OWNER/ADMIN/MANAGER, per `BillingController` imports) — is there any backend route that lets a non-OWNER member change billing/plan, and does the frontend actually hide that control for non-owners, or is the gate UI-only?

## Onboarding & Verification

8. `brand-onboarding.tsx` posts to `OnboardingController` — does every field the onboarding wizard collects (industry, company size, use-case) get persisted, or are some fields collected in the UI state and never sent in the request body?
9. `brand-verification.tsx` — is there a real backend verification status (`VERIFIED`/`PENDING`/`REJECTED`) that other brand-only actions (fund escrow, invite creators) are gated on, or does the UI show a verification badge that no server check ever consults?
10. `BrandEmailOtpService` — after OTP is verified once, is the OTP code invalidated server-side, or can the same OTP code be resubmitted to re-verify a different email on the same account?
11. Does `OnboardingService`'s "next action" resolution actually match what `brand-onboarding.next-action-live.test.tsx` asserts against the live backend, or does the test only cover a mocked/stale response shape?
12. If a brand abandons onboarding partway and later resumes, does the resume path reconstruct state from the persisted `Workspace`/`OnboardingService` row, or does the wizard restart from step 1 and silently overwrite previously-saved answers?

## Campaign Creation & Lifecycle

13. `CampaignController.create` (`POST /campaigns`, backed by `CampaignWriteRequest`) — for every field `brand-new-campaign.tsx` renders (budget, deliverables, target creator criteria, deadline), is there a corresponding field in `CampaignWriteRequest`, or does at least one UI control's value never leave the browser?
14. `CampaignValidator` — does it enforce the same budget-minimum / deadline-in-future rules the frontend form validates, or can a request crafted to skip client validation create a campaign with a negative/zero budget or a past deadline?
15. `CampaignController.update` (`PATCH /campaigns/{campaignId}`, `CampaignPatchRequest`) — once a campaign has funded escrow or an accepted deal, does the backend actually block a budget-changing patch, or can a brand PATCH the budget after money has already moved against the original figure?
16. `CampaignController.delete` — does deleting a campaign with an active `Collaboration`/deal actually get rejected, or does it succeed and orphan the deal/escrow rows it was attached to?
17. `CampaignController.duplicate` (`POST /campaigns/{campaignId}/duplicate`) — does the duplicated campaign get a genuinely new id and reset status, or does `CampaignService.duplicate` copy over state (e.g. status, escrow references) that should never carry to a new campaign?
18. `brand-edit-campaign.tsx` — for a campaign already in an active/live status, does the backend actually enforce which fields are still editable, or is the "active edit gate" (per `brand-campaign-detail-active-edit-gate.test.tsx`) a frontend-only disable that a direct PATCH call bypasses?
19. `CampaignTemplateService`/`CampaignTemplateController` — when a brand creates a campaign from a template, do all template fields actually populate the new campaign's persisted row, or does the template only pre-fill the form and some fields get lost if the brand never touches that field?
20. `CampaignController.list` supports `status`, `search`, `sortBy`, `sortOrder` — do all `status` values the frontend filter chips offer on `brand-campaigns.tsx` map to real backend status enum values, or does at least one filter option return an empty list against every real campaign because the value string never matches the enum?
21. Is there a campaign status the state machine can enter that the frontend's status-badge switch (`stage-colors.ts` `statusBadgeClass()`) has no case for, so it silently falls through to a default/blank badge?
22. `brand-campaign-tracking.tsx` / `CampaignTrackingController` — is click/conversion tracking data actually written by a real webhook/pixel path, or does the tracking dashboard render numbers computed client-side from data that was never verified against a server-side event?
23. `DeliverableMetricService.getCampaignAnalytics` — the javadoc says analytics are "Always creator-reported ... never platform-verified"; does `brand-campaign-detail.tsx` display that provenance to the brand, or does the UI present creator-self-reported numbers as if they were verified?
24. When a campaign's budget is exhausted (all milestones funded/paid), does any backend transition automatically move the campaign to a "completed"/"budget exhausted" status, or does it stay "active" forever with no server-side signal that spend is done?

## Creator Discovery & Shortlisting

25. `CreatorController.search` (`GET /creators/search`) — do all the filter parameters `brand-discover.tsx` sends (category, follower range, engagement rate, location) actually get applied server-side, or does at least one filter get accepted and silently ignored by `CreatorDiscoveryService`?
26. `CreatorController.save` (`POST /creators/{creatorId}/save`) — is the "saved"/shortlist state actually persisted per-workspace, or is it saved per-user such that a teammate on the same workspace doesn't see creators another teammate shortlisted?
27. `CreatorController.suggestions` (`POST /creators/suggestions`) — does this endpoint receive the campaign's actual targeting criteria as input, or is it called with an empty/generic body that makes "suggested creators" identical for every campaign?
28. `CreatorDiscoveryFacetsCache` — when a creator's profile data changes (follower count, category), does the facet cache actually invalidate, or can discovery filters keep surfacing stale creators for counts/categories that no longer match?
29. `CreatorController.similar` (`GET /creators/{username}/similar`) — is the similarity computed from real profile/engagement data, or is it a fixed/random fallback list when the underlying scoring service has no data for that creator?
30. `brand-creator-profile.tsx` — does every stat shown (followers, engagement rate, past campaign count) come from a field the backend `CreatorProfileService`/`CreatorAnalyticsService` actually returns, or is at least one number fabricated/hardcoded client-side when the API response has no such field?
31. `CreatorController.featured` (`GET /creators/featured`) — is "featured" driven by a real curation/ranking flag in the database, or does it return the same static/mock list regardless of the requesting brand's industry or campaign context?
32. When a brand's plan has a tracked-creator limit (`Plan.getTrackedCreatorLimit`), is that limit checked anywhere before a new creator is saved/shortlisted, or does the number displayed on `brand-billing-settings.tsx`'s usage meter never actually block the action that increments it?

## Invitations, Offers & Deal Room

33. `CreatorController.invite` (`POST /creators/{creatorId}/invite`, `InviteRequest`) — does every field the invite modal collects (proposed rate, message, deliverables) reach `CreatorDiscoveryService.invite`, or is the invite persisted with fields silently dropped?
34. Once a brand sends an invite via `CreatorController.invite`, does the creator actually receive a notification/inbox entry, or does the invite row get created with no delivery path to the creator side at all?
35. `DealController.create` (`POST /deals`, `CreateDealRequest`) — is a deal proposal's price/deliverable list re-validated against the campaign's persisted budget server-side, or can a brand (or a compromised client) propose a deal whose total exceeds the campaign budget with no rejection?
36. `DealController.accept` and `DealController.reject` both take an optional `Idempotency-Key` header — if the header is omitted, can a double-click/double-submit on the accept button in `brand-deals.tsx`/`brand-pipeline.tsx` accept the same deal twice or trigger two downstream effects (e.g. two escrow-fund prompts)?
37. `DealController.counter` — after a brand sends a counter-offer, does the deal's status actually transition to a state that blocks the creator from also accepting the pre-counter terms, or is there a race where both the original offer and the counter can be accepted?
38. `DealService.reject` accepts an optional `RejectRequest` body — is the rejection reason actually persisted and shown back to the creator, or does the UI collect a reason that never reaches the stored deal record?
39. The deal message SSE stream (`GET /deals/{dealId}/messages/stream`) replays missed messages via `Last-Event-ID` — if the browser tab is closed and reopened without the `EventSource`/fetch-SSE ever setting `Last-Event-ID`, do the messages sent during the gap actually get fetched through `GET /deals/{dealId}/messages`, or are they lost from the visible thread until a manual refresh?
40. `DealController.sendMessage` — is there any character/attachment-size limit enforced server-side matching what `brand-messages.tsx`'s composer enforces client-side, or can a request bypass the UI limit entirely?
41. `DealController.markRead` — does marking a deal's messages read actually update the unread badge count returned elsewhere (e.g. `brand-dashboard.tsx`'s summary, `brand-notifications.tsx`), or do those two read states drift out of sync?
42. `DealController.listDeliverables` (`GET /deals/{dealId}/deliverables`) — per its own comment referencing a prior "persistence gap," is deliverable data now actually written by the creator-submission path, or can this endpoint still return an empty list for a deal that visibly has submitted deliverables in the UI?
43. `brand-campaign-detail.contract-wiring.test.ts` / `brand-campaign-detail.contract-ui.test.tsx` mock `campaignsGet`, `dealsList`, `contractsGenerate` — does the real `brand-campaign-detail.tsx` wire the "Generate Contract" button to `contractsGenerate` with the actual deal's real id, or does the test's mock hide a case where the live button sends a stale/empty deal id?
44. Is there a deal status (e.g. `EXPIRED`, `WITHDRAWN`) reachable in `DealDtos`/the backend state machine that no button or code path in `brand-deals.tsx`/`brand-pipeline.tsx` can ever produce, meaning that status is dead on arrival from the brand side?

## Contracts & E-Signature

45. `ContractController.generate` (`POST /contracts`, `ContractGenerateRequest`) — does contract generation pull the deal's actual agreed price/deliverables at generation time, or can it be called with terms that no longer match the current state of the deal (e.g. after a counter-offer)?
46. Per the `ContractController.sign` comment describing the removal of the brand-signs-for-creator relay: is there any remaining code path (Meera tool, admin endpoint, or another controller) that lets a brand-authenticated principal record a signature attributed to the creator, or is the creator-only signing path now the sole route to `ACTIVE`?
47. `ContractSignRequest.name()` (the typed legal signer name, per the `[F-0292]` comment) — is this name actually rendered into the generated PDF via `ContractPdfService`, or does the PDF still show a placeholder/blank signer name despite the UI collecting it?
48. `ContractController.pdfDownloadUrl` 404s with `CONTRACT_PDF_NOT_READY` until both parties sign — does `brand-contracts.tsx` handle that 404 by showing a clear "awaiting signature" state, or does it show a broken/blank download link or an unhandled error?
49. `ContractController.list` filters by `dealId` — if a brand queries contracts for a `dealId` belonging to a different workspace's deal, does `ContractService.listForBrand` actually scope by workspace ownership, or does it return contracts across workspaces because the filter only matches on `dealId`?
50. Once a contract reaches `ACTIVE` (fully signed), does the backend prevent `ContractController.generate` from being called again for the same deal to produce a second, conflicting contract, or can duplicate active contracts exist for one deal?
51. Does the PDF a brand downloads via `pdfDownloadUrl` actually match the terms shown in `brand-contracts.tsx`'s on-screen summary at generation time, or can the two diverge if the campaign/deal was edited after PDF generation but before the presigned link was minted?
52. `brand-campaign-detail.contract-ui.test.tsx`'s `openActiveTab()`/`toastMock` — does the real UI show a success toast for contract generation only after the backend call actually resolves with a created contract, or can the toast fire optimistically before confirming the POST succeeded, hiding a real failure?

## Escrow Funding & Release

53. `EscrowController.fund` (`POST /wallet/escrow/fund`) — the javadoc states `EscrowFundRequest` carries no amount field and the amount is always re-derived server-side; does `brand-wallet.tsx`'s fund-confirmation modal display the amount from the same `deriveFundAmount` source, or does it show a client-computed estimate that could differ from what actually gets debited?
54. `EscrowController.fund` requires an `Idempotency-Key` header — does `brand-wallet.tsx`'s fund button actually generate and send a fresh key per click (and reuse it correctly on retry), or is the key omitted/regenerated per retry in a way that lets a network retry double-fund an escrow hold?
55. `EscrowController.release` requires exactly one of `milestoneId` or `escrowHoldId` — does `brand-campaign-detail.tsx`'s release action reliably know which of the two applies for a Meera-originated (milestone-less) campaign-level hold versus a milestone-gated hold, or can it send both/neither and get a 400 the UI doesn't surface clearly?
56. `EscrowController.refund` is documented as having "DELIBERATELY NO FRONTEND CALLER" — is that actually true across `api.ts` and `meera-api.ts`, or does some brand-facing button silently call `/wallet/escrow/refund` in violation of the documented policy (funds should only move via release or dispute resolution)?
57. `EscrowController.list` (`GET /wallet/escrow`) backs the wallet page's escrow-items panel, replacing prior mock data per its comment — does every field the panel renders (status, milestone name, amount) actually come from `EscrowStatusResponse`, or does the panel still fabricate/derive a field client-side that the backend response doesn't carry?
58. When `EscrowService.release` succeeds, does the corresponding `CampaignServiceInvoiceService`/GST invoice generation happen synchronously in the same transaction, or (per the "escrow invoice backfill gap" pattern) can a release succeed while its invoice silently fails to generate, with only a log line and no visible marker on `brand-wallet.tsx`?
59. `PayoutService.queuePayout` (`POST /wallet/escrow/payout`) — once a payout is queued, does `brand-wallet.tsx` or `brand-campaign-detail.tsx` show its real settlement status (queued/processing/paid/failed), or does the UI just show "paid" the moment the queue call succeeds regardless of whether the payout actually settles?
60. Given Razorpay Route requires per-payment transfers and creator-linked KYC accounts while `EscrowService` implements a pooled wallet ledger — does the money actually leave the platform's pooled account to the specific creator's bank account on release, or does "release" only update a ledger row inside Influora's own database with no real transfer executed?
61. Is TDS (tax deducted at source) actually computed and withheld anywhere in the escrow-release or payout path, or does the release amount always equal the full milestone amount with no tax line ever subtracted?
62. `EscrowController.status`/`EscrowService.getStatus` — does the brand-visible escrow status ever diverge from the actual payment-gateway-side state (e.g. showing `FUNDED` when the Razorpay-side charge is still pending/failed), and is there a reconciliation job (`PayoutReconciliationService`) that a brand's escrow view actually reflects, or is that reconciliation invisible to the frontend?
63. `AmountDerivationService` — for a campaign with multiple milestones, does the derivation correctly sum only the unfunded milestone(s) requested, or can a second `fund` call for a different milestone on the same campaign re-derive (and re-charge) an amount that double-counts an already-funded milestone?

## Deliverable Review & Approval

64. `BrandDeliverableController.approve` — after approval, does escrow release for that deliverable's milestone happen automatically, or does approval just flip a status flag while the money stays unreleased until a separate manual action the brand isn't told is required?
65. `BrandDeliverableController.revise` (`ReviseRequest`) — is the revision feedback text actually delivered to the creator's deliverable view, or does the request succeed (200) while the creator-side UI shows no indication a revision was requested?
66. `BrandDeliverableController.reject` — per its "B6 — the missing route" comment, does `brand-campaign-detail.tsx` or wherever deliverables are reviewed actually call this route today, or is there still no wired frontend button, leaving the fixed backend route with zero live callers from the brand side?
67. Can a deliverable be approved twice (double-click, or approve after an already-approved state) — does `BrandDeliverableService.approve` reject a second approval attempt, or does it silently re-run whatever side effects approval triggers?
68. `DeliverableSafetyReviewService.getReview` — the javadoc says the safety verdict is "advisory-only ... never blocks approve/reject/revise"; does `brand-campaign-detail.tsx` make that non-blocking nature clear to the brand, or does it present the safety-review badge in a way that implies it gates approval when it doesn't?
69. When a brand requests a revision, is there a limit on revision rounds enforced server-side matching whatever the campaign/contract terms specify, or can revisions be requested indefinitely with no server-side cap even if the UI implies a limited number of rounds?
70. `BrandDeliverableController.getDetail` — does the response include the actual submitted media/asset URLs the creator uploaded, or can a deliverable exist with a submission record but no retrievable asset link, leaving the brand unable to actually review the content?
71. If a brand rejects a deliverable, does the underlying `Collaboration`/deal status change to reflect that (blocking auto-progression to payment), or can a rejected deliverable's milestone still be released via `EscrowController.release` because the two systems aren't actually linked?

## Shipment (Product-Seeding)

72. `DealController.markShipped` publishes `ShipmentCreatedEvent` — does anything actually consume that event to notify the creator, or is it published into a void with no registered listener, leaving `submitShippingAddress` the creator's only way to discover shipment status (by polling)?
73. `DealController.getShipment` returns a synthetic `AWAITING_ADDRESS` response when no row exists — does `brand-campaign-detail.tsx` distinguish that synthetic state from a real persisted status, or could it show a real tracking number field for a shipment that was never actually created?
74. `ShipmentService.confirmReceipt` — does creator confirmation of receipt trigger any downstream state change visible to the brand (e.g. unblocking deliverable submission or content posting), or does the confirmation just sit in the `Shipment` row with no consumer?
75. Is there a brand-side control to mark a shipment as lost/undelivered, or does the shipment flow only ever move forward (address → shipped → received) with no path for the brand to flag or recover from a failed delivery?

## Disputes

76. `BrandDisputeController.listDisplay` (`GET /brand/disputes/list`) is documented read-only, with opening/resolving staying on `DealController`/`AdminDisputeController` — does `brand-disputes.tsx` actually expose any action button (respond, add evidence, resolve) that calls a real endpoint, or are all such controls either missing their handler or wired to a route that doesn't exist for brand principals?
77. `DealController.openDispute` (`POST /deals/{dealId}/disputes`) — once a dispute is opened, does `EscrowService` actually freeze the associated hold from release (per the "dispute-freeze guard" mentioned in `EscrowController.refund`'s javadoc), or can a brand still call `/wallet/escrow/release` on a disputed milestone while the dispute is open?
78. `DisputeLifecycleStatus` (`src/lib/api.ts`) — does every status value in that type correspond to a status the backend `DisputeService` can actually set, or does the frontend render a status label for a value the backend state machine never produces?
79. When a dispute resolves through `DisputeService.resolve` → `EscrowService.adminRefundForDispute`, does the brand's wallet/transaction history (`GET /wallet/transactions`) actually show the resulting refund as a distinct line item, or does the refunded amount disappear from the brand's visible ledger?
80. Is there a time limit on when a brand can open a dispute relative to deliverable approval/escrow release, and if the backend enforces one, does `brand-campaign-detail.tsx`/`brand-deals.tsx` show the brand that window before they attempt the action, or do they only discover it via a rejected request?
81. `DisputeCard()`/`statusBadgeClass()` in `brand-disputes.tsx` — for a dispute in a terminal state (resolved in the creator's favor vs. the brand's favor), does the UI actually differentiate the outcome, or does it show the same generic "resolved" badge regardless of which party the resolution favored?

## Invoicing & GST Documents

82. `CampaignServiceInvoiceService`/`CampaignServiceInvoicePdfService` — is the GST split (`GstSplitUtil`) computed from the brand workspace's actual registered GSTIN/state, or does it default to a fixed state/rate regardless of the brand's real registered address?
83. `HsnSacCodeService` — does every invoice line item actually get a real HSN/SAC code attached, or can an invoice be generated (and be GST-non-compliant) with a missing/placeholder code that the PDF renders blank?
84. `InvoiceNumberService` — are invoice numbers guaranteed sequential/gapless per the numbering scheme GST filing requires, or can a failed/rolled-back invoice generation attempt consume a number that then never appears on any actual invoice, leaving a silent gap?
85. `BillingController.getInvoicePdf` — is the PDF byte stream actually regenerated fresh or served from a persisted, immutable copy, and if regenerated on each request, can two downloads of the "same" invoice ever differ if plan/workspace data changed in between?
86. `CommissionInvoiceService`/`CommissionInvoicePdfService` — does the brand ever see the platform's commission/fee invoice distinct from the creator-service invoice, or are the two conflated in `brand-billing-settings.tsx`'s invoice list such that the brand can't tell what they're being billed for?
87. Given the known escrow-invoice backfill gap (a release can complete while its GST invoice silently fails, logged only via `log.error` with no retry/marker) — does `BillingController.getInvoices` ever surface a "missing invoice" state to the brand, or would a brand with a real gap simply see one fewer invoice than releases, with nothing in the UI hinting a document is missing?
88. Does the invoice PDF's line-item amount ever get cross-checked against the actual `WalletTransactionRowResponse` ledger entry for the same event, or could the two values diverge (e.g. after a partial refund) with no reconciliation check anywhere in the code?

## Billing & Plan Limits

89. `BillingController.getUsage` computes `trackedCreatorsUsed` via `UsageCounterService` against `UsageMetric.TRACKED_CREATOR` — earlier discovery/shortlist questions aside, is `UsageCounterService.increment`(or equivalent) actually called anywhere in `CreatorController.save`/`CreatorDiscoveryService`, or does the counter stay at zero forever regardless of how many creators the brand tracks?
90. `Plan.isExportEnabled()` gates the `exportsUsed`/export limit shown in `UsageSummaryResponse` — is there an actual export endpoint (`ReportExportController`) that checks `isExportEnabled()` before generating a report, or can a brand on a plan with exports disabled still successfully call export and download data?
91. `activeSeatsUsed` (via `WorkspaceMemberService.getActiveSeatCount`) — is there an enforced seat cap tied to the plan when `WorkspaceMemberController.invite` is called, or does `getUsage` just display a seat count with no corresponding check that blocks inviting past the plan's seat limit?
92. `aiCreditsRemaining`/`BrandAiCredit` — is the credit actually decremented by whatever AI feature (Meera chat, campaign-brief generation) consumes it, or does the counter shown on `brand-billing-settings.tsx` stay static while AI features remain fully usable regardless of remaining credits?
93. `SubscriptionService.initiateCheckout` — can `ALREADY_SUBSCRIBED` actually be triggered by clicking "Upgrade" twice in quick succession on `brand-billing-settings.tsx` before the first checkout redirect completes, and if so, does the UI surface that error or show a generic failure?
94. `SubscriptionService.cancel` — after cancellation, does the workspace's plan actually downgrade to Free at period end (with `getPlan`'s lazy Free-provisioning), or does the brand keep paid-plan access (e.g. above-Free tracked-creator limits) indefinitely because nothing re-checks the plan after the subscription period lapses?

## Brand Analytics

95. `brand-analytics.tsx`/`AnalyticsController` (`/analytics/creators/{creatorId}/metrics`, `/scores`, `/demographics`) — do these endpoints require the requesting brand to have an actual active/past collaboration with that creator (`MetricsAuthorizationService`), or can any authenticated brand pull another brand's contracted creator's private metrics by id?
96. `brand-creator-analytics.tsx` — are the displayed engagement/reach numbers computed from the same creator-reported data flagged elsewhere as unverified, and if so, does this page carry the same "creator-reported, not platform-verified" disclosure `CampaignController.analytics`'s javadoc requires, or does it present the numbers without that caveat?
97. `DeliverableMetricController`/`DeliverableMetricService` — when a creator hasn't yet reported metrics for a deliverable, does `brand-campaign-detail.tsx`'s analytics panel show an explicit "not yet reported" state, or does it render a zero/blank value indistinguishable from "reported zero engagement"?

## Notifications

98. Per `NotificationController.list`'s own javadoc, `useNotifications.ts` (wired into `NotificationBell.tsx`) reads the response body unwrapped while the endpoint now returns it inside an `ApiResponse` envelope — does the live brand notification bell actually render new notifications and the correct unread count, or does it silently break (empty/undefined data) because the hook still expects the pre-envelope shape?
99. `NotificationController`'s `setPreference`/`PreferencesResponse` — does toggling a notification-type preference off on `brand-notifications.tsx` actually stop `EmailPreference`-gated emails for that event type from being sent, or does the email-sending path (wherever it triggers per event) skip checking `EmailPreferenceRepository` for at least one brand-relevant event type?

## Brand-Side Meera

100. `CreateCampaignExecutor`, `ConfirmLaunchExecutor`, and `RequestPaymentExecutor` (Meera tools) run against an `OnBehalfTokenService`-minted token — does that token actually carry write scope for every action these executors perform (campaign create, launch confirm, payment/escrow request), or can a subset of the tool chain 403 mid-conversation because the minted token's scope doesn't cover every tool a brand-facing Meera session is offered?
