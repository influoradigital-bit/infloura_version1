confirm object: verb=FIX dispatch=produce scope=1 oracles=0
   admissible
NOT CHECKED: whether the done_when, once it passes, means the work is actually right; whether the named oracle was ever run or its recorded output is genuine; whether `evidence.command` is the command that actually ran, whether `evidence.result` is that command's real output, and whether the LINE cited by `evidence.where` exists or supports the claim — only that the location can be opened at all and, where it is an rc record, that the record does not contradict the verdict (whether that record names a pid and an observer is gates/liveness.py's question, not this gate's); whether the `isolation` this object carries is the isolation the checker actually had — that is the dispatcher's observation and this gate reads only what reached the object; and whether the declared blind spot is the blind spot that matters — only the shape, the provenance, the cited location, the isolation ceiling and the testability of the object in front of it
