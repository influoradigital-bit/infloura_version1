{
 "verdict": "proved",
 "oracle": "script",
 "isolation": "oracle",
 "done_when": "a webhook test posting an order whose coupon code is not a valid Influora coupon asserts HTTP 200, and it fails against the pre-fix controller",
 "not_checked": [
  "whether a real Shopify or WooCommerce delivery now behaves this way in production — no store is connected in any environment and no live webhook was fired; the whole proof is unit-level",
  "the 15 Testcontainers tests, which did not execute on this host (no Docker), so no database-backed regression could have been caught by this run",
  "whether returning 200 for INVALID_CODE loses information a brand needs — the outcome is now only a log line, with no metric, marker or surfaced count of unattributed orders",
  "ConversionWebhookController, deliberately left returning 4xx; if a brand's own server retries, the same storm shape is possible there and this gate does not cover it",
  "the other three F-0725-adjacent defects found in the same audit (F-0726 unsigned tenant selector, F-0727 unverified site claim, F-0728 cross-workspace code shadowing) — all still open"
 ],
 "skipped": [
  {
   "step": "gates/e2e.sh",
   "reason": "no store is connected in any environment, so an end-to-end store journey has nothing to exercise"
  },
  {
   "step": "gates/frontend.sh",
   "reason": "this fix touches only Java; the frontend gate's findings were already recorded in T-STORECHK-0907 and are unrelated to it"
  }
 ],
 "evidence": {
  "oracle": "script",
  "command": "bash .proof-os/gates/store-webhook-terminal-outcome-acked.sh",
  "result": "exit 0 - PROVED: terminal redemption outcomes are acknowledged 200 and transient ones still surface non-2xx, on both store webhooks",
  "where": ".proof-os/gates/store-webhook-terminal-outcome-acked.sh:80"
 }
}