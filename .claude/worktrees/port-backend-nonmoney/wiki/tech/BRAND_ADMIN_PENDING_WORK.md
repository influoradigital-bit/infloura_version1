# BRAND & ADMIN — Pending Work Tracker (Live-Wiring Gap Closure)

> **Compiled by:** Priya (CTO), from the code-only audit in `MASTER-BUSINESS-PLAN.md` (2026-07-10),
> cross-verified directly against source (`src/App.tsx`, `src/hooks/useMeeraStream.ts`, `influora-ai/.env`).
> **Sequencing: BRAND first (all items must be `[x]`) before starting ADMIN.**
> **Loop cadence:** Arjun runs this tracker every cycle; Tara refreshes `MASTER-BUSINESS-PLAN.md`
> scorecard (§3.1 / §3.3 / §7) every 10 minutes to reflect newly-closed items.

---

## Loop Protocol

1. **Arjun** reads this file each cycle. Finds the highest-priority `[ ]` item in the **active section**
   (BRAND until every item is `[x]`, then ADMIN).
2. Arjun assigns it to the **Owner** agent(s) listed on that line.
3. Owner implements/reviews. Pipeline: **Vikram/Ananya build → Kavya QA → Kabir security (if flagged) → Meera build/local verify.**
4. On full PASS, Arjun marks the line `[x]` with date + one-line evidence (test count, build output, file:line fixed).
5. **Tara** updates `MASTER-BUSINESS-PLAN.md`'s scorecard tables to match reality — never bump a % without a closed item here as evidence.
6. Repeat from step 1.

**Status legend:** `[ ]` not started · `[~]` in progress · `[x]` done (evidence noted inline)

---

## PART 1 — BRAND (58% built / 35% live) — WORK THIS SECTION FIRST

### P0 — Ship blockers / silent-failure mutations (fix before anything else)
- [x] **Fix campaign edit** — `campaign-form.tsx:239` resolves a single hardcoded ID; editing any real campaign fails. Owner: **Ananya**. QA: Kavya. Verify: Meera. **DONE 2026-07-10.** Replaced hardcoded `mockCampaign` lookup with real `api.campaigns.get(campaignId)` fetch + loading/error states + budget-integrity guard (no more silent stale-data fallback). Kavya: 2 rounds, final PASS (`wiki/errors/campaign-form-edit-bugfix-RE-REVIEW.md`). Meera: build PASS (4601 modules, 1m4s), typecheck clean for this file, 173/174 repo tests passing (1 unrelated failure). Gap noted: no dedicated test coverage for this component yet.
- [x] **Stop silent no-op contract mutations** — approve/revise UI actions are no-ops (`:356`); user believes the action succeeded when nothing persisted. Wire to the real `ContractController` endpoints (backend already exists per creator-side pattern). Owner: **Vikram** (confirm endpoint contract) + **Ananya** (wire UI). Security: **Kabir** (IDOR / workspace-isolation check on the new wiring — same pattern already closed on the creator side). QA: Kavya. Verify: Meera. **DONE 2026-07-10** (unblocked by P1 "Contracts (40% → live)" below). Approve/revise call the real, workspace-scoped `api.deliverables.approve/requestRevision` with loading state, error Alert, optimistic update — Kabir PASS WITH FINDINGS (1 non-blocking LOW). In live mode, this page's Deliverables tab intentionally does not surface the dialog (no deliverables-list endpoint exists to populate it — see new backlog item below); the actual live approve/revise path is the already-audited Deal Room. No remaining silent-success case in either mode. Meera: build PASS (4601 modules), `ContractServiceTest` 18/18.
- [x] **Fix `liveApi` fallback masking outages** — on fetch failure the UI shows an empty list instead of an error state (`creator-discovery.tsx:588`; same pattern likely reused in brand discover). Owner: **Ananya**. QA: Kavya. **DONE 2026-07-10.** Note: the file at `src/components/brand/discover/creator-discovery.tsx` — the exact cited spot (main `fetchCreators`, ~line 588) was already fixed in prior uncommitted work and correctly shows an `apiError` banner. Found and fixed 2 *other* silent-catch spots in the same file that matched the same anti-pattern: (1) Featured Creators section — was silently clearing to `[]` on fetch failure, now sets `featuredError` + shows a dashed-border notice with Retry; (2) invite-dialog campaign dropdown — same silent-clear bug, same fix pattern (error state + Retry). Kavya: full PASS, confirmed no other silent `.catch()` patterns remain in the file, demo mode unaffected, retry wiring correct (`wiki/errors/creator-discovery-p0-3-review.md`). Meera: build PASS (4601 modules, 55.16s), typecheck clean for this file, 173/174 repo tests (3 failures pre-existing/unrelated). Gap noted: no dedicated test file exists for this component.

### P1 — Wire mock-backed features to real endpoints
- [x] **Contracts (40% → live)** — replace `mockContracts` with real `api.contracts.*` calls end-to-end. Owner: **Vikram** + **Ananya**. Security: Kabir. QA: Kavya. Verify: Meera. **DONE 2026-07-10.** Backend: new `GET /contracts` brand branch (`ContractService.listForBrand` + `ContractController`), workspace-scoped via `brandContext.requireMember`, 18/18 tests incl. cross-workspace rejection. Frontend: `contracts-and-deliverables.tsx` split into `ContractsAndDeliverablesDemo` (untouched) and `ContractsAndDeliverablesLive` (new) — sidebar via `api.deals.list('brand')`, detail via `api.contracts.get`, Payments tab renders real `milestones[]` (not hardcoded to 2), sign wired to `api.contracts.sign`, Contract tab shows real facts (no fabricated clauses), Deliverables tab shows honest gap state + Deal Room link. **CORRECTION (2026-07-11, from Deal Room cycle):** the original PDF-via-`api.contracts.pdfDownloadUrl` claim was wrong — that endpoint doesn't exist on the real backend at all; fixed to show an honest "delivered by email once both parties sign" gap message instead of a misleading "not ready yet" error. Kabir PASS, Kavya PASS, Meera build PASS (4601 modules, 21.19s). **New backlog item surfaced** (not yet tracked as a fix, just noted): no `GET /deals/{id}/deliverables` backend endpoint exists — `DealResponse` already has `deliverablesDone`/`deliverablesTotal` counts but no per-deliverable list; needed to fully populate the Deliverables tab. Also noted (unrelated, routed separately): `CreatorDeliverableServiceTest`/`PortfolioServiceTest` currently fail to compile against a new `MalwareScanService` constructor (Kabir K6-3 finding, already routed to Vikram in the Creator workstream) — blocks a clean full-module `mvn test`, does not affect this item's isolated test run.
- [x] **Deals (40% → live)** — replace `mockDealRooms`; wire counter-offer to the real `DealController.counter` endpoint (already built and idempotent on the creator side — reuse the pattern). Owner: **Ananya** + **Vikram**. QA: Kavya. **DONE 2026-07-10** (Kabir looped in beyond the original scope — money-adjacent). `brand-chat.tsx` (the real routed page; `deal-room-dashboard.tsx`/`brand-deals.tsx` confirmed dead/unrouted, untouched) — deal list now live via `api.deals.list('brand')` with loading/error/retry; new `brand-deal-counter-modal.tsx` wired to `api.deals.counter(id, payload, 'brand')`, confirmed dual-role-safe on the backend. Kabir: PASS WITH FINDINGS (1 non-blocking LOW — `deals.counter` doesn't send a client `Idempotency-Key` header unlike other money-adjacent calls; server's deterministic fallback key still dedupes safely). Kavya PASS. Meera build PASS (4602 modules, 21.73s), typecheck clean for both files. **Scope note — Accept intentionally NOT wired:** investigation (Vikram, independently confirmed by Kabir) found `DealService.accept()`/`reject()` are hard-gated creator-only (`creatorContext.requireCreator`) with hardcoded creator-perspective data even past that gate — wiring the frontend today would just 403. Left as an honest in-UI gap notice instead of a dead button. **New backlog item:** brand-initiated accept/reject needs real backend work (role-aware branch mirroring `counter()`, fix hardcoded response data in `doAccept()`, and decide whether a "can't accept your own last offer" guard is missing) — not yet added as its own tracker line.
- [x] **Campaigns (55% → live)** — wire the rich existing UI off mock data onto `CampaignController`. Owner: **Ananya** + **Vikram**. QA: Kavya. **DONE 2026-07-10.** Create/edit submit path (`campaign-form.tsx`) was already live from earlier work. Fixed the remaining gap: `campaigns-list.tsx` had zero `isApiLive()` gating at all — now fetches via `api.campaigns.list({status,search})`, debounced, with loading skeleton + error Alert/Retry (no silent-empty-on-failure). **Real finding:** backend `CampaignMapper.toResponse` always passes `CampaignMetrics.empty()`, so `collaboratorsCount` is always 0 from the API today, not a real count — Ananya correctly did not trust/fabricate it; live mode shows an honest `—/max` with a tooltip instead, and omits the progress bar (no real source) rather than faking one. Demo mode untouched. Kavya PASS (7/7 independent checks), Meera build PASS (4602 modules, 12.57s), typecheck clean for this file. Gap noted: no test file exists for this component (frontend) — only backend `CampaignServiceTest`.
- [x] **Onboarding (65% → live)** — 1,564-LOC multi-step flow; final submit wiring is thin, complete it. Owner: **Ananya**. QA: Kavya. **DONE 2026-07-10.** Tracker description was stale (same pattern as 2 earlier items) — the 3-step `brand-onboarding.tsx` flow was already fully live-wired (`api.auth.brandRegister`, OTP send/verify/resend, `api.onboarding.saveBrandCompany`, `api.onboarding.completeBrand`), confirmed genuinely correct end-to-end. Actual work: removed ~560 lines of dead code (4 unused legacy step components from an old 6-step design) from `onboarding-steps.tsx` (1586→1038 lines); corrected stale header comments (KYC/wallet-funding deferral claims that didn't match reality); small fixes to `brand-onboarding.tsx` (hasBrandToken skip-to-step-2, doc comment, one color-token fix). Kavya PASS WITH ADVISORY, also added A-GA-3 OTP error handling + `useReducedMotion()` accessibility during her review. Meera build PASS (4602 modules, 20.01s), typecheck clean across all 3 files. Gap noted: zero test coverage exists for onboarding. **New backlog item added below: KYC collection at first campaign creation.**
- [x] **Deal Room (60% → live)** — 11 prop-driven components need real persistence instead of external/local-only state. Owner: **Ananya** + **Vikram**. **SHIPPED/CONDITIONAL 2026-07-11.** All work committed & build-verified (5 commits): backend `GET /deals/{dealId}/deliverables` + brand message coverage + Kabir M-1 kind-spoof fix (`9761f71`, Kabir PASS 0C/0H); FE live **messages** (`e274fe1`) + live **deliverables** list/approve/revise (`ccbff0f`) in `brand-chat.tsx` — `isApiLive()`-gated, loading/error/retry, no silent failure, demo mode untouched. Kavya QA **PASS WITH FINDINGS** (`brand-deal-room-B1-kavya-qa-final.md`); all findings fixed (`50b37ea` — M-1 dealId snapshot, M-2 runtime array guard, LOW-3 status mapping), `npm run build` PASS + tsc clean. **CONDITIONS (pre-prod):** (a) browser runtime E2E needs the Spring/MySQL stack (not runnable this session) — **Meera to close**; (b) shipment persistence has no backend — honest local-only gap, **Priya product decision** (P2 backlog). **History:** original FE work was destroyed by a mid-session `git stash` (root-cause guardrails: `f86f90c`); a concurrent writer then recorded a FALSE `[x]` whose code was verified absent — corrected in `9195f66`. **Separate follow-up (reported, unverified):** Contracts `pdfDownloadUrl`/bulk-list may have no backing endpoint — verify + correct the earlier Contracts P1 item if confirmed.
- [~] **Timeline (55% → live)** — polished/presentational only, needs a real data layer behind it. Owner: **Ananya** + **Vikram**. **PARTIAL 2026-07-11.** `src/components/brand/timeline/collaboration-timeline.tsx` (committed `f86f90c`) is genuinely live-wired for **messages + proposals** via `api.messages.list/send/markRead`, `isApiLive()`-gated with error/loading/retry + a clean sessionStorage/`buildDefaultEvents` demo fallback. Kavya QA **PASS on all technical checks** (security, gating, error handling, TS, a11y; 4 LOW advisories) — `wiki/errors/brand-timeline-B2-kavya-qa.md`. **BLOCKER to full close (Kavya FAIL on "real data layer"):** the backend only persists **3 of 7** timeline event kinds (`text`/`proposal`/`system`); **contract/deliverable/payment/shipment events have no activity-log backend**, so the timeline cannot show a full real feed. Not a frontend bug — the FE can't fabricate events the backend never writes. **Escalated to Priya** (ship PARTIAL vs. build a unified activity-log for all 7 kinds). New backend backlog item below.

### P2 — Dead / deferred features
- [ ] **Settings / Store Integration** — frontend hook is correct but backend returns `NOT_IMPLEMENTED`; either build the backend or remove the dead UI entry point (Arjun to decide scope with Priya). Owner: **Vikram**.
- [ ] **Brand-initiated deal accept/reject (new, surfaced 2026-07-10)** — `DealService.accept()`/`reject()` are hard-gated `creatorContext.requireCreator(principal)`; even past that gate `doAccept()` hardcodes creator-perspective response data. Needs a role-aware branch mirroring the already-dual-role `counter()`, a fix to `doAccept()`'s hardcoded data, and a product decision on whether a "can't accept your own last offer" guard is missing. Until this lands, brand users can only counter, never accept, a creator's proposal from the UI. Owner: **Vikram**. Security: **Kabir** (same money-adjacent class as counter). QA: Kavya.
- [~] **KYC collection at first campaign creation (new, surfaced 2026-07-10)** — the onboarding flow's header comment claims GSTIN/PAN KYC is "deferred, collected just-in-time at `/brand/campaigns/new`" — was verified false (zero UI call sites). **FRONTEND DONE 2026-07-11 (`7a1e644`).** CTO ruling: **KYC OPTIONAL** — so a soft, dismissible `BrandKycPrompt` card now sits on the campaign-creation entry page (`brand-new-campaign.tsx`): collects GSTIN/PAN + docs, uploads via `api.uploads.upload`, submits via `api.onboarding.submitBrandKyc`; **never blocks** campaign creation; dismissal remembered in localStorage; demo mode simulates submit. `npm run build` PASS + tsc clean. **Remaining (backend, Maven-gated verification):** confirm `POST /onboarding/brand/kyc` compiles/works in a Java env; Kabir compliance review; Kavya QA. Follow-up: auto-hide the prompt once a brand-KYC *status read* endpoint exists (none today). Owner: **Ananya** (FE ✅) + **Vikram** (BE verify). Security: **Kabir**. QA: Kavya.
- [ ] **Contract PDF delivery (new, surfaced 2026-07-11)** — no backend endpoint exists for PDF download or generation at all (`ContractController` only has `generate`/`get`/`sign`); the approved design (per a stashed `wiki/tech/approved-deps.md` entry) was delivery via an MSG91 email link, not a client-fetched URL. Both Contracts and Deal Room currently show an honest "you'll receive a copy by email" gap message in place of a real download. Needs a product/architecture decision (build the email-delivery flow vs. a real download endpoint) before implementation. Owner: **Vikram** (decide with Priya) + **Ananya** (wire whichever UI results). Security: Kabir (contract documents are sensitive). QA: Kavya.
- [ ] **Dangling git stash needs a deliberate decision (new, surfaced 2026-07-11)** — `stash@{0}` contains ~+3174 lines touching `src/lib/api.ts` and several deal-room/contract-panel files, never popped. Its unstashed contents appear to be the origin of a silent regression this cycle (a concurrent edit reverted part of `api.ts` back toward this stash's pre-state, breaking already-shipped Contracts work until caught and fixed). Not actioned — dropping or popping a stash is destructive and needs explicit review, not an automated call. Owner: **Priya/Arjun** to inspect (`git stash show -p stash@{0}`) and decide pop vs. drop.

### Cleanup
- [x] **Deduplicate source trees** — triplicate `components/brand` vs `src/components/brand` vs `.claude/worktrees` copies. Owner: **Vikram** (architecture call). **DONE 2026-07-11 (`85e94d3`).** Canonical tree = `src/components/` (`@/` alias → `./src` only; tsconfig include = `src/**` only). Root-level `components/` (67 files, Next.js-era dupe) confirmed dead — zero `src/` imports, no CSS/asset imports, outside the Vite build — removed via `git rm`. `npm run build` PASS (17.27s) after removal proves nothing referenced it. `.claude/worktrees/**` are git worktrees (separate, left untouched).

---

## PART 2 — ADMIN (50% built / 15% live) — START ONLY AFTER PART 1 IS ALL `[x]`

### P0 — Ship blockers (security-critical, do first within Admin)
- [ ] **Add `/admin` route guard** — `src/App.tsx:409` has zero auth wrapper (comment literally says "demo data only until M2 backend lands"); the console that moves money and approves KYC is publicly routable today. Owner: **Vikram** (or Ananya, whoever owns routing). **Kabir sign-off mandatory** before merge — this is the single highest-risk open item in the app.
- [ ] **Wire `services/api-contracts.ts`** into the 7 dead admin data hooks — the client is complete and typed, just unused (e.g. real client commented out at `useBrandDetail.ts:163`). Owner: **Vikram** (confirm backend contract still matches) + **Ananya** (uncomment/wire hooks).

### P1 — Module wiring (work in this order)
- [ ] **Users (55%)** — `BrandProfile`/`CreatorProfile` UI is real; wire the stubbed actions (KYC approve etc.) to `api-contracts.ts`. Owner: **Ananya** + **Vikram**. Security: **Kabir** (KYC approval is compliance-adjacent).
- [ ] **Dashboard (55%)** — `usePulseData` is 100% mock; wire to the real WS/REST feed (UI is already WS-ready). Owner: **Ananya** + **Vikram**.
- [ ] **Moderation (45%)** — `FlagQueue` UI + tests are real; `moderationApi` exists but is unused, wire it up. Owner: **Ananya** + **Vikram**. QA: **Kavya** (existing tests need re-verification against live data, not mock).
- [ ] **Support (45%)** — `TicketList` UI is real; wire reply/assign actions to the backend. Owner: **Ananya** + **Vikram**.
- [ ] **Campaigns (45%)** — `CampaignTable` is currently read-only over mock; wire to real data. Owner: **Ananya** + **Vikram**.
- [ ] **Finance (40%)** — `FeeControlPanel` submit is a stub; `financeApi`/`escrowApi` are typed but unused. **Highest-risk item in Admin** — this configures platform fee/escrow money movement. Owner: **Vikram**. Security: **Kabir mandatory sign-off** (no maker-checker control exists yet — require one here, don't ship without it). QA: Kavya. Verify: Meera.

### Cross-cutting (run alongside Admin work, not blocking sequence)
- [ ] **Rotate AI provider keys** — `influora-ai/.env` holds live-shaped `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`SARVAM_API_KEY`. Confirmed not committed to git and gitignored, but plaintext-on-disk keys should be rotated on principle. Owner: **Kabir** to coordinate rotation and confirm no git-history leak.
- [ ] **Wire real CI** — currently only a Lighthouse perf check runs; add `mvn test`, `pytest`, `vitest`, `npm run build` to the pipeline so ~100 Java + 33 Python tests actually gate merges. Owner: **Meera**.

---

## Agent Roles in This Loop

| Agent | Role in this tracker |
|---|---|
| **Arjun** | Reads this file every cycle, assigns the next `[ ]` item to its owner(s), updates status |
| **Vikram** | Backend wiring/confirmation, architecture-adjacent decisions |
| **Ananya** | Frontend wiring — replace mock data/no-op handlers with real `api.*` calls |
| **Kabir** | Security review — mandatory on anything touching money, KYC, or auth (Finance, Users, `/admin` guard, contract mutations) |
| **Kavya** | QA gate — every item needs her PASS before it can be marked `[x]` |
| **Meera** | Build/local verification (`npm run build`, `mvn test`) — final gate before `[x]` |
| **Tara** | Every 10 minutes, refreshes `MASTER-BUSINESS-PLAN.md` §3.1/§3.3/§7 scorecards to match this file's actual closed items |
| **Priya** | Owns this file; arbitrates any architecture question Arjun escalates |

---

**Do not bump a percentage in `MASTER-BUSINESS-PLAN.md` without a corresponding `[x]` item here as evidence.** This file is the source of truth for what's actually done; the business plan is the reporting layer on top of it.
