# P2-17 — Production AI-spend ceiling / kill-switch

**Owner:** Rohan (proposal first) → Vikram (impl) · **Reviewers:** Kavya → Meera · **Priority:** P2 · **Depends on:** P0-1 + Rohan proposal
**Status:** 🔵 IN REVIEW (Rohan spec done, Vikram implementation done — awaiting Kavya QA → Meera real `pytest`)

## Goal
There is **no runtime AI-spend ceiling** anywhere — Meera chat + classify_site + backfill all bill against Anthropic/Google with no cap or kill-switch. Add one.

## Files
- `influora-ai/app/config.py` (ceiling/kill-switch config)
- `influora-ai/app/routes/chat.py` (or middleware — enforcement point)

## Steps
1. **Rohan:** proposal — max spend, per-workspace vs global, kill-switch mechanism (`wiki/decisions/budget-proposals/`).
2. **Vikram:** enforce — requests stop / degrade when ceiling hit or kill-switch active; log + alert.

## Acceptance criteria
- [ ] Rohan spend-model + kill-switch proposal signed off
- [ ] Requests hard-stop at ceiling / when kill-switch on
- [ ] Kavya QA · Meera verify

## Completion log
- Rohan · 2026-07-12 · spend-ceiling + kill-switch proposal written and signed off: `wiki/decisions/budget-proposals/2026-07-12-ai-spend-ceiling-and-killswitch.md` — global daily hard ceiling $15/day (all 3 routes), `AI_SPEND_KILL_SWITCH` env var (instant hard block, checked first), per-workspace $3/day soft cap (WARNING-only, chat route), monthly $300 informational (manual tracking, not code-enforced — documented per-process-counter limitation). Handing off to Vikram.
- Vikram · 2026-07-13 · Implemented per spec §3: new `influora-ai/app/costs/pricing.py` (`PRICING_TABLE` keyed by `CLAUDE_MODEL`/`GEMINI_MODEL`, `estimate_cost_usd` using `Decimal`, raises `ValueError` on an unpriced model instead of silently under-billing); `app/costs/spend_tracker.py` (module-level, per-process, `asyncio.Lock`-guarded `global_total`/`per_workspace` counter, resets on UTC day rollover — Phase-1 per-process limitation documented in the module docstring per spec §3.2/§4); `app/costs/gate.py` (`check_spend_gate()` — kill-switch checked first, then daily ceiling, zero provider calls on either trip). `config.py` Settings gained `ai_daily_spend_ceiling_usd`/`ai_spend_kill_switch`/`ai_workspace_daily_soft_cap_usd` via the existing `_get_float`/`_get_bool` helpers, defaults exactly per spec §3.5. Wired `check_spend_gate()` into all 3 in-scope routes: `routes/chat.py` (before `run_tool_loop`), `routes/analyze_site.py`'s classify_site path (before the scrape, so a blocked request skips the scrape too), and `routes/brand_safety.py` (before the Claude call) — confirmed via `BrandSafetyScoreService`/`BrandSafetyAiClient` that Java's `ScoreCalculationJob` reaches the Python AI service through exactly this `/internal/brand-safety` route, so no separate Java-side wiring was needed. Cost recording: chat.py and brand_safety.py use the existing `usage`/`final_usage` dicts; `GeminiProvider.classify_site` was extended with a best-effort `usage` field (from `response.usage_metadata`, getattr-based so a missing/renamed SDK field never breaks a successful classification) so `analyze_site.py` can record real Gemini cost too. Every successful call emits one `ai_spend` structured log line; chat.py additionally logs a WARNING (never blocks) when a workspace crosses the $3/day soft cap, per spec (chat is the only route with a reliable workspace_id today). New tests: `tests/costs/test_pricing.py`, `test_spend_tracker.py` (including UTC-day-rollover), `test_gate.py` (kill-switch precedence, ceiling math), and `tests/routes/test_ai_spend_gate.py` (route-level: kill-switch blocks with zero `_get_claude()` calls, ceiling breach blocks with zero calls, a normal under-ceiling request proceeds and the tracker's global total exactly matches `estimate_cost_usd` afterward) — 17 new tests, all green. Full `pytest`: **203 passed** (up from the previously-reported 186 green baseline + these 17, no regressions). Deferred exactly as the spec's §4 says to: no cross-instance shared ledger, no Slack/email paging, no hard per-workspace block, no code-enforced monthly cap — all Phase 2/manual-tracking per spec. Handing off to Kavya QA → Meera for the real verification run.
- Kavya · 2026-07-13 · **QA APPROVED.** Kill-switch checked first (gate.py:36, before ceiling check line 43) per spec §3.3 ordering, daily $15 ceiling enforced (`global_total >= ceiling`, line 45), resets at UTC midnight via `_roll_if_new_day_locked` (spend_tracker.py:43-52), per-workspace $3/day soft cap WARNING-logged chat-only (chat.py:239-244, not blocking per spec). Zero-spend-on-block guarantee verified in all 3 routes: `chat.py` gate at 107-117 (before `_get_claude()` line 129, before `run_tool_loop` line 154), `analyze_site.py` gate at 90-100 (before scrape/Gemini), `brand_safety.py` verified by test (test_ai_spend_gate.py:141-179 mocks `_get_claude`, asserts `assert_not_called()` on both kill-switch and ceiling breach). Decimal throughout (no float in money math): pricing.py uses `Decimal` for all cost math, spend_tracker.py stores `global_total`/`per_workspace` as `Decimal`, gate.py:44 converts settings float to `Decimal(str(...))` for comparison. Async lock prevents race conditions (every `_state` access guarded by `async with _lock`). Per-process limitation honestly documented (spend_tracker.py module docstring lines 1-17, "effective real ceiling is $x × worker_count" if >1 worker, Phase 2 shared-ledger suggestion flagged but out of scope per spec §4). Failure modes correct: unpriced model raises `ValueError` (fail-loud, test_pricing.py:36-38 verifies), Gemini `usage_metadata` absence doesn't break classify_site (gemini.py:158-168 getattr-based, returns `None` → `Decimal("0")` in cost log, no crash). Config defaults match spec exactly ($15.0, `false`, $3.0, config.py:192/195/198). Test coverage comprehensive: 17 new tests (unit + route-level integration), `test_ai_spend_gate.py` explicitly verifies zero-provider-calls on block and exact cost-tracking match. No issues found. All spec acceptance criteria met. Handing off to Meera for real `pytest` run (expect 203 passed, matching Vikram's claim).
- Meera · 2026-07-13 · **REAL VERIFICATION RUN.** Confirmed `pytest.ini` config (`testpaths = tests`, `asyncio_mode = auto`) and ran the project's actual `.venv` interpreter (`influora-ai/.venv/Scripts/python.exe -m pytest -q`), no prior-claimed numbers trusted. Full suite result: **203 passed, 1 warning in 18.22s** (the warning is a pre-existing pydantic `SkipValidation` deprecation notice, unrelated to this change) — exact match to Vikram's claimed 203/0 failures. Targeted run of the 4 named files (`tests/costs/test_pricing.py`, `tests/costs/test_spend_tracker.py`, `tests/costs/test_gate.py`, `tests/routes/test_ai_spend_gate.py`): **17 passed in 4.20s** — all green, matches the claimed 17 new tests exactly. No discrepancy found in either the full-suite or targeted numbers. **VERDICT: PASS, no regression.** Marking ✅ DONE in INDEX.md.
