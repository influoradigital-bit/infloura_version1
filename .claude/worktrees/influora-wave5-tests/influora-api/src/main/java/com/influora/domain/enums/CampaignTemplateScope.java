package com.influora.domain.enums;

/** B5 — {@code CampaignTemplate.scope}: SYSTEM (curated, free, read-only) vs CUSTOM (per-workspace, Pro-gated save). */
public enum CampaignTemplateScope {
    SYSTEM,
    CUSTOM
}
