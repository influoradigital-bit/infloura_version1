package com.influora.domain.enums;

/** Mirrors {@code MessageKind} in {@code src/lib/api.ts}. */
public enum DealMessageKind {
    text,
    system,
    proposal,
    contract,
    deliverable,
    payment,
    shipment
}
