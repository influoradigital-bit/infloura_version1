package com.influora.domain.enums;

/** Matches the `status` ENUM('PENDING','READY','FAILED','DELETED') on the `file_uploads` table (V1). */
public enum FileUploadStatus {
    PENDING,
    READY,
    FAILED,
    DELETED
}
